# 1.2.3 VS Code

Visual Studio Code — бесплатный редактор кода от Microsoft. Самый популярный выбор для JavaScript/TypeScript разработки.

---

## Почему VS Code?

| Плюсы | Минусы |
|-------|--------|
| Бесплатный | Потребляет больше памяти, чем Vim/Sublime |
| Огромная экосистема расширений | Может тормозить на слабых машинах |
| Встроенный терминал и Git | Требует настройки для максимальной продуктивности |
| Отличная поддержка JS/TS/Vue |  |
| Активное сообщество |  |

---

## Установка

### Windows

```bash
# Вариант 1: Установщик
# Скачать с https://code.visualstudio.com/

# Вариант 2: winget
winget install Microsoft.VisualStudioCode
```

### macOS

```bash
# Вариант 1: Установщик
# Скачать с https://code.visualstudio.com/

# Вариант 2: Homebrew
brew install --cask visual-studio-code
```

### Linux (Ubuntu/Debian)

```bash
# Через snap
sudo snap install code --classic

# Или через apt
sudo apt install code
```

### Проверка установки

После установки открой терминал и проверь:

```bash
code --version
```

---

## Интерфейс

```
┌─────────────────────────────────────────────────────────────┐
│  Меню                                                ─ □ ✕  │
├────────┬────────────────────────────────────────────────────┤
│        │  Вкладки файлов                                    │
│ Боковая├────────────────────────────────────────────────────┤
│ панель │                                                    │
│        │              Редактор кода                         │
│ Explorer                                                    │
│ Search │                                                    │
│ Git    │                                                    │
│ Debug  ├────────────────────────────────────────────────────┤
│ Extensions   Встроенный терминал                            │
│        │                                                    │
└────────┴────────────────────────────────────────────────────┘
```

**Основные области:**
- **Activity Bar** (слева) — переключение между панелями
- **Side Bar** — файлы, поиск, Git, расширения
- **Editor** — основная область редактирования
- **Panel** (снизу) — терминал, проблемы, вывод
- **Status Bar** (внизу) — информация о файле, ветке Git

---

## Горячие клавиши

Самые важные сочетания (учи постепенно).

> 💡 Горячие клавиши могут отличаться из-за расширений или кастомных настроек. Проверь свои: **File → Preferences → Keyboard Shortcuts** (или `Ctrl+K Ctrl+S` / `Cmd+K Cmd+S`). Там можно найти любую команду и увидеть/изменить её сочетание.

### Общие

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Ctrl+Shift+P` | `Cmd+Shift+P` | Командная палитра (главное меню) |
| `Ctrl+P` | `Cmd+P` | Быстрое открытие файла |
| `Ctrl+,` | `Cmd+,` | Настройки |
| `` Ctrl+` `` | `` Ctrl+` `` | Открыть/закрыть терминал |
| `Ctrl+B` | `Cmd+B` | Показать/скрыть боковую панель |
| `Ctrl+Shift+E` | `Cmd+Shift+E` | Фокус на проводник файлов |

### Редактирование

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Ctrl+D` | `Cmd+D` | Выделить следующее совпадение |
| `Ctrl+Shift+L` | `Cmd+Shift+L` | Выделить все совпадения |
| `Alt+↑/↓` | `Option+↑/↓` | Переместить строку вверх/вниз |
| `Shift+Alt+↑/↓` | `Shift+Option+↑/↓` | Копировать строку вверх/вниз |
| `Ctrl+/` | `Cmd+/` | Закомментировать строку |
| `Ctrl+Shift+K` | `Cmd+Shift+K` | Удалить строку |
| `Ctrl+Enter` | `Cmd+Enter` | Вставить строку ниже |
| `Ctrl+Shift+Enter` | `Cmd+Shift+Enter` | Вставить строку выше |
| `Ctrl+]` / `Ctrl+[` | `Cmd+]` / `Cmd+[` | Увеличить/уменьшить отступ |

### Навигация

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Ctrl+G` | `Cmd+G` | Перейти к строке |
| `Ctrl+Shift+O` | `Cmd+Shift+O` | Перейти к символу в файле |
| `F12` | `F12` | Перейти к определению |
| `Alt+F12` | `Option+F12` | Показать определение (peek) |
| `Shift+F12` | `Shift+F12` | Найти все ссылки |
| `Ctrl+Tab` | `Ctrl+Tab` | Переключение между вкладками |

### Мультикурсор

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Alt+Click` | `Option+Click` | Добавить курсор |
| `Ctrl+Alt+↑/↓` | `Cmd+Option+↑/↓` | Добавить курсор выше/ниже |

### 📝 Практика 1.2.3.1: Освой горячие клавиши

1. Открой любой файл в VS Code
2. Попробуй каждое сочетание из раздела "Редактирование"
3. Используй `Ctrl+D` чтобы выделить и заменить несколько слов одновременно
4. Перемести несколько строк с помощью `Alt+↑/↓`

---

## Расширения для MEVN стека

Открой панель расширений: `Ctrl+Shift+X` (или `Cmd+Shift+X` на macOS)

### Обязательные

| Расширение | Описание |
|------------|----------|
| **ESLint** | Проверка кода на ошибки и стиль |
| **Prettier** | Автоформатирование кода |
| **Vue - Official** | Поддержка Vue.js (подсветка, автодополнение) |
| **TypeScript Vue Plugin (Volar)** | TypeScript в Vue компонентах |

### Рекомендуемые

| Расширение | Описание |
|------------|----------|
| **GitLens** | Расширенная работа с Git (кто менял строку, история) |
| **Auto Rename Tag** | Автопереименование парных HTML-тегов |
| **Path Intellisense** | Автодополнение путей к файлам |
| **Error Lens** | Показывает ошибки прямо в коде |
| **Material Icon Theme** | Красивые иконки для файлов |
| **MongoDB for VS Code** | Работа с MongoDB прямо из редактора |

### Опциональные

| Расширение | Описание |
|------------|----------|
| **REST Client** | Тестирование API прямо в VS Code |
| **Thunder Client** | Альтернатива Postman внутри VS Code |
| **Code Spell Checker** | Проверка орфографии в коде |
| **TODO Highlight** | Подсветка TODO и FIXME комментариев |

### 📝 Практика 1.2.3.2: Установи расширения

1. Открой панель расширений (`Ctrl+Shift+X`)
2. Установи обязательные расширения: ESLint, Prettier, Vue - Official
3. Установи 2-3 рекомендуемых на свой выбор
4. Перезагрузи VS Code

---

## Настройка

### Файл настроек

VS Code хранит настройки в JSON. Открой: `Ctrl+Shift+P` → "Preferences: Open Settings (JSON)"

Рекомендуемые настройки для начала:

```json
{
  // Форматирование
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",

  // Отступы
  "editor.tabSize": 2,
  "editor.insertSpaces": true,

  // Шрифт
  "editor.fontSize": 14,
  "editor.fontFamily": "'Fira Code', 'Cascadia Code', Consolas, monospace",
  "editor.fontLigatures": true,

  // Внешний вид
  "editor.minimap.enabled": false,
  "editor.wordWrap": "on",
  "editor.bracketPairColorization.enabled": true,

  // Файлы
  "files.autoSave": "onFocusChange",
  "files.trimTrailingWhitespace": true,
  "files.insertFinalNewline": true,

  // Терминал
  "terminal.integrated.fontSize": 13,

  // Git
  "git.confirmSync": false,
  "git.autofetch": true,

  // ESLint
  "eslint.validate": ["javascript", "typescript", "vue"],

  // Vue
  "[vue]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

### Prettier конфигурация

Создай файл `.prettierrc` в корне проекта:

```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

### 📝 Практика 1.2.3.3: Настрой VS Code

1. Открой настройки JSON (`Ctrl+Shift+P` → "Open Settings JSON")
2. Добавь настройки из примера выше
3. Создай тестовый файл `test.js` с неформатированным кодом:

```javascript
const user={name:"John",age:25,email:"john@example.com"}
function sayHello(name){console.log("Hello, "+name+"!")}
const numbers=[1,2,3,4,5]
numbers.forEach(function(n){console.log(n)})
```

4. Сохрани файл — код должен автоматически отформатироваться в:

```javascript
const user = { name: 'John', age: 25, email: 'john@example.com' };
function sayHello(name) {
  console.log('Hello, ' + name + '!');
}
const numbers = [1, 2, 3, 4, 5];
numbers.forEach(function (n) {
  console.log(n);
});
```

---

## Работа с проектом

### Открытие проекта

```bash
# Открыть папку в VS Code
code /path/to/project

# Открыть текущую папку
code .

# Открыть файл
code file.js
```

### Workspace (рабочая область)

Если работаешь с несколькими связанными проектами:

1. File → Add Folder to Workspace
2. File → Save Workspace As...

Это создаст `.code-workspace` файл с настройками для всех папок.

### .vscode папка

В каждом проекте можно создать папку `.vscode/` с настройками:

```
project/
├── .vscode/
│   ├── settings.json    # Настройки для этого проекта
│   ├── extensions.json  # Рекомендуемые расширения
│   └── launch.json      # Конфигурация отладки
├── src/
└── package.json
```

**extensions.json** — рекомендации по расширениям для команды:

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "vue.volar"
  ]
}
```

---

## Встроенный терминал

### Основные команды

| Действие | Сочетание |
|----------|-----------|
| Открыть/закрыть | `` Ctrl+` `` |
| Новый терминал | ``Ctrl+Shift+` `` |
| Разделить терминал | `Ctrl+Shift+5` |
| Переключиться между терминалами | `Ctrl+PageUp/PageDown` |
| Очистить | `Ctrl+K` (в терминале) |

### Настройка shell по умолчанию

`Ctrl+Shift+P` → "Terminal: Select Default Profile"

Выбери:
- **Windows**: Git Bash или PowerShell
- **macOS/Linux**: zsh или bash

---

## Встроенный Git

VS Code имеет отличную интеграцию с Git.

### Source Control панель

Открой: `Ctrl+Shift+G`

Здесь ты можешь:
- Видеть изменённые файлы
- Делать stage/unstage
- Писать commit message
- Делать push/pull

### Полезные функции

- **Gutter indicators** — цветные полоски слева показывают изменения
- **Inline diff** — клик на изменённую строку показывает разницу
- **Timeline** — история изменений файла (внизу Explorer)

### 📝 Практика 1.2.3.4: Git в VS Code

1. Открой репозиторий из практики Git
2. Измени любой файл
3. Открой Source Control (`Ctrl+Shift+G`)
4. Посмотри diff изменений (клик на файл)
5. Сделай stage и commit через интерфейс VS Code

---

## Отладка (Debug)

### Базовая отладка Node.js

1. Открой файл `.js`
2. Поставь breakpoint (клик слева от номера строки)
3. `F5` или Run → Start Debugging
4. Выбери "Node.js"

### launch.json

Для более сложной отладки создай `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Run Server",
      "program": "${workspaceFolder}/server.js"
    }
  ]
}
```

---

## Полезные фишки

### Emmet

VS Code поддерживает Emmet для быстрого написания HTML:

```
div.container>ul>li*3
```

Нажми `Tab` и получишь:

```html
<div class="container">
  <ul>
    <li></li>
    <li></li>
    <li></li>
  </ul>
</div>
```

### Snippets (сниппеты)

Создай свои шаблоны кода: `Ctrl+Shift+P` → "Snippets: Configure User Snippets"

### Zen Mode

Режим без отвлечений: `Ctrl+K Z` (выход — `Esc Esc`)

### Command Palette

`Ctrl+Shift+P` — главный инструмент. Любое действие можно найти здесь.

---

## Ресурсы

- 📖 [Официальная документация](https://code.visualstudio.com/docs)
- 📹 [VS Code Tips and Tricks](https://code.visualstudio.com/docs/getstarted/tips-and-tricks)
- 🎮 [Keyboard Shortcuts PDF](https://code.visualstudio.com/shortcuts/keyboard-shortcuts-windows.pdf)
- 📖 [Awesome VS Code](https://github.com/viatsko/awesome-vscode) — список расширений

---

## Чеклист

После изучения ты должен уметь:

- [ ] Установить VS Code и базовые расширения
- [ ] Использовать горячие клавиши для редактирования
- [ ] Настроить автоформатирование (Prettier + ESLint)
- [ ] Работать с Git через интерфейс VS Code
- [ ] Использовать встроенный терминал
- [ ] Открывать проект из командной строки (`code .`)

---

⏱ **Время на изучение**: 1-2 часа

📝 **Практик**: 4
