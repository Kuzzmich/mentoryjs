# 2.1.3 Условия и циклы

Управление потоком выполнения — сердце любой программы. Условия позволяют принимать решения, циклы — повторять действия.

---

## 🎯 Что ты узнаешь

- Условные операторы: `if/else`, `else if`
- Оператор `switch` для множественного выбора
- Тернарный оператор для коротких условий
- Циклы: `for`, `while`, `do...while`
- Специальные циклы: `for...of` и `for...in`
- Управление циклами: `break` и `continue`
- Когда использовать каждый тип цикла

---

## Условные операторы

### if / else

Базовая конструкция для принятия решений:

```javascript
let temp = 25;

if (temp > 30) {
  console.log("Жарко");
} else if (temp > 20) {
  console.log("Тепло");
} else if (temp > 10) {
  console.log("Прохладно");
} else {
  console.log("Холодно");
}
```

**Важно:** условие приводится к boolean. Ложные значения: `false`, `0`, `""`, `null`, `undefined`, `NaN`.

```javascript
// Короткая запись для одного действия
if (temp > 30) console.log("Жарко");

// С блоком (рекомендуется)
if (temp > 30) {
  console.log("Жарко");
}
```

### Тернарный оператор

Короткая запись `if-else` для присваивания значения:

```javascript
// Вместо if-else
let message;
if (temp > 20) {
  message = "Тепло";
} else {
  message = "Холодно";
}

// Тернарный оператор
let message = temp > 20 ? "Тепло" : "Холодно";

// Можно вкладывать (но осторожно)
let status = temp > 30 ? "Жарко" : temp > 20 ? "Тепло" : "Холодно";
```

> 💡 Используй тернарный оператор для простых случаев. Если логика сложная — лучше `if-else`.

### switch

Для множественного выбора:

```javascript
let weatherCode = "rain";

switch (weatherCode) {
  case "clear":
    console.log("☀️ Ясно");
    break;
  case "clouds":
    console.log("☁️ Облачно");
    break;
  case "rain":
    console.log("🌧 Дождь");
    break;
  case "snow":
    console.log("❄️ Снег");
    break;
  default:
    console.log("🌡 Неизвестно");
}
```

> ⚠️ **Важно:** `break` прерывает выполнение. Без него код "провалится" в следующий case.

```javascript
// Группировка case
switch (weatherCode) {
  case "clear":
  case "sunny":
    console.log("☀️ Солнечно");
    break;
  case "rain":
  case "drizzle":
    console.log("🌧 Осадки");
    break;
}
```

---

## Циклы

### for

Классический цикл с счётчиком:

```javascript
// Базовый синтаксис
for (let i = 0; i < 5; i++) {
  console.log(`День ${i + 1}`);
}
// День 1, День 2, День 3, День 4, День 5

// Итерация по массиву
let temperatures = [20, 22, 25, 23, 21];
for (let i = 0; i < temperatures.length; i++) {
  console.log(`Температура: ${temperatures[i]}°C`);
}
```

**Структура:**
- `let i = 0` — инициализация (выполняется 1 раз)
- `i < 5` — условие (проверяется перед каждой итерацией)
- `i++` — шаг (выполняется после каждой итерации)

### while

Цикл с предусловием:

```javascript
let count = 0;
while (count < 5) {
  console.log(`Итерация ${count}`);
  count++;
}

// Осторожно с бесконечными циклами!
let isRaining = true;
while (isRaining) {
  // Обязательно измени условие, иначе зависнет
  // isRaining = false;
}
```

### do...while

Цикл с постусловием (выполнится минимум 1 раз):

```javascript
let i = 0;
do {
  console.log(`Итерация ${i}`);
  i++;
} while (i < 5);

// Полезно для меню/повторных действий
let input;
do {
  input = prompt("Введи команду (exit для выхода):");
} while (input !== "exit");
```

### for...of

Итерация по значениям массива:

```javascript
let cities = ["Москва", "Петербург", "Казань"];

for (let city of cities) {
  console.log(city);
}
// Москва
// Петербург
// Казань

// С индексом через entries()
for (let [index, city] of cities.entries()) {
  console.log(`${index + 1}. ${city}`);
}
```

> 💡 **for...of** — лучший выбор для итерации по массивам. Проще и читабельнее.

### for...in

Итерация по ключам объекта:

```javascript
let weather = {
  city: "Москва",
  temp: 25,
  humidity: 65
};

for (let key in weather) {
  console.log(`${key}: ${weather[key]}`);
}
// city: Москва
// temp: 25
// humidity: 65
```

> ⚠️ **Не используй for...in для массивов** — он может вернуть дополнительные свойства.

---

## Управление циклами

### break

Прерывает цикл:

```javascript
// Найти первый день с температурой > 25
let temperatures = [20, 22, 28, 23, 30];
for (let i = 0; i < temperatures.length; i++) {
  if (temperatures[i] > 25) {
    console.log(`Нашли жару на день ${i + 1}: ${temperatures[i]}°C`);
    break;  // прерываем цикл
  }
}
```

### continue

Пропускает текущую итерацию:

```javascript
// Вывести только положительные температуры
let temperatures = [5, -3, 10, -1, 15];
for (let temp of temperatures) {
  if (temp < 0) {
    continue;  // пропускаем отрицательные
  }
  console.log(`${temp}°C`);
}
// 5°C
// 10°C
// 15°C
```

---

## 🔧 Практика 2.1.3.1: Добавляем логику в погодное приложение

Продолжаем развивать погодное приложение. Добавим условную логику для отображения статуса погоды.

**Задание:**

Открой файлы `index.html` и `script.js` из предыдущей практики.

В `script.js` добавь после существующего кода:

```javascript
// TODO 1: Определи статус погоды на основе температуры
// Создай переменную weatherStatus
// Если temp > 30: "Жарко 🥵"
// Если temp > 20: "Тепло ☀️"
// Если temp > 10: "Прохладно 🍂"
// Иначе: "Холодно ❄️"
// Используй if/else if/else

// TODO 2: Определи, нужен ли зонт
// Создай переменную umbrellaAdvice
// Если в description есть слово "дождь" (используй .toLowerCase() и .includes()):
//   "Возьми зонт ☔"
// Иначе:
//   "Зонт не нужен"

// TODO 3: Определи комфортность по влажности
// Создай переменную humidityStatus
// Если humidity > 80: "Высокая влажность 💧"
// Если humidity > 60: "Комфортная влажность"
// Иначе: "Низкая влажность"

// TODO 4: Выведи все статусы в консоль красиво
// Формат:
// === АНАЛИЗ ПОГОДЫ ===
// Температура: X°C
// Статус: [weatherStatus]
// Совет: [umbrellaAdvice]
// Влажность: [humidityStatus]
```

**Проверь себя:**
- Измени `temperature` и `description` — статусы должны меняться
- Попробуй разные варианты: "Дождь", "дождливо", "ДОЖДЬ" — все должны работать

---

## 🔧 Практика 2.1.3.2: Прогноз на неделю

Добавим массив с температурами на неделю и рассчитаем статистику.

```javascript
// Прогноз температур на 7 дней
const weekForecast = [18, 20, 22, 19, 17, 21, 23];
const daysOfWeek = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"];

// TODO 1: Выведи прогноз на каждый день
// Формат: "Пн: 18°C"
// Используй цикл for или for...of

// TODO 2: Найди максимальную температуру
// Создай переменную maxTemp, начни с первого элемента
// Пройди циклом по массиву и обнови maxTemp если нашёл больше

// TODO 3: Найди минимальную температуру
// Аналогично maxTemp

// TODO 4: Рассчитай среднюю температуру
// Сумма всех температур / количество дней
// Подсказка: используй цикл для суммирования

// TODO 5: Посчитай тёплые дни (temp >= 20)
// Создай счётчик, увеличивай его в цикле при условии

// TODO 6: Выведи итоговую статистику
// Формат:
// === ПРОГНОЗ НА НЕДЕЛЮ ===
// Макс: X°C
// Мин: Y°C
// Средняя: Z°C
// Тёплых дней: N
```

**Ожидаемый результат:**
```
Пн: 18°C
Вт: 20°C
...
=== ПРОГНОЗ НА НЕДЕЛЮ ===
Макс: 23°C
Мин: 17°C
Средняя: 20°C
Тёплых дней: 4
```

---

## 🔧 Практика 2.1.3.3: Интерактивный выбор города

Создадим простое меню для выбора города из списка.

```javascript
// База данных городов с погодой
const citiesWeather = {
  "Москва": { temp: 15, description: "Облачно" },
  "Санкт-Петербург": { temp: 12, description: "Дождь" },
  "Казань": { temp: 18, description: "Ясно" },
  "Сочи": { temp: 25, description: "Солнечно" }
};

// TODO 1: Выведи список доступных городов
// Формат:
// Доступные города:
// 1. Москва
// 2. Санкт-Петербург
// ...
// Используй for...in и счётчик

// TODO 2: Найди самый тёплый город
// Пройди по объекту, сравни температуры
// Выведи: "Самый тёплый город: Сочи (25°C)"

// TODO 3: Найди города с хорошей погодой
// Условие: temp >= 18 И description НЕ содержит "дождь"
// Выведи список таких городов

// TODO 4: Создай функцию отображения погоды (без создания функции пока!)
// Просто скопируй код для одного города и адаптируй для вывода любого
// Используй switch для эмодзи погоды:
// "Ясно" / "Солнечно" → ☀️
// "Облачно" → ☁️
// "Дождь" → 🌧️
// default → 🌡️
```

---

## 📝 Итоговое задание

Создай систему анализа почасового прогноза погоды.

```javascript
// Температура каждые 3 часа (8 значений = 24 часа)
const hourlyTemp = [10, 12, 15, 18, 20, 18, 14, 11];
const hours = ["00:00", "03:00", "06:00", "09:00", "12:00", "15:00", "18:00", "21:00"];

// TODO 1: Выведи почасовой прогноз
// Формат: "00:00 - 10°C"

// TODO 2: Определи время суток с максимальной температурой
// Выведи: "Пик тепла в 12:00: 20°C"

// TODO 3: Найди самое большое изменение температуры между соседними измерениями
// Например: между 15°C и 20°C изменение = 5°C
// Выведи: "Максимальное изменение: 5°C"

// TODO 4: Определи тренд температуры (растёт, падает, стабильно)
// Сравни первую половину дня (4 измерения) со второй
// Если средняя во второй половине выше — "Теплеет"
// Если ниже — "Холодает"
// Если разница < 2°C — "Стабильно"

// TODO 5: Найди "комфортные" часы (temp между 15 и 20)
// Выведи список таких часов

// TODO 6: Создай текстовый график (бонус, необязательно)
// Пример:
// 00:00 ■■■■■
// 03:00 ■■■■■■
// 06:00 ■■■■■■■■
// (каждый ■ = 2°C)
```

**Подсказки:**
- Для максимума: перебери массив, храни индекс максимального элемента
- Для изменений: сравни `hourlyTemp[i]` с `hourlyTemp[i + 1]`
- Для тренда: посчитай среднее первой и второй половины отдельно
- Для графика: используй цикл внутри цикла или метод `.repeat()`

---

## ✅ Чеклист раздела

После изучения темы ты должен:

- [ ] Использовать `if/else` для условной логики
- [ ] Применять тернарный оператор для простых случаев
- [ ] Использовать `switch` для множественного выбора
- [ ] Писать циклы `for` с правильной структурой
- [ ] Использовать `for...of` для итерации по массивам
- [ ] Использовать `for...in` для итерации по объектам
- [ ] Понимать разницу между `while` и `do...while`
- [ ] Применять `break` и `continue` по необходимости
- [ ] Находить максимум/минимум/сумму в массиве
- [ ] Фильтровать данные по условию

---

## 🔗 Ресурсы

- 📖 [Learn JavaScript: Условные операторы](https://learn.javascript.ru/ifelse)
- 📖 [Learn JavaScript: Циклы](https://learn.javascript.ru/while-for)
- 📖 [MDN: if...else](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/if...else)
- 📖 [MDN: switch](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/switch)
- 📖 [MDN: for](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/for)
- 📖 [MDN: for...of](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/for...of)

---

⏱ **Время на изучение**: 4-5 часов

---

**Предыдущая тема:** [2.1.2 Массивы и объекты](./2.1.2-Массивы-и-объекты.md)

**Следующая тема:** [2.1.4 Функции и области видимости](./2.1.4-Функции-и-области-видимости.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](ссылка)
