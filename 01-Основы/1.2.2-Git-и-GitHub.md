# 1.2.2 Git и GitHub

## Зачем нужен Git?

**Git** — система контроля версий. Решает три проблемы:

| Проблема | Решение Git |
|----------|-------------|
| "Случайно удалил код" | Откат к любой версии |
| "Кто это написал?" | История изменений с авторами |
| "Как работать вдвоём над одним файлом?" | Ветки и слияние |

**GitHub** — облачный хостинг для Git-репозиториев + соцсеть для разработчиков.

---

## Установка Git

### Windows

```bash
# Вариант 1: Установщик
# Скачать с https://git-scm.com/download/win

# Вариант 2: winget
winget install Git.Git

# После установки — перезапустить терминал
```

### macOS

```bash
# Вариант 1: Xcode Command Line Tools (установится автоматически)
git --version

# Вариант 2: Homebrew
brew install git
```

### Linux (Ubuntu/Debian)

```bash
sudo apt update && sudo apt install git
```

### Проверка установки

```bash
git --version
# git version 2.43.0
```

---

## Первоначальная настройка

> ⚠️ **Обязательно** выполни после установки:

```bash
# Имя и email (будут в каждом коммите)
git config --global user.name "Твоё Имя"
git config --global user.email "твой@email.com"

# Основная ветка main (не master)
git config --global init.defaultBranch main

# Проверить настройки
git config --list
```

---

## Базовые концепции

```
┌─────────────────────────────────────────────────────────────┐
│                      Рабочий процесс                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Working Directory    Staging Area       Repository        │
│   (рабочая папка)      (индекс)           (история)         │
│                                                             │
│   ┌──────────┐        ┌──────────┐       ┌──────────┐      │
│   │ файлы    │ ──────▶│ готовые  │ ─────▶│ коммиты  │      │
│   │ проекта  │ git add│ к коммиту│ commit│ (снимки) │      │
│   └──────────┘        └──────────┘       └──────────┘      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Коммит** — снимок состояния проекта в определённый момент.

---

## Основные команды

### Создание репозитория

```bash
# Создать новый репозиторий в текущей папке
git init

# Или клонировать существующий
git clone https://github.com/user/repo.git
```

### Ежедневная работа

```bash
# Посмотреть статус (что изменилось)
git status

# Добавить файл в индекс
git add filename.js

# Добавить все изменённые файлы
git add .

# Создать коммит
git commit -m "Добавил функцию авторизации"

# Посмотреть историю
git log --oneline
```

### 📝 Практика 1.2.2.1: Первый репозиторий

1. Создай папку `git-practice` и перейди в неё
2. Инициализируй репозиторий: `git init`
3. Создай файл `README.md` с текстом "# Мой проект"
4. Выполни `git status` — файл должен быть "untracked"
5. Добавь в индекс: `git add README.md`
6. Выполни `git status` — файл должен быть "staged"
7. Сделай коммит: `git commit -m "Initial commit"`
8. Проверь историю: `git log --oneline`

---

## Работа с ветками

**Ветка** — независимая линия разработки.

```bash
# Список веток (* — текущая)
git branch

# Создать новую ветку
git branch feature-login

# Переключиться на ветку
git checkout feature-login
# или (Git 2.23+)
git switch feature-login

# Создать и сразу переключиться
git checkout -b feature-login
# или
git switch -c feature-login

# Слить ветку в текущую
git merge feature-login

# Удалить ветку (после слияния)
git branch -d feature-login
```

### Типичный workflow

```
main ─────●─────────────●─────────●───────▶
           \           /
feature     └────●────●
               работа  merge
```

### 📝 Практика 1.2.2.2: Работа с ветками

1. В репозитории из практики 1.2.2.1:
2. Создай ветку `feature-about`: `git switch -c feature-about`
3. Создай файл `about.md` с описанием проекта
4. Закоммить: `git add . && git commit -m "Добавил about"`
5. Вернись на main: `git switch main`
6. Убедись, что `about.md` исчез (он в другой ветке)
7. Слей ветку: `git merge feature-about`
8. Теперь `about.md` есть в main

---

## Работа с GitHub

### Регистрация на GitHub

1. Зайди на [github.com](https://github.com) → "Sign up"
2. Введи email, пароль, username
3. Подтверди email

### Создание репозитория на GitHub

1. Нажми "+" → "New repository"
2. Введи имя (например, `my-first-project`)
3. **НЕ** ставь галочку "Add a README file" (создадим локально)
4. Нажми "Create repository"
5. GitHub покажет инструкции — скопируй URL репозитория

---

## Git Remote — связь с GitHub

**Remote** (удалённый репозиторий) — копия проекта на сервере (GitHub, GitLab, Bitbucket).

```bash
# Посмотреть список удалённых репозиториев
git remote

# Подробнее (с URL)
git remote -v

# Добавить удалённый репозиторий
git remote add origin https://github.com/username/repo.git

# Изменить URL (если ошибся или переходишь на SSH)
git remote set-url origin https://github.com/username/new-repo.git

# Удалить связь
git remote remove origin
```

**origin** — стандартное имя для основного удалённого репозитория. Можно использовать любое имя, но `origin` — общепринятое.

### Отправка и получение кода

```bash
# Отправить изменения на GitHub (первый раз)
git push -u origin main
# -u (--set-upstream) запоминает связь ветки с remote

# Последующие пуши (ветка уже связана)
git push

# Получить изменения с GitHub
git pull

# Только скачать изменения (без слияния)
git fetch
```

---

## Аутентификация на GitHub

### Вариант 1: HTTPS + Personal Access Token

GitHub больше не принимает пароли. Нужен токен.

**Создание токена:**

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. "Generate new token (classic)"
3. Название: `my-laptop` (любое)
4. Expiration: 90 days (или No expiration)
5. Scopes: ✅ `repo` (полный доступ к репозиториям)
6. "Generate token"
7. **Скопируй токен сразу!** Он покажется только один раз

**Использование:**

При первом `git push` Git спросит логин и пароль:
- Username: твой GitHub username
- Password: **вставь токен** (не пароль!)

> 💡 **Совет:** чтобы не вводить токен каждый раз, включи кэширование: `git config --global credential.helper store`. После первого ввода токен сохранится.

### Вариант 2: SSH-ключи (рекомендуется)

Безопаснее и удобнее — не нужно вводить токен.

**Генерация ключа:**

```bash
# Создать SSH-ключ
ssh-keygen -t ed25519 -C "твой@email.com"
# Нажми Enter на все вопросы (или задай пароль для ключа)

# Запустить ssh-agent
eval "$(ssh-agent -s)"

# Добавить ключ
ssh-add ~/.ssh/id_ed25519
```

**Добавление ключа на GitHub:**

1. Скопируй публичный ключ:

```bash
# macOS
cat ~/.ssh/id_ed25519.pub | pbcopy

# Linux
cat ~/.ssh/id_ed25519.pub
# скопируй вручную

# Windows (Git Bash)
cat ~/.ssh/id_ed25519.pub | clip
```

2. GitHub → Settings → SSH and GPG keys → "New SSH key"
3. Title: `my-laptop`
4. Key: вставь скопированный ключ
5. "Add SSH key"

**Проверка:**

```bash
ssh -T git@github.com
# Hi username! You've successfully authenticated...
```

**Использование SSH URL:**

```bash
# Вместо HTTPS
git remote set-url origin git@github.com:username/repo.git

# Или при клонировании
git clone git@github.com:username/repo.git
```

---

## Типичные сценарии

### Сценарий 1: Новый проект → GitHub

```bash
# 1. Создай папку и инициализируй Git
mkdir my-project && cd my-project
git init

# 2. Создай файлы, сделай коммит
echo "# My Project" > README.md
git add .
git commit -m "Initial commit"

# 3. Создай репозиторий на GitHub (без README!)

# 4. Свяжи и запушь
git remote add origin https://github.com/username/my-project.git
git push -u origin main
```

### Сценарий 2: Клонировать существующий репозиторий

```bash
# HTTPS
git clone https://github.com/username/repo.git

# SSH
git clone git@github.com:username/repo.git

# Клонировать в конкретную папку
git clone https://github.com/username/repo.git my-folder
```

### Сценарий 3: Подключить существующий проект к новому репозиторию

```bash
# Если уже есть origin, сначала удали
git remote remove origin

# Добавь новый
git remote add origin https://github.com/username/new-repo.git

# Запушь
git push -u origin main
```

### 📝 Практика 1.2.2.3: Подключение к GitHub и Push

1. Зарегистрируйся на GitHub (если ещё нет)
2. Создай Personal Access Token (Settings → Developer settings → Tokens)
3. Создай пустой репозиторий `git-practice` на GitHub
4. В локальном репозитории из практики 1.2.2.1:
   - Проверь remote: `git remote -v` (должно быть пусто)
   - Добавь origin: `git remote add origin <URL>`
   - Проверь снова: `git remote -v`
   - Запушь: `git push -u origin main`
   - Введи username и токен
5. Обнови страницу репозитория на GitHub — код должен появиться

---

## Файл .gitignore

Указывает, какие файлы НЕ отслеживать.

**Зачем?**
- Не засорять репозиторий временными файлами
- Не выкладывать секреты (пароли, ключи API)
- Не пушить системные файлы

```bash
# Секреты (пароли, ключи API) — НИКОГДА не коммить!
.env
secrets.txt
config.local.js

# Логи
*.log
logs/

# Системные файлы
.DS_Store        # macOS
Thumbs.db        # Windows

# Временные файлы
temp/
*.tmp

# IDE (настройки редактора)
.idea/           # WebStorm
.vscode/         # VS Code

# Позже добавишь (когда изучишь Node.js):
# node_modules/  # зависимости npm
# dist/          # сборка проекта
```

> ⚠️ **Важно**: добавь `.gitignore` ДО первого коммита с этими файлами. Если файл уже закоммичен, `.gitignore` на него не подействует.

### 📝 Практика 1.2.2.4: Настройка .gitignore

1. В репозитории из практики 1.2.2.1 создай файл `.gitignore`
2. Добавь в него три строки: `*.log`, `temp/`, `.env`
3. Создай тестовые файлы:
   - `debug.log` (файл с любым текстом)
   - папку `temp/` с файлом `test.txt` внутри
   - `notes.txt` (обычный файл)
4. Выполни `git status`:
   - `debug.log` и `temp/` — НЕ должны отображаться
   - `notes.txt` и `.gitignore` — должны быть в списке
5. Закоммить: `git add .gitignore && git commit -m "Добавил gitignore"`

---

## Полезные команды

```bash
# Отменить изменения в файле (до add)
git checkout -- filename.js
# или (Git 2.23+)
git restore filename.js

# Убрать файл из индекса (после add, до commit)
git reset HEAD filename.js
# или
git restore --staged filename.js

# Изменить последний коммит (сообщение или добавить файлы)
git commit --amend -m "Новое сообщение"

# Посмотреть изменения
git diff              # рабочая директория vs индекс
git diff --staged     # индекс vs последний коммит

# Посмотреть историю красиво
git log --oneline --graph --all
```

---

## Конфликты при слиянии

Возникают, когда один и тот же участок кода изменён в двух ветках.

```bash
<<<<<<< HEAD
const name = "Alice";    ← твои изменения
=======
const name = "Bob";      ← изменения из другой ветки
>>>>>>> feature-branch
```

**Решение**:

1. Открой файл с конфликтом
2. Выбери нужный вариант (или объедини оба)
3. Удали маркеры `<<<<<<<`, `=======`, `>>>>>>>`
4. Сохрани и закоммить: `git add . && git commit`

---

## Шпаргалка команд

| Команда | Описание |
|---------|----------|
| `git init` | Создать репозиторий |
| `git clone <url>` | Клонировать репозиторий |
| `git status` | Статус изменений |
| `git add <file>` | Добавить в индекс |
| `git commit -m "msg"` | Создать коммит |
| `git remote -v` | Показать удалённые репозитории |
| `git remote add origin <url>` | Добавить удалённый репозиторий |
| `git remote set-url origin <url>` | Изменить URL репозитория |
| `git push -u origin main` | Первый пуш (связать ветку) |
| `git push` | Отправить на сервер |
| `git pull` | Получить с сервера |
| `git fetch` | Скачать изменения (без слияния) |
| `git branch` | Список веток |
| `git switch <branch>` | Переключить ветку |
| `git merge <branch>` | Слить ветку |
| `git log --oneline` | История коммитов |
| `git diff` | Показать изменения |
| `git stash` | Спрятать изменения временно |
| `git stash pop` | Достать спрятанное |

---

## Ресурсы

- 📖 [Git How To](https://githowto.com/ru) — интерактивный тур
- 📖 [Pro Git Book](https://git-scm.com/book/ru/v2) — подробная книга (бесплатно)
- 🎮 [Learn Git Branching](https://learngitbranching.js.org/?locale=ru_RU) — визуальный тренажёр
- 📹 [Git для начинающих (YouTube)](https://www.youtube.com/results?search_query=git+для+начинающих)
- 📖 [GitHub Docs](https://docs.github.com/ru) — официальная документация

---

## Чеклист

После изучения ты должен уметь:

- [ ] Создать репозиторий (`git init`)
- [ ] Делать коммиты с осмысленными сообщениями
- [ ] Создавать и переключать ветки
- [ ] Сливать ветки (`git merge`)
- [ ] Подключить локальный репозиторий к GitHub (`git remote add`)
- [ ] Настроить аутентификацию (токен или SSH)
- [ ] Пушить код на GitHub (`git push`)
- [ ] Клонировать репозиторий (`git clone`)
- [ ] Настроить `.gitignore`
- [ ] Понимать, что делать при конфликте

---

⏱ **Время на изучение**: 3-4 часа

📝 **Практик**: 4
