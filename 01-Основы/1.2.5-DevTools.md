# 1.2.5 DevTools браузера

DevTools (Developer Tools) — встроенные инструменты разработчика в браузере. Незаменимый инструмент для отладки, анализа и оптимизации веб-приложений.

---

## 🎯 Что ты узнаешь

- Как открывать и использовать DevTools
- Вкладка Elements: инспектирование и редактирование HTML/CSS
- Вкладка Console: выполнение JavaScript и отладка
- Вкладка Sources: пошаговая отладка с breakpoints
- Вкладка Network: анализ HTTP-запросов и производительности
- Вкладка Application: работа с Storage, Cookies, Cache
- Mobile режим для тестирования адаптивности
- Полезные горячие клавиши и трюки

---

## Как открыть

| Способ | Windows/Linux | macOS |
|--------|---------------|-------|
| Горячая клавиша | `F12` или `Ctrl+Shift+I` | `Cmd+Option+I` |
| Правый клик | "Inspect" / "Просмотреть код" | "Inspect" / "Просмотреть код" |
| Меню | Menu → More Tools → Developer Tools | Menu → More Tools → Developer Tools |
| На элементе | Right-click → Inspect | Right-click → Inspect |

> 💡 Рекомендую Chrome или Edge — у них самые мощные DevTools. Firefox тоже хорош. Я использую Chrome — примеры в этом разделе на нём. Сам я юзаю Chrome

---

## Основные вкладки

```
┌─────────────────────────────────────────────────────────────────────┐
│ Elements │ Console │ Sources │ Network │ Application │ ...       ✕  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                      Содержимое вкладки                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

| Вкладка | Для чего |
|---------|----------|
| **Elements** | Просмотр и редактирование HTML/CSS |
| **Console** | JavaScript консоль, логи, ошибки |
| **Sources** | Исходный код, отладка, breakpoints |
| **Network** | HTTP-запросы, загрузка ресурсов |
| **Application** | localStorage, cookies, кэш |
| **Performance** | Анализ производительности |
| **Lighthouse** | Аудит страницы (скорость, SEO, доступность) |

---

## Elements — работа с DOM и CSS

### Навигация по DOM

- **Стрелки** — раскрыть/свернуть элементы
- **Ctrl+F** (`Cmd+F`) — поиск по DOM
- **Right-click** на элементе — меню действий

### Редактирование HTML

1. Двойной клик на тексте — редактировать текст
2. Right-click → "Edit as HTML" — редактировать весь элемент
3. Перетаскивание — перемещение элементов

### Редактирование CSS

В правой панели (Styles):
- Клик на значение — изменить
- Клик на свойство — отключить (чекбокс)
- Клик на пустое место в правиле — добавить свойство
- `+` — добавить новое правило

### Box Model

Внизу панели Styles — визуальная модель блока:
- **margin** (оранжевый)
- **border** (жёлтый)
- **padding** (зелёный)
- **content** (синий)

Наведи на значение — увидишь подсветку на странице.

### 📝 Практика 1.2.5.1: Исследуй DOM

1. Открой любой сайт (например, google.com)
2. Открой DevTools (`F12`)
3. Выбери элемент на странице (`Ctrl+Shift+C` и клик)
4. В панели Elements найди этот элемент
5. Измени его текст (двойной клик)
6. Измени цвет фона в Styles (`background-color: red`)
7. Обнови страницу — изменения пропадут (они были только локальными)

---

## Console — JavaScript консоль

### Основные функции

```javascript
// Вывод информации
console.log('Обычное сообщение');
console.info('Информация');
console.warn('Предупреждение');    // жёлтый
console.error('Ошибка');           // красный

// Группировка
console.group('Группа');
console.log('Внутри группы');
console.groupEnd();

// Таблица (для массивов/объектов)
console.table([{name: 'John', age: 25}, {name: 'Jane', age: 30}]);

// Время выполнения
console.time('timer');
// ... код ...
console.timeEnd('timer');  // timer: 123ms

// Очистка консоли
console.clear();
```

### Выполнение кода

Консоль — это REPL для JavaScript. Можно:
- Выполнять любой JS код
- Обращаться к переменным на странице
- Вызывать функции
- Использовать `$0` — последний выбранный элемент в Elements

### Фильтрация сообщений

Над консолью есть фильтры:
- **All** — все сообщения
- **Errors** — только ошибки
- **Warnings** — предупреждения
- **Info** — информация
- **Verbose** — подробные логи

### 📝 Практика 1.2.5.2: Работа с Console

1. Открой DevTools → Console
2. Введи `2 + 2` и нажми Enter
3. Введи `document.title` — увидишь заголовок страницы
4. Введи `console.table(performance.timing)` — увидишь таблицу метрик
5. Перейди в Elements, выбери элемент
6. В Console введи `$0` — это выбранный элемент
7. Введи `$0.style.border = '2px solid red'` — добавь рамку

---

## Sources — отладка JavaScript

### Навигация

- **Слева** — дерево файлов
- **Центр** — исходный код
- **Справа** — панель отладки

### Breakpoints (точки останова)

**Как поставить:**
1. Открой файл в Sources
2. Клик на номере строки — появится синяя точка
3. Перезагрузи страницу или вызови функцию
4. Выполнение остановится на этой строке

**Типы breakpoints:**
- **Line** — обычная точка на строке
- **Conditional** — остановка по условию (right-click → Add conditional)
- **Logpoint** — вывод в консоль без остановки (right-click → Add logpoint)

### Управление отладкой

| Кнопка | Действие | Клавиша |
|--------|----------|---------|
| ▶️ Resume | Продолжить выполнение | `F8` |
| ⏭️ Step over | Следующая строка | `F10` |
| ⬇️ Step into | Войти в функцию | `F11` |
| ⬆️ Step out | Выйти из функции | `Shift+F11` |

### Панель отладки (справа)

- **Watch** — отслеживание переменных
- **Call Stack** — стек вызовов
- **Scope** — переменные в текущей области
- **Breakpoints** — список всех точек останова

### 📝 Практика 1.2.5.3: Отладка кода

1. Создай HTML файл:

```html
<!DOCTYPE html>
<html>
<head><title>Debug Test</title></head>
<body>
  <button onclick="calculate()">Calculate</button>
  <script>
    function calculate() {
      let a = 5;
      let b = 10;
      let result = multiply(a, b);
      console.log('Result:', result);
    }

    function multiply(x, y) {
      return x * y;
    }
  </script>
</body>
</html>
```

2. Открой файл в браузере
3. Открой DevTools → Sources
4. Найди свой скрипт (или вкладка Page → file)
5. Поставь breakpoint на строке `let result = multiply(a, b);`
6. Нажми кнопку "Calculate"
7. Выполнение остановится — посмотри значения `a` и `b` в Scope
8. Нажми "Step into" (`F11`) — войди в функцию `multiply`
9. Нажми "Resume" (`F8`) — продолжи выполнение

---

## Network — анализ сетевых запросов

### Основные колонки

| Колонка | Описание |
|---------|----------|
| **Name** | URL запроса |
| **Status** | HTTP статус (200, 404, 500...) |
| **Type** | Тип ресурса (document, script, xhr, fetch...) |
| **Size** | Размер ответа |
| **Time** | Время загрузки |
| **Waterfall** | Визуальная временная шкала |

### Фильтры

Над списком запросов:
- **All** — все запросы
- **Fetch/XHR** — AJAX запросы (самое важное для API)
- **JS** — JavaScript файлы
- **CSS** — стили
- **Img** — изображения
- **Doc** — HTML документы

### Детали запроса

Клик на запросе открывает детали:

- **Headers** — заголовки запроса и ответа
- **Preview** — предпросмотр ответа (JSON красиво)
- **Response** — сырой ответ
- **Timing** — детальная разбивка времени
- **Cookies** — куки запроса

### Полезные опции

- **Preserve log** — сохранять логи при переходах
- **Disable cache** — отключить кэш (важно при разработке!)
- **Throttling** — эмуляция медленного интернета (Slow 3G, Offline)

### 📝 Практика 1.2.5.4: Анализ сетевых запросов

1. Открой DevTools → Network
2. Включи "Disable cache"
3. Перезагрузи страницу
4. Посмотри список запросов — найди самый большой файл
5. Отфильтруй по "Fetch/XHR"
6. Открой любой сайт с API (например, github.com)
7. Кликни на XHR запрос — посмотри Headers и Response
8. Попробуй Throttling → "Slow 3G" — почувствуй медленный интернет

---

## Application — хранилища данных

### Local Storage

**Application → Local Storage**

- Хранит данные без срока годности
- До 5-10 MB
- Только строки (объекты нужно JSON.stringify)

```javascript
// В консоли
localStorage.setItem('name', 'John');
localStorage.getItem('name');  // 'John'
localStorage.removeItem('name');
localStorage.clear();  // очистить всё
```

### Session Storage

**Application → Session Storage**

- Хранит данные до закрытия вкладки
- Такой же API как localStorage

### Cookies

**Application → Cookies**

- Отправляются с каждым запросом на сервер
- Имеют срок годности
- Ограничение ~4 KB

### IndexedDB

**Application → IndexedDB**

- Полноценная база данных в браузере
- Для больших объёмов данных

### 📝 Практика 1.2.5.5: Работа с хранилищами

1. Открой любой сайт
2. DevTools → Application → Local Storage
3. Посмотри, что сайт хранит
4. В Console добавь своё значение:
   ```javascript
   localStorage.setItem('test', 'Hello from DevTools!');
   ```
5. Обнови вкладку Local Storage — увидишь новое значение
6. Удали его через интерфейс (right-click → Delete)

---

## Полезные горячие клавиши

| Действие | Windows/Linux | macOS |
|----------|---------------|-------|
| Открыть DevTools | `F12` | `Cmd+Option+I` |
| Открыть Console | `Ctrl+Shift+J` | `Cmd+Option+J` |
| Выбрать элемент | `Ctrl+Shift+C` | `Cmd+Shift+C` |
| Поиск по всем файлам | `Ctrl+Shift+F` | `Cmd+Option+F` |
| Переключение вкладок | `Ctrl+[` / `Ctrl+]` | `Cmd+[` / `Cmd+]` |
| Command Menu | `Ctrl+Shift+P` | `Cmd+Shift+P` |

---

## Мобильная отладка

### Device Mode

`Ctrl+Shift+M` (`Cmd+Shift+M`) — переключение в режим мобильного устройства.

Можно:
- Выбрать устройство (iPhone, Pixel, iPad...)
- Задать произвольное разрешение
- Эмулировать touch-события
- Тестировать адаптивность

### Отладка на реальном устройстве (Android)

1. Включи "Developer options" и "USB debugging" на телефоне
2. Подключи телефон по USB
3. В Chrome открой `chrome://inspect`
4. Увидишь вкладки с телефона — можешь отлаживать

---

## Дополнительные инструменты

### Lighthouse

**DevTools → Lighthouse**

Автоматический аудит страницы:
- **Performance** — скорость загрузки
- **Accessibility** — доступность
- **Best Practices** — лучшие практики
- **SEO** — оптимизация для поисковиков

### Performance

**DevTools → Performance**

Запись и анализ производительности:
1. Нажми Record
2. Выполни действия на странице
3. Останови запись
4. Анализируй timeline

### Memory

**DevTools → Memory**

Анализ потребления памяти, поиск утечек.

---

## Ресурсы

- 📖 [Chrome DevTools Documentation](https://developer.chrome.com/docs/devtools/)
- 📹 [Chrome DevTools Tips](https://www.youtube.com/playlist?list=PLNYkxOF6rcIAcezfL8q0rjt13ufKseL5X)
- 📖 [Firefox DevTools](https://firefox-source-docs.mozilla.org/devtools-user/)

---

## Чеклист

После изучения ты должен уметь:

- [ ] Открывать DevTools и ориентироваться во вкладках
- [ ] Инспектировать и редактировать DOM/CSS
- [ ] Использовать Console для отладки
- [ ] Ставить breakpoints и отлаживать JavaScript
- [ ] Анализировать сетевые запросы (Network)
- [ ] Просматривать и редактировать localStorage/cookies
- [ ] Использовать Device Mode для мобильной отладки

---

⏱ **Время на изучение**: 2-3 часа

📝 **Практик**: 5

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](https://xmentors.ru/dev?utm_source=baza_js)