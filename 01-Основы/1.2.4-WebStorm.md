# 1.2.4 WebStorm

WebStorm — платная IDE от JetBrains, специализированная для JavaScript/TypeScript разработки. Альтернатива VS Code с более мощными встроенными инструментами.

---

## 🎯 Что ты узнаешь

- Отличия между VS Code и WebStorm
- Как установить WebStorm (включая бесплатную лицензию для студентов)
- Интерфейс и основные инструменты IDE
- Мощный рефакторинг кода
- Встроенная отладка приложений
- Git интеграция и работа с ветками
- Горячие клавиши и продуктивные фишки
- Настройка под свои нужды

---

## VS Code vs WebStorm

| Критерий | VS Code | WebStorm |
|----------|---------|----------|
| Цена | Бесплатный | Платный (~$70/год, бесплатно для студентов) |
| Тип | Редактор кода + расширения | Полноценная IDE |
| Настройка | Нужно ставить расширения | Работает "из коробки" |
| Рефакторинг | Базовый | Мощный, надёжный |
| Автодополнение | Хорошее (с расширениями) | Отличное (встроенное) |
| Потребление памяти | ~300-500 MB | ~1-2 GB |
| Скорость запуска | Быстрый | Медленнее |
| Git интеграция | Хорошая | Отличная |
| Отладка | Через расширения | Встроенная, мощная |

**Когда выбрать WebStorm:**
- Работаешь над большими проектами
- Часто делаешь рефакторинг
- Хочешь всё "из коробки" без настройки расширений
- Команда уже использует JetBrains IDE

**Когда выбрать VS Code:**
- Нужен лёгкий и быстрый редактор
- Работаешь с разными языками (не только JS)
- Не хочешь платить
- Устраивает настройка через расширения

---

## Установка

### Скачивание

1. Перейди на [jetbrains.com/webstorm](https://www.jetbrains.com/webstorm/)
2. Скачай установщик для своей ОС
3. Установи, следуя инструкциям

### Лицензия

**Варианты:**
- **Non-commercial use** — бесплатно для личных проектов, обучения, open-source. При первом запуске выбери "For non-commercial use"
- **30-дневный триал** — для ознакомления с полной версией
- **Подписка** — \$69/год (первый год), потом \$55/год
- **Бесплатно для студентов** — через [JetBrains Educational](https://www.jetbrains.com/community/education/)
- **Early Access Program (EAP)** — бесплатные preview-версии

> 💡 Для обучения и личных проектов выбирай **Non-commercial use** — это полностью легально и бесплатно.

### JetBrains Toolbox (рекомендуется)

Удобный менеджер для всех продуктов JetBrains:

1. Скачай [JetBrains Toolbox](https://www.jetbrains.com/toolbox-app/)
2. Установи Toolbox
3. Через него установи WebStorm
4. Toolbox будет автоматически обновлять IDE

---

## Первый запуск

### Настройка при первом запуске

1. **Тема**: выбери Light или Dark (Darcula)
2. **Keymap**: оставь default или выбери "VS Code" если переходишь с него
3. **Plugins**: можно пропустить, добавишь позже

### Открытие проекта

```
File → Open → выбери папку проекта
```

Или из терминала:

```bash
# Если добавил WebStorm в PATH
webstorm /path/to/project

# macOS
open -a "WebStorm" /path/to/project
```

---

## Интерфейс

```
┌─────────────────────────────────────────────────────────────┐
│  Меню               Навигация              Поиск      ─ □ ✕ │
├──────────┬──────────────────────────────────────────────────┤
│          │  Вкладки файлов                                  │
│ Project  ├──────────────────────────────────────────────────┤
│ (дерево  │                                                  │
│ файлов)  │              Редактор кода                       │
│          │                                                  │
│          │                                                  │
│          ├──────────────────────────────────────────────────┤
│          │  Terminal / Run / Debug / Git / Problems         │
└──────────┴──────────────────────────────────────────────────┘
```

**Основные панели:**
- **Project** (слева) — дерево файлов
- **Editor** — редактирование кода
- **Tool Windows** (снизу/справа) — терминал, Git, отладка
- **Navigation Bar** (сверху) — быстрая навигация по файлу

---

## Горячие клавиши

> 💡 Горячие клавиши могут отличаться. Проверь свои: **File → Settings → Keymap** (или `Ctrl+Alt+S` → Keymap).

### Главные (запомни первыми)

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Shift+Shift` | `Shift+Shift` | **Search Everywhere** — поиск всего |
| `Ctrl+Shift+A` | `Cmd+Shift+A` | Find Action — поиск команд |
| `Ctrl+Alt+S` | `Cmd+,` | Настройки |
| `Alt+Enter` | `Option+Enter` | Показать quick-fix (исправления) |

### Навигация

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Ctrl+N` | `Cmd+O` | Перейти к классу/файлу |
| `Ctrl+Shift+N` | `Cmd+Shift+O` | Перейти к файлу |
| `Ctrl+E` | `Cmd+E` | Недавние файлы |
| `Ctrl+B` | `Cmd+B` | Перейти к определению |
| `Ctrl+Alt+B` | `Cmd+Option+B` | Перейти к реализации |
| `Alt+F7` | `Option+F7` | Найти использования |
| `Ctrl+G` | `Cmd+L` | Перейти к строке |

### Редактирование

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Ctrl+D` | `Cmd+D` | Дублировать строку |
| `Ctrl+Y` | `Cmd+Backspace` | Удалить строку |
| `Ctrl+/` | `Cmd+/` | Закомментировать |
| `Ctrl+Shift+/` | `Cmd+Shift+/` | Блочный комментарий |
| `Alt+↑/↓` | `Option+↑/↓` | Переместить строку |
| `Ctrl+Shift+↑/↓` | `Cmd+Shift+↑/↓` | Переместить метод/блок |
| `Ctrl+Alt+L` | `Cmd+Option+L` | Форматировать код |
| `Ctrl+Space` | `Ctrl+Space` | Автодополнение |

### Рефакторинг

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Shift+F6` | `Shift+F6` | Переименовать |
| `Ctrl+Alt+M` | `Cmd+Option+M` | Извлечь метод |
| `Ctrl+Alt+V` | `Cmd+Option+V` | Извлечь переменную |
| `Ctrl+Alt+Shift+T` | `Ctrl+T` | Меню рефакторинга |

### Запуск и отладка

| Windows/Linux | macOS | Действие |
|---------------|-------|----------|
| `Shift+F10` | `Ctrl+R` | Запустить |
| `Shift+F9` | `Ctrl+D` | Запустить в режиме отладки |
| `F8` | `F8` | Step Over (следующая строка) |
| `F7` | `F7` | Step Into (войти в функцию) |
| `F9` | `Cmd+Option+R` | Resume (продолжить) |

### 📝 Практика 1.2.4.1: Освой навигацию

1. Открой любой проект в WebStorm
2. Нажми `Shift+Shift` — попробуй найти файл, класс, настройку
3. Используй `Ctrl+E` (`Cmd+E`) чтобы переключаться между файлами
4. В любом файле нажми `Alt+F7` (`Option+F7`) на функции — посмотри где она используется

---

## Настройка для MEVN

### Включение поддержки Vue

WebStorm поддерживает Vue из коробки, но проверь:

1. **File → Settings → Languages & Frameworks → JavaScript**
   - JavaScript language version: `ECMAScript 6+`

2. **File → Settings → Languages & Frameworks → TypeScript**
   - Включи TypeScript service

3. **File → Settings → Plugins**
   - Убедись что Vue.js плагин включён

### ESLint

1. **File → Settings → Languages & Frameworks → JavaScript → Code Quality Tools → ESLint**
2. Выбери "Automatic ESLint configuration"
3. Включи "Run eslint --fix on save"

### Prettier

1. **File → Settings → Languages & Frameworks → JavaScript → Prettier**
2. Укажи путь к Prettier (обычно `node_modules/prettier`)
3. Включи "On save" для автоформатирования

### Node.js

1. **File → Settings → Languages & Frameworks → Node.js**
2. Укажи путь к Node.js интерпретатору
3. WebStorm автоматически подхватит `package.json`

### 📝 Практика 1.2.4.2: Настрой проект

1. Создай папку проекта и открой её в WebStorm
2. Инициализируй проект и установи ESLint:

```bash
npm init -y
npm install eslint --save-dev
npx eslint --init
# Выбери: "To check syntax and find problems" → "JavaScript modules" → "None" → No TypeScript → "Node" → "JavaScript"
```

3. Настрой ESLint в WebStorm: **Settings → Languages & Frameworks → JavaScript → Code Quality Tools → ESLint** → выбери "Automatic ESLint configuration"
4. Создай файл `test.js` с ошибками:

```javascript
var x = 1
var y = 2
console.log(x)
// y не используется — ESLint подчеркнёт

function test(){
console.log("no indent")    // неправильный отступ
}

const unused = "hello"      // неиспользуемая переменная
```

5. Должны появиться жёлтые/красные подчёркивания
6. Наведи на ошибку или нажми `Alt+Enter` — выбери "Fix" или посмотри описание

---

## Мощные фичи WebStorm

### Рефакторинг

WebStorm умеет безопасно:
- **Rename** (`Shift+F6`) — переименует везде, включая импорты
- **Extract Method** — выделить код → `Ctrl+Alt+M`
- **Extract Variable** — выделить выражение → `Ctrl+Alt+V`
- **Move** — перенести файл/функцию с обновлением импортов

### Live Templates (сниппеты)

Встроенные шаблоны кода. Напиши сокращение и нажми `Tab`:

| Сокращение | Результат |
|------------|-----------|
| `log` | `console.log()` |
| `if` | `if` блок |
| `for` | `for` цикл |
| `fun` | `function` |

Свои шаблоны: **Settings → Editor → Live Templates**

### Local History

WebStorm сохраняет локальную историю изменений:

**Right-click на файле → Local History → Show History**

Полезно если забыл закоммитить и потерял изменения.

### Scratches

Временные файлы для экспериментов:

`Ctrl+Alt+Shift+Insert` (`Cmd+Shift+N` на macOS) → New Scratch File

---

## Встроенный терминал

Открыть: `Alt+F12` (или View → Tool Windows → Terminal)

Работает так же как системный терминал, но внутри IDE.

---

## Git интеграция

WebStorm имеет мощную Git интеграцию:

- **Commit**: `Ctrl+K` (`Cmd+K`)
- **Push**: `Ctrl+Shift+K` (`Cmd+Shift+K`)
- **Pull**: `Ctrl+T` (`Cmd+T`)
- **Branches**: в правом нижнем углу или `` Ctrl+Shift+` ``
- **Git Log**: `Alt+9` — визуальная история

### Разрешение конфликтов

Одна из лучших фич WebStorm — визуальный merge-инструмент. При конфликте:

1. WebStorm покажет список конфликтующих файлов
2. Двойной клик на файле откроет **3-панельный редактор**:
   - Слева — твои изменения
   - Справа — входящие изменения
   - По центру — результат
3. Кликай на стрелки `>>` или `<<` чтобы принять изменения с нужной стороны
4. Можно редактировать результат вручную
5. Нажми "Apply" когда готово

> 💡 Это намного удобнее, чем разбираться с маркерами `<<<<<<<` в текстовом файле.

### 📝 Практика 1.2.4.3: Git в WebStorm

1. Открой репозиторий в WebStorm
2. Измени файл
3. Открой Commit (`Ctrl+K`) — посмотри diff
4. Сделай коммит через интерфейс
5. Открой Git Log (`Alt+9`) — посмотри историю
6. Попробуй создать конфликт (измени одну строку в двух ветках) и разреши его через интерфейс

---

## Отладка

### Базовая отладка

1. Поставь breakpoint — клик слева от номера строки
2. Right-click на файле → Debug
3. Программа остановится на breakpoint
4. Используй панель Debug для просмотра переменных

### Отладка Node.js сервера

1. Открой `package.json`
2. Найди скрипт (например, `"start": "node server.js"`)
3. Нажми зелёную стрелку рядом → Debug

### Отладка в браузере

1. **Run → Edit Configurations → + → JavaScript Debug**
2. Укажи URL (например, `http://localhost:3000`)
3. Запусти в режиме Debug
4. WebStorm откроет браузер и подключится к нему

---

## Полезные плагины

**Settings → Plugins → Marketplace**

| Плагин | Описание |
|--------|----------|
| **MongoDB** | Работа с MongoDB |
| **.env files support** | Подсветка .env файлов |
| **Rainbow Brackets** | Цветные скобки |
| **Key Promoter X** | Подсказывает горячие клавиши |

---

## Ресурсы

- 📖 [WebStorm Documentation](https://www.jetbrains.com/help/webstorm/)
- 📹 [WebStorm Guide](https://www.jetbrains.com/webstorm/guide/)
- 🎮 [Keymap Reference](https://resources.jetbrains.com/storage/products/webstorm/docs/WebStorm_ReferenceCard.pdf)

---

## Чеклист

После изучения ты должен уметь:

- [ ] Установить WebStorm и активировать лицензию
- [ ] Использовать `Shift+Shift` для поиска всего
- [ ] Настроить ESLint и Prettier
- [ ] Делать рефакторинг (`Shift+F6` для переименования)
- [ ] Работать с Git через интерфейс WebStorm
- [ ] Запускать отладку

---

⏱ **Время на изучение**: 1-2 часа

📝 **Практик**: 3

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](https://xmentors.ru/dev?utm_source=baza_js)