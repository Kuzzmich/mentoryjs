# 2.1.4 Массивы и объекты

> **Цель:** Научиться работать со структурами данных — массивами и объектами. Освоить методы массивов для обработки коллекций данных.

---

## Массивы

**Массив** — упорядоченная коллекция элементов.

### Создание массивов

```javascript
// Литерал массива (рекомендуется)
const temperatures = [20, 22, 25, 23, 21];

// Конструктор (не используй)
const arr = new Array(5);  // массив из 5 пустых элементов

// Пустой массив
const empty = [];

// Массив с разными типами (можно, но не рекомендуется)
const mixed = [1, "текст", true, null];
```

### Доступ к элементам

```javascript
const cities = ["Москва", "Петербург", "Казань"];

console.log(cities[0]);     // "Москва" (индексы с 0)
console.log(cities[2]);     // "Казань"
console.log(cities[10]);    // undefined (элемента нет)

// Длина массива
console.log(cities.length); // 3

// Последний элемент
console.log(cities[cities.length - 1]); // "Казань"
```

### Изменение массива

```javascript
const temps = [20, 22, 25];

// Изменить элемент
temps[0] = 18;
console.log(temps);  // [18, 22, 25]

// Добавить в конец
temps.push(27);
console.log(temps);  // [18, 22, 25, 27]

// Добавить в начало
temps.unshift(15);
console.log(temps);  // [15, 18, 22, 25, 27]

// Удалить последний
const last = temps.pop();
console.log(last);   // 27
console.log(temps);  // [15, 18, 22, 25]

// Удалить первый
const first = temps.shift();
console.log(first);  // 15
console.log(temps);  // [18, 22, 25]
```

---

## Методы массивов

### forEach — итерация

```javascript
const temperatures = [20, 22, 25, 23];

temperatures.forEach((temp, index) => {
  console.log(`День ${index + 1}: ${temp}°C`);
});
// День 1: 20°C
// День 2: 22°C
// День 3: 25°C
// День 4: 23°C
```

> ⚠️ `forEach` ничего не возвращает. Используй для побочных эффектов (вывод, изменение внешних данных).

### map — преобразование

Создаёт **новый массив** с преобразованными элементами:

```javascript
const celsius = [0, 10, 20, 30];

// Преобразовать в Фаренгейты
const fahrenheit = celsius.map(c => c * 9/5 + 32);
console.log(fahrenheit);  // [32, 50, 68, 86]

// Исходный массив не изменился
console.log(celsius);     // [0, 10, 20, 30]
```

### filter — фильтрация

Создаёт **новый массив** с элементами, прошедшими проверку:

```javascript
const temperatures = [18, 25, 15, 30, 12, 28];

// Только тёплые дни (>= 20)
const warmDays = temperatures.filter(temp => temp >= 20);
console.log(warmDays);  // [25, 30, 28]
```

### find — поиск элемента

Возвращает **первый найденный элемент** или `undefined`:

```javascript
const cities = [
  { name: "Москва", temp: 15 },
  { name: "Сочи", temp: 25 },
  { name: "Казань", temp: 18 }
];

const warm = cities.find(city => city.temp > 20);
console.log(warm);  // { name: "Сочи", temp: 25 }

const cold = cities.find(city => city.temp < 10);
console.log(cold);  // undefined
```

### reduce — агрегация

Сворачивает массив в одно значение:

```javascript
const temperatures = [20, 22, 25, 23, 21];

// Сумма
const sum = temperatures.reduce((acc, temp) => acc + temp, 0);
console.log(sum);  // 111

// Среднее
const average = sum / temperatures.length;
console.log(average);  // 22.2

// Максимум
const max = temperatures.reduce((acc, temp) => {
  return temp > acc ? temp : acc;
}, temperatures[0]);
console.log(max);  // 25
```

**Параметры reduce:**
- `acc` — аккумулятор (результат предыдущей итерации)
- `temp` — текущий элемент
- `0` — начальное значение аккумулятора

### some / every — проверки

```javascript
const temperatures = [20, 22, 25, 23, 21];

// Хотя бы один > 24?
const hasWarm = temperatures.some(temp => temp > 24);
console.log(hasWarm);  // true

// Все > 15?
const allWarm = temperatures.every(temp => temp > 15);
console.log(allWarm);  // true
```

### includes — наличие элемента

```javascript
const cities = ["Москва", "Сочи", "Казань"];

console.log(cities.includes("Сочи"));     // true
console.log(cities.includes("Омск"));     // false
```

### sort — сортировка

> ⚠️ **Изменяет исходный массив!**

```javascript
const nums = [3, 1, 4, 1, 5];

// Для чисел нужна функция сравнения
nums.sort((a, b) => a - b);  // по возрастанию
console.log(nums);  // [1, 1, 3, 4, 5]

nums.sort((a, b) => b - a);  // по убыванию
console.log(nums);  // [5, 4, 3, 1, 1]

// Строки сортируются лексикографически
const cities = ["Москва", "Казань", "Сочи"];
cities.sort();
console.log(cities);  // ["Казань", "Москва", "Сочи"]
```

### slice — копирование части

```javascript
const temps = [20, 22, 25, 23, 21];

// Со 2-го по 4-й элемент (не включая 4-й индекс)
const part = temps.slice(1, 4);
console.log(part);   // [22, 25, 23]

// Последние 2 элемента
const last = temps.slice(-2);
console.log(last);   // [23, 21]

// Копия массива
const copy = temps.slice();
```

### join — в строку

```javascript
const cities = ["Москва", "Сочи", "Казань"];

console.log(cities.join(", "));  // "Москва, Сочи, Казань"
console.log(cities.join(" - ")); // "Москва - Сочи - Казань"
```

---

## Объекты

**Объект** — неупорядоченная коллекция пар ключ-значение.

### Создание объектов

```javascript
// Литерал объекта (рекомендуется)
const weather = {
  city: "Москва",
  temperature: 15,
  humidity: 65,
  description: "Облачно"
};

// Пустой объект
const empty = {};

// Вложенные объекты
const forecast = {
  city: "Москва",
  current: {
    temp: 15,
    humidity: 65
  },
  tomorrow: {
    temp: 18,
    humidity: 70
  }
};
```

### Доступ к свойствам

```javascript
const weather = {
  city: "Москва",
  temperature: 15,
  "has-rain": false  // ключ с дефисом
};

// Точечная нотация (предпочтительнее)
console.log(weather.city);         // "Москва"
console.log(weather.temperature);  // 15

// Скобочная нотация
console.log(weather["city"]);      // "Москва"
console.log(weather["has-rain"]);  // false (для ключей с дефисом)

// Динамический доступ
const key = "temperature";
console.log(weather[key]);         // 15
```

### Изменение объекта

```javascript
const weather = {
  city: "Москва",
  temp: 15
};

// Изменить свойство
weather.temp = 18;

// Добавить свойство
weather.humidity = 65;

// Удалить свойство
delete weather.humidity;

console.log(weather);  // { city: "Москва", temp: 18 }
```

### Методы объектов

```javascript
const weather = {
  city: "Москва",
  temp: 15,
  // Метод (функция внутри объекта)
  getStatus() {
    if (this.temp > 20) return "Тепло";
    return "Прохладно";
  }
};

console.log(weather.getStatus());  // "Прохладно"
```

### Перебор свойств

```javascript
const weather = {
  city: "Москва",
  temp: 15,
  humidity: 65
};

// Ключи
const keys = Object.keys(weather);
console.log(keys);  // ["city", "temp", "humidity"]

// Значения
const values = Object.values(weather);
console.log(values);  // ["Москва", 15, 65]

// Пары [ключ, значение]
const entries = Object.entries(weather);
console.log(entries);
// [["city", "Москва"], ["temp", 15], ["humidity", 65]]

// Перебор
for (let key in weather) {
  console.log(`${key}: ${weather[key]}`);
}
```

---

## Деструктуризация

### Деструктуризация массивов

```javascript
const temperatures = [20, 22, 25];

// Обычный способ
const first = temperatures[0];
const second = temperatures[1];

// Деструктуризация
const [first, second, third] = temperatures;
console.log(first);   // 20
console.log(second);  // 22
console.log(third);   // 25

// Пропуск элементов
const [, , third] = temperatures;
console.log(third);   // 25

// Остаток массива
const [first, ...rest] = temperatures;
console.log(first);   // 20
console.log(rest);    // [22, 25]
```

### Деструктуризация объектов

```javascript
const weather = {
  city: "Москва",
  temp: 15,
  humidity: 65
};

// Деструктуризация
const { city, temp, humidity } = weather;
console.log(city);      // "Москва"
console.log(temp);      // 15

// Переименование
const { city: cityName, temp: temperature } = weather;
console.log(cityName);      // "Москва"
console.log(temperature);   // 15

// Значения по умолчанию
const { pressure = 750 } = weather;
console.log(pressure);  // 750 (свойства нет, взято значение по умолчанию)

// Вложенные объекты
const forecast = {
  city: "Москва",
  current: { temp: 15, humidity: 65 }
};

const { current: { temp, humidity } } = forecast;
console.log(temp);  // 15
```

### В параметрах функции

```javascript
// Деструктуризация в параметрах
function printWeather({ city, temp, humidity }) {
  console.log(`${city}: ${temp}°C, влажность ${humidity}%`);
}

const weather = { city: "Москва", temp: 15, humidity: 65 };
printWeather(weather);  // "Москва: 15°C, влажность 65%"
```

---

## Spread и Rest операторы

### Spread (...) — разворачивание

```javascript
// Копирование массива
const temps = [20, 22, 25];
const copy = [...temps];
console.log(copy);  // [20, 22, 25]

// Объединение массивов
const week1 = [20, 22, 25];
const week2 = [23, 21, 24];
const twoWeeks = [...week1, ...week2];
console.log(twoWeeks);  // [20, 22, 25, 23, 21, 24]

// Копирование объекта
const weather = { city: "Москва", temp: 15 };
const updated = { ...weather, temp: 18 };
console.log(updated);  // { city: "Москва", temp: 18 }

// Объединение объектов
const current = { temp: 15, humidity: 65 };
const location = { city: "Москва", country: "Россия" };
const full = { ...location, ...current };
console.log(full);
// { city: "Москва", country: "Россия", temp: 15, humidity: 65 }
```

### Rest (...) — сбор остатка

```javascript
// В параметрах функции
function sum(...numbers) {
  return numbers.reduce((acc, n) => acc + n, 0);
}

console.log(sum(1, 2, 3));        // 6
console.log(sum(1, 2, 3, 4, 5));  // 15

// В деструктуризации
const [first, ...rest] = [20, 22, 25, 23];
console.log(first);  // 20
console.log(rest);   // [22, 25, 23]
```

---

## 🔧 Практика 2.1.4.1: Работа с массивом температур

```javascript
const weekTemperatures = [18, 20, 22, 19, 17, 21, 23];
const daysOfWeek = ["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"];

// TODO 1: Используй map для создания массива объектов
// Формат: [{ day: "Пн", temp: 18 }, { day: "Вт", temp: 20 }, ...]
// Подсказка: map принимает второй параметр — индекс

// TODO 2: Используй filter для получения только тёплых дней (>= 20)
// Результат: массив объектов

// TODO 3: Используй find для поиска дня с температурой 22°C
// Результат: объект дня или undefined

// TODO 4: Используй reduce для подсчёта средней температуры
// Формула: сумма / количество
// Округли до 1 знака (.toFixed(1))

// TODO 5: Используй some для проверки: есть ли день с температурой > 22?

// TODO 6: Используй every для проверки: все ли дни теплее 15°C?

// TODO 7: Отсортируй массив температур по возрастанию (создай копию!)
// Подсказка: используй slice() или spread для копирования

// TODO 8: Выведи прогноз одной строкой через join
// Формат: "Пн: 18°C, Вт: 20°C, Ср: 22°C, ..."
```

---

## 🔧 Практика 2.1.4.2: Массив объектов городов

```javascript
const cities = [
  { name: "Москва", temp: 15, humidity: 65, population: 12500000 },
  { name: "Санкт-Петербург", temp: 12, humidity: 75, population: 5400000 },
  { name: "Казань", temp: 18, humidity: 60, population: 1250000 },
  { name: "Сочи", temp: 25, humidity: 70, population: 400000 },
  { name: "Екатеринбург", temp: 10, humidity: 55, population: 1500000 }
];

// TODO 1: Создай массив только названий городов
// Используй map
// Результат: ["Москва", "Санкт-Петербург", ...]

// TODO 2: Найди все города с комфортной температурой (15-22°C)
// Используй filter

// TODO 3: Найди самый тёплый город
// Используй reduce
// Результат: объект города

// TODO 4: Посчитай суммарное население всех городов
// Используй reduce

// TODO 5: Проверь, есть ли город с влажностью > 80%
// Используй some

// TODO 6: Создай новый массив с добавлением поля "comfortable"
// true если temp 15-22 И humidity 50-70
// Используй map

// TODO 7: Отсортируй города по температуре (от холодного к тёплому)
// Создай копию массива с помощью spread, затем sort
```

---

## 🔧 Практика 2.1.4.3: Деструктуризация и spread

```javascript
const weather = {
  city: "Москва",
  temp: 15,
  humidity: 65,
  wind: { speed: 5, direction: "СВ" },
  forecast: [18, 20, 17]
};

// TODO 1: Извлеки city, temp, humidity с помощью деструктуризации

// TODO 2: Извлеки скорость ветра из вложенного объекта
// const { wind: { speed } } = weather;

// TODO 3: Извлеки первые два дня прогноза
// const { forecast: [day1, day2] } = weather;

// TODO 4: Создай новый объект updatedWeather с изменённой температурой (18)
// Используй spread оператор

// TODO 5: Добавь в updatedWeather новое поле pressure: 755
// Используй spread

// TODO 6: Создай функцию printWeatherShort
// Принимает объект погоды, деструктурирует в параметрах
// Выводит: "Москва: 15°C"

// TODO 7: Создай функцию mergeWeatherData
// Принимает два объекта погоды
// Возвращает объединённый объект (используй spread)
// Второй объект должен перезаписать свойства первого

const weather1 = { city: "Москва", temp: 15 };
const weather2 = { temp: 18, humidity: 70 };
// mergeWeatherData(weather1, weather2)
// Результат: { city: "Москва", temp: 18, humidity: 70 }
```

---

## 📝 Итоговое задание

Создай систему управления данными о погоде для нескольких городов.

```javascript
const weatherData = [
  {
    city: "Москва",
    country: "Россия",
    measurements: [
      { time: "00:00", temp: 12, humidity: 70 },
      { time: "06:00", temp: 10, humidity: 75 },
      { time: "12:00", temp: 15, humidity: 65 },
      { time: "18:00", temp: 13, humidity: 68 }
    ]
  },
  {
    city: "Сочи",
    country: "Россия",
    measurements: [
      { time: "00:00", temp: 20, humidity: 80 },
      { time: "06:00", temp: 22, humidity: 78 },
      { time: "12:00", temp: 28, humidity: 75 },
      { time: "18:00", temp: 25, humidity: 77 }
    ]
  }
];

// TODO 1: Создай функцию getCityAverage
// Параметр: объект города
// Возвращает: среднюю температуру по всем измерениям
// Используй reduce на measurements

// TODO 2: Создай функцию getCityMaxTemp
// Параметр: объект города
// Возвращает: объект измерения с максимальной температурой
// Используй reduce на measurements

// TODO 3: Создай функцию getAllCitiesAverages
// Параметр: массив weatherData
// Возвращает: массив объектов { city: "...", avgTemp: ... }
// Используй map + getCityAverage

// TODO 4: Создай функцию findHottestCity
// Параметр: массив weatherData
// Возвращает: объект самого тёплого города (по средней)
// Используй reduce + getCityAverage

// TODO 5: Создай функцию getComfortableHours
// Параметр: объект города
// Возвращает: массив времён, когда temp 18-25 И humidity < 80
// Используй filter на measurements

// TODO 6: Создай функцию createWeatherReport
// Параметр: массив weatherData
// Возвращает: строку с отчётом
// Формат:
// === СВОДКА ПОГОДЫ ===
//
// Москва (Россия):
// - Средняя температура: 12.5°C
// - Пик тепла: 12:00 (15°C)
// - Комфортные часы: нет
//
// Сочи (Россия):
// - Средняя температура: 23.8°C
// - Пик тепла: 12:00 (28°C)
// - Комфортные часы: 00:00, 06:00, 18:00
//
// Самый тёплый город: Сочи (23.8°C)

// TODO 7: Вызови createWeatherReport и выведи результат
```

**Требования:**
- Используй методы массивов (map, filter, reduce)
- Применяй деструктуризацию где уместно
- Переиспользуй функции
- Не изменяй исходные данные

---

## ✅ Чеклист раздела

После изучения темы ты должен:

- [ ] Создавать и изменять массивы
- [ ] Использовать `map` для преобразования массивов
- [ ] Использовать `filter` для фильтрации
- [ ] Использовать `reduce` для агрегации
- [ ] Применять `find`, `some`, `every` для поиска и проверок
- [ ] Создавать и изменять объекты
- [ ] Перебирать свойства объектов
- [ ] Использовать деструктуризацию массивов и объектов
- [ ] Применять spread оператор для копирования и объединения
- [ ] Комбинировать методы массивов для решения задач

---

## 🔗 Ресурсы

- 📖 [Learn JavaScript: Массивы](https://learn.javascript.ru/array)
- 📖 [Learn JavaScript: Методы массивов](https://learn.javascript.ru/array-methods)
- 📖 [Learn JavaScript: Объекты](https://learn.javascript.ru/object)
- 📖 [Learn JavaScript: Деструктуризация](https://learn.javascript.ru/destructuring-assignment)
- 📖 [MDN: Array](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Array)
- 📖 [MDN: Object](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Object)

---

⏱ **Время на изучение**: 5-6 часов

---

**Предыдущая тема:** [2.1.3 Функции и области видимости](./2.1.3-Функции-и-области-видимости.md)

**Следующая тема:** [2.1.5 DOM и события](./2.1.5-DOM-и-события.md)
