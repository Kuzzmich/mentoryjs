# 3.2.10 Продвинутая настройка TypeScript и миграция с JS

`tsconfig.json` — это не просто «включить TypeScript». Правильная конфигурация влияет на строгость проверок, скорость компиляции и удобство импортов. В этом разделе разберём ключевые флаги и настроим проект погодного приложения до финального состояния.

---

## 🎯 Что ты узнаешь

- Что даёт `strict` и какие флаги он включает
- Как настроить алиасы путей (`paths`) вместо `../../`
- Как постепенно мигрировать JS-файлы в TypeScript (`allowJs`)
- Как ускорить компиляцию (`skipLibCheck`, `incremental`)
- Как читать ошибки TypeScript и находить их причину

---

## 📖 Теория

### 1. Флаг `strict` и что внутри него

`"strict": true` — это сокращение, которое включает сразу несколько флагов:

```json
{
  "compilerOptions": {
    "strict": true
    // Эквивалентно включению всех этих флагов:
    // "strictNullChecks": true       — нельзя присвоить null/undefined без явного указания
    // "strictFunctionTypes": true    — строгая проверка типов функций-аргументов
    // "strictBindCallApply": true    — строгие типы для bind/call/apply
    // "strictPropertyInitialization": true — свойства класса должны быть инициализированы
    // "noImplicitAny": true          — нельзя использовать неявный any
    // "noImplicitThis": true         — нельзя использовать this без явного типа
    // "useUnknownInCatchVariables": true — catch-переменная типа unknown, не any
    // "alwaysStrict": true           — добавляет "use strict" во все файлы
  }
}
```

Самые важные из них на практике — `strictNullChecks` и `noImplicitAny`. Именно они находят большинство реальных багов.

```typescript
// БЕЗ strictNullChecks — ошибка только в рантайме
function getCity(data: WeatherData | null) {
  return data.city; // ← TypeError в рантайме, TypeScript молчит
}

// С strictNullChecks — ошибка сразу при компиляции
function getCity(data: WeatherData | null) {
  return data.city; // ← TS2531: Object is possibly 'null'
}
```

---

### 2. Дополнительные флаги строгости

Есть полезные флаги, которые `strict` не включает — их стоит добавить вручную:

```json
{
  "compilerOptions": {
    "strict": true,

    // Ошибка при неиспользуемых локальных переменных
    "noUnusedLocals": true,

    // Ошибка при неиспользуемых параметрах функций
    "noUnusedParameters": true,

    // Ошибка, если не все ветки функции возвращают значение
    "noImplicitReturns": true,

    // Ошибка при провале сквозь case в switch без break
    "noFallthroughCasesInSwitch": true,

    // Запрещает обращаться к индексу объекта без проверки на undefined
    "noUncheckedIndexedAccess": true
  }
}
```

`noUncheckedIndexedAccess` стоит упомянуть отдельно — он самый неожиданный:

```typescript
const cities = ["Москва", "Лондон"];

// БЕЗ noUncheckedIndexedAccess:
const first: string = cities[0]; // ок — но cities[99] тоже string по типу

// С noUncheckedIndexedAccess:
const first: string | undefined = cities[0]; // честно — может быть undefined
```

---

### 3. Алиасы путей (`paths`)

Длинные относительные пути — частая проблема в растущем проекте:

```typescript
// Без алиасов — неудобно
import { WeatherData } from "../../../../types/weather-base";
import { validate } from "../../modules/decorators/propertyDecorators";
```

`paths` в `tsconfig.json` позволяет задать короткие псевдонимы:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "@types/*": ["src/types/*"],
      "@modules/*": ["src/modules/*"]
    }
  }
}
```

```typescript
// С алиасами — чисто
import { WeatherData } from "@types/weather-base";
import { validate } from "@modules/decorators/propertyDecorators";
```

> **Важно для Vite:** `paths` в tsconfig работает только для TypeScript. Чтобы Vite тоже понимал эти алиасы при бандлинге, нужно дублировать их в `vite.config.ts`:
>
> ```typescript
> import { defineConfig } from "vite";
> import { resolve } from "path";
>
> export default defineConfig({
>   resolve: {
>     alias: {
>       "@": resolve(__dirname, "src"),
>     },
>   },
> });
> ```

---

### 4. Постепенная миграция с JS (`allowJs`)

Когда переводишь существующий JS-проект на TypeScript, не нужно переписывать всё сразу. `allowJs` позволяет JS и TS файлам сосуществовать:

```json
{
  "compilerOptions": {
    "allowJs": true,         // TypeScript не ругается на .js файлы
    "checkJs": true,         // TypeScript проверяет .js файлы по мере возможности
    "maxNodeModuleJsDepth": 1
  }
}
```

Стратегия миграции: начни с файлов, у которых меньше всего зависимостей, переименуй `.js` → `.ts`, добавь типы, исправь ошибки — и двигайся вверх по дереву зависимостей.

---

### 5. Производительность компиляции

В больших проектах компиляция может занимать секунды. Два флага ускоряют её:

```json
{
  "compilerOptions": {
    // Пропускает проверку типов в .d.ts файлах из node_modules
    // Безопасно для большинства проектов — типы библиотек обычно корректны
    "skipLibCheck": true,

    // Кэширует результаты компиляции в tsbuildinfo файл
    // Следующий запуск tsc проверяет только изменившиеся файлы
    "incremental": true,
    "tsBuildInfoFile": ".tsbuildinfo"
  }
}
```

`skipLibCheck` — де-факто стандарт: он уже включён в шаблонах Vite, CRA и большинстве стартеров.

---

### 6. Итоговый `tsconfig.json` для Vite-проекта

Разумная конфигурация для production-проекта:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,

    "skipLibCheck": true,
    "incremental": true,

    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },

    "experimentalDecorators": true,

    "outDir": "dist",
    "rootDir": "src",
    "declaration": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

---

## 📊 Шпаргалка

| Флаг | Что делает | Когда включать |
|---|---|---|
| `strict` | Включает все строгие проверки | Всегда |
| `noUnusedLocals` | Ошибка на неиспользуемые переменные | В новых проектах |
| `noImplicitReturns` | Ошибка, если функция иногда не возвращает | Всегда |
| `skipLibCheck` | Игнорирует ошибки в node_modules | Всегда |
| `incremental` | Кэш компиляции | В крупных проектах |
| `paths` | Алиасы путей | Когда `../../` мешает |
| `allowJs` | JS и TS в одном проекте | При миграции |

---

## 🎯 Практика 3.2.10.1: Аудит `tsconfig.json`

**Задача:** Включить строгие флаги и исправить ошибки, которые они обнаружат в проекте.

### Шаги:

1. Открой `tsconfig.json` проекта и добавь флаги строгости:

   ```json
   {
     "compilerOptions": {
       "strict": true,
       "noUnusedLocals": true,
       "noUnusedParameters": true,
       "noImplicitReturns": true,
       "noFallthroughCasesInSwitch": true
     }
   }
   ```

2. Запусти проверку и собери список ошибок:

   ```bash
   npx tsc --noEmit 2>&1 | head -50
   ```

3. Разбери типичные ошибки, которые возникают:

   ```typescript
   // TS2345 — несовместимые типы (например, string | null вместо string)
   // TS7006 — параметр имеет неявный тип any
   // TS2366 — не все пути кода возвращают значение
   // TS6133 — переменная объявлена, но не используется
   ```

4. Исправь найденные ошибки. Частые решения:

   ```typescript
   // Было: параметр без типа
   function format(value) { ... }

   // Стало:
   function format(value: number): string { ... }

   // Было: возможный null не обработан
   const city = data.city;

   // Стало:
   const city = data?.city ?? "Неизвестный город";

   // Было: неиспользуемый параметр
   function handler(event, index) { return event.value; }

   // Стало: префикс _ говорит TypeScript «я знаю, что не использую»
   function handler(event, _index) { return event.value; }
   ```

5. Добейся чистой компиляции:

   ```bash
   npx tsc --noEmit
   # Вывод должен быть пустым — никаких ошибок
   ```

### Требования:
- `npx tsc --noEmit` завершается без ошибок
- `noUnusedLocals` и `noUnusedParameters` включены
- Никакого `// @ts-ignore` для обхода ошибок — только честные исправления

<details>
<summary>💡 Подсказка: как найти все ошибки сразу с нужной детализацией</summary>

```bash
# Показать все ошибки с именами файлов и номерами строк:
npx tsc --noEmit 2>&1

# Посчитать количество ошибок:
npx tsc --noEmit 2>&1 | grep "error TS" | wc -l

# Найти только ошибки типа "implicit any":
npx tsc --noEmit 2>&1 | grep "TS7006"
```
</details>

### Проверка:
```bash
npx tsc --noEmit
```

---

## 🎯 Практика 3.2.10.2: Алиасы путей

**Задача:** Настроить алиас `@/` для `src/` и обновить импорты в нескольких ключевых файлах проекта.

### Шаги:

1. Добавь `baseUrl` и `paths` в `tsconfig.json`:

   ```json
   {
     "compilerOptions": {
       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"]
       }
     }
   }
   ```

2. Настрой алиас в `vite.config.ts`:

   ```typescript
   import { defineConfig } from "vite";
   import { resolve } from "path";

   export default defineConfig({
     resolve: {
       alias: {
         // TODO: добавь алиас "@" → resolve(__dirname, "src")
       },
     },
   });
   ```

3. Найди файлы с длинными относительными путями:

   ```bash
   grep -rn "from \"\.\.\/" src/ --include="*.ts" | grep -v "node_modules"
   ```

4. Обнови импорты в нескольких файлах — замени относительные пути на `@/`:

   ```typescript
   // TODO: было (пример)
   import type { WeatherData } from "../../types/weather-base";
   import { validate } from "../../modules/decorators/propertyDecorators";

   // TODO: стало
   import type { WeatherData } from "@/types/weather-base";
   import { validate } from "@/modules/decorators/propertyDecorators";
   ```

   > Менять нужно не все файлы — достаточно тех, где путь длиннее двух уровней (`../../`).
   > Соседние файлы в одной директории (`./utils`) проще оставить как есть.

5. Проверь, что Vite и TypeScript оба понимают алиас:

   ```bash
   npx tsc --noEmit       # TypeScript доволен
   npm run build          # Vite собирает без ошибок
   ```

### Требования:
- `import ... from "@/types/..."` работает и в TypeScript, и при сборке Vite
- Нет `Cannot find module '@/...'` ни в редакторе, ни при компиляции

<details>
<summary>💡 Подсказка: если редактор не видит алиас</summary>

Убедись, что `tsconfig.json` лежит в корне проекта (там же, где `package.json`).
VS Code и WebStorm подхватывают `paths` автоматически из `tsconfig.json` в корне.

Если используешь монорепозиторий — проверь, что IDE открыта из правильной директории.
</details>

### Проверка:
```bash
npx tsc --noEmit && npm run build
```

---

## 🎯 Практика 3.2.10.3: Миграция JS-утилиты в TypeScript

**Задача:** Взять простой JS-файл с утилитами и перевести его в TypeScript — добавить типы, исправить ошибки.

### Шаги:

1. Создай файл `src/utils/format.js` — это наш «легаси» JS-файл:

   ```javascript
   // Форматирует температуру для отображения
   export function formatTemperature(value, unit) {
     const symbol = unit === "fahrenheit" ? "°F" : "°C";
     return `${Math.round(value)}${symbol}`;
   }

   // Форматирует время последнего обновления
   export function formatLastUpdated(timestamp) {
     const date = new Date(timestamp * 1000);
     return date.toLocaleString("ru-RU", {
       hour: "2-digit",
       minute: "2-digit",
     });
   }

   // Возвращает текстовый класс для температуры (для CSS)
   export function temperatureClass(value) {
     if (value < 0) return "cold";
     if (value < 15) return "cool";
     if (value < 25) return "warm";
     return "hot";
   }
   ```

2. Включи `allowJs` временно, чтобы проект не сломался во время миграции:

   ```json
   {
     "compilerOptions": {
       "allowJs": true
     }
   }
   ```

3. Переименуй файл в `.ts` и добавь типы:

   ```typescript
   // TODO: переименуй format.js → format.ts
   // TODO: добавь типы ко всем параметрам и возвращаемым значениям

   type TemperatureUnit = "celsius" | "fahrenheit";
   type TemperatureClass = "cold" | "cool" | "warm" | "hot";

   export function formatTemperature(
     value: number,
     unit: TemperatureUnit
   ): string {
     // TODO: реализация остаётся той же — только типы добавились
   }

   export function formatLastUpdated(timestamp: number): string {
     // TODO
   }

   export function temperatureClass(value: number): TemperatureClass {
     // TODO
   }
   ```

4. Убери `allowJs` после успешной миграции (или оставь, если в проекте ещё есть JS-файлы):

   ```bash
   npx tsc --noEmit
   # Проверь, что format.ts компилируется без ошибок
   ```

5. Обнови импорт в файлах, которые использовали `format.js`:

   ```typescript
   // Было:
   import { formatTemperature } from "./utils/format.js";

   // Стало (расширение не нужно):
   import { formatTemperature } from "@/utils/format";
   ```

### Требования:
- Все три функции имеют явные типы параметров и возвращаемого значения
- `temperatureClass` возвращает `TemperatureClass`, а не просто `string`
- `npx tsc --noEmit` проходит чисто

<details>
<summary>💡 Подсказка: стратегия миграции для большого JS-проекта</summary>

```
1. Включи allowJs + checkJs — увидишь ошибки без переименования
2. Начни с файлов без зависимостей (утилиты, константы, типы)
3. Переименовывай .js → .ts по одному файлу
4. Исправляй ошибки сразу после каждого переименования
5. Двигайся вверх: утилиты → сервисы → компоненты → точка входа
6. Убери allowJs когда все файлы стали .ts
```
</details>

### Проверка:
```bash
npx tsc --noEmit
```

---

## 🎯 Практика 3.2.10.4: Финальная проверка проекта

**Задача:** Собрать итоговый `tsconfig.json` для проекта и убедиться, что весь накопленный TypeScript-код компилируется чисто.

### Шаги:

1. Собери финальный `tsconfig.json`, объединив всё из раздела:

   ```json
   {
     "compilerOptions": {
       "target": "ES2022",
       "module": "ESNext",
       "moduleResolution": "bundler",
       "lib": ["ES2022", "DOM", "DOM.Iterable"],

       "strict": true,
       "noUnusedLocals": true,
       "noUnusedParameters": true,
       "noImplicitReturns": true,
       "noFallthroughCasesInSwitch": true,

       "skipLibCheck": true,
       "incremental": true,

       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"]
       },

       "experimentalDecorators": true,

       "outDir": "dist",
       "rootDir": "src",
       "sourceMap": true
     },
     "include": ["src/**/*"],
     "exclude": ["node_modules", "dist", ".tsbuildinfo"]
   }
   ```

2. Запусти полную проверку всего проекта:

   ```bash
   npx tsc --noEmit
   ```

   Если есть ошибки — исправь. Для каждой ошибки попробуй сначала понять её без подсказок:
   - TS2345 — несовместимость типов
   - TS2339 — свойство не существует на типе
   - TS7006 — неявный `any`
   - TS2366 — не все ветки возвращают значение

3. Запусти сборку:

   ```bash
   npm run build
   ```

4. Проверь структуру `src/` — убедись, что все файлы на своих местах:

   ```
   src/
   ├── types/
   │   ├── weather-base.ts
   │   ├── weather-forecast.ts
   │   ├── weather-ui.ts
   │   ├── index.ts
   │   └── augmentations/
   │       ├── axios.d.ts
   │       ├── globals.d.ts
   │       └── env.d.ts
   ├── modules/
   │   ├── api/
   │   │   ├── ApiClient.ts
   │   │   ├── axiosInstance.ts
   │   │   └── weather.ts
   │   ├── services/
   │   │   └── WeatherService.ts
   │   └── decorators/
   │       ├── classDecorators.ts
   │       ├── methodDecorators.ts
   │       └── propertyDecorators.ts
   └── utils/
       └── format.ts
   ```

5. Ответь письменно (в комментарии или заметке) на три вопроса:

   ```
   // TODO: ответь на вопросы:
   // 1. Какой флаг из strict поймал больше всего ошибок в твоём коде?
   // 2. Какой файл было сложнее всего типизировать и почему?
   // 3. Что бы ты сделал иначе, если бы начинал проект заново?
   ```

### Требования:
- `npx tsc --noEmit` — ноль ошибок
- `npm run build` — сборка завершается успешно
- `tsconfig.json` содержит `strict: true` и алиас `@/*`

<details>
<summary>💡 Подсказка: если ошибок слишком много и не знаешь с чего начать</summary>

```bash
# Сгруппируй ошибки по типу — начни с самого частого:
npx tsc --noEmit 2>&1 | grep "error TS" | sed 's/.*\(TS[0-9]*\).*/\1/' | sort | uniq -c | sort -rn

# Пример вывода:
#  12 TS2345   ← начни с этих
#   8 TS7006
#   3 TS2339
#   1 TS2366
```
</details>

### Проверка:
```bash
npx tsc --noEmit && npm run build
```

---

## ✅ Чеклист раздела

После выполнения всех практик ты должен уметь:

- [ ] Объяснить, какие флаги включает `strict: true`
- [ ] Включить и отключить отдельные флаги строгости (`noUnusedLocals`, `noImplicitReturns`)
- [ ] Настроить алиасы путей в `tsconfig.json` и `vite.config.ts`
- [ ] Мигрировать JS-файл в TypeScript с добавлением типов
- [ ] Прочитать сообщение об ошибке TypeScript и найти её причину
- [ ] Собрать итоговый `tsconfig.json` для Vite-проекта

---

## 🔗 Ресурсы

**Официальная документация:**
- [TSConfig Reference](https://www.typescriptlang.org/tsconfig) — все флаги с описанием
- [Migrating from JavaScript](https://www.typescriptlang.org/docs/handbook/migrating-from-javascript.html) — официальный гайд по миграции
- [Vite: Path Aliases](https://vite.dev/config/shared-options.html#resolve-alias) — алиасы в Vite

**Статьи:**
- [TypeScript Strict Mode](https://www.typescriptlang.org/docs/handbook/2/basic-types.html#strictness) — объяснение strict и его флагов
- [TypeScript Project Configuration](https://www.typescriptlang.org/docs/handbook/project-references.html) — для монорепозиториев

**Связанные материалы:**
- [typescript-eslint](https://typescript-eslint.io/) — линтер для TypeScript-проектов
- [Prettier + TypeScript](https://prettier.io/docs/en/options.html) — форматирование TypeScript-кода

---

⏱ **Время на изучение**: 2-3 часа

---

**Предыдущая тема:** [3.2.9 Declaration Merging и Module Augmentation](./3.2.9-Declaration-Merging.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](https://xmentors.ru/dev)
