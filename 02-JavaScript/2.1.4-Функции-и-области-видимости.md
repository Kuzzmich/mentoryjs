# 2.1.4 Функции и области видимости

Функции — строительные блоки любого приложения. Без функций код превращается в хаос из повторений и запутанной логики.

---

## 🎯 Что ты узнаешь

- Три способа объявления функций: declaration, expression, arrow
- Параметры, аргументы и значения по умолчанию
- Возврат значений через `return`
- Области видимости: глобальная, функциональная, блочная
- Что такое hoisting для функций
- Чистые функции и побочные эффекты
- Разницу между стрелочными и обычными функциями

---

## Зачем нужны функции?

Функция — переиспользуемый блок кода, который выполняет конкретную задачу.

**Преимущества:**
- Избегаешь дублирования кода
- Код легче читать и поддерживать
- Можно тестировать изолированно
- Создаёшь абстракции

---

## Объявление функций

### Function Declaration

Классический способ:

```javascript
function greet(name) {
  return `Привет, ${name}!`;
}

console.log(greet("Алексей"));  // "Привет, Алексей!"
```

**Особенности:**
- Поднимается (hoisting) — можно вызвать до объявления
- Имеет имя
- Создаёт свою область видимости

```javascript
// Работает благодаря hoisting
console.log(sayHello("Иван"));  // "Привет, Иван!"

function sayHello(name) {
  return `Привет, ${name}!`;
}
```

### Function Expression

Функция как значение переменной:

```javascript
const greet = function(name) {
  return `Привет, ${name}!`;
};

console.log(greet("Алексей"));  // "Привет, Алексей!"
```

**Особенности:**
- НЕ поднимается — нельзя вызвать до объявления
- Может быть анонимной или именованной
- Переменная поднимается, но не инициализируется

```javascript
// Ошибка!
console.log(sayHello("Иван"));  // ReferenceError

const sayHello = function(name) {
  return `Привет, ${name}!`;
};
```

### Arrow Functions (Стрелочные функции)

Современный короткий синтаксис:

```javascript
// Базовый синтаксис
const greet = (name) => {
  return `Привет, ${name}!`;
};

// Короткая запись (неявный return)
const greet = (name) => `Привет, ${name}!`;

// Один параметр — скобки необязательны
const double = n => n * 2;

// Без параметров — пустые скобки
const getRandomNumber = () => Math.random();

// Возврат объекта — оборачивай в ()
const createUser = (name) => ({ name: name, active: true });
```

**Когда использовать стрелочные функции:**
- ✅ Короткие функции (1-3 строки)
- ✅ Колбэки в `.map()`, `.filter()` и т.д.
- ✅ Когда не нужен свой `this` (подробнее о `this` — в продвинутом уровне)

**Когда НЕ использовать:**
- ❌ Методы объектов (проблемы с `this`)
- ❌ Длинные функции (хуже читаемость)

> 💡 **О контексте `this`:** Стрелочные функции не создают свой контекст `this`. Пока это можно не понимать глубоко — изучим подробно в теме "Замыкания и контекст".

---

## Параметры и аргументы

```javascript
function greet(name) {  // name — параметр
  return `Привет, ${name}!`;
}

greet("Алексей");  // "Алексей" — аргумент
```

### Значения по умолчанию

```javascript
function greet(name = "Гость") {
  return `Привет, ${name}!`;
}

console.log(greet());          // "Привет, Гость!"
console.log(greet("Алексей")); // "Привет, Алексей!"
```

### Rest параметры

Собирают неограниченное количество аргументов в массив:

```javascript
function sum(...numbers) {
  let total = 0;
  for (let num of numbers) {
    total += num;
  }
  return total;
}

console.log(sum(1, 2, 3));        // 6
console.log(sum(1, 2, 3, 4, 5));  // 15
```

---

## Return (возврат значения)

```javascript
function add(a, b) {
  return a + b;  // возвращает результат
}

let result = add(5, 3);  // result = 8
```

**Важно:**
- После `return` код не выполняется
- Функция без `return` возвращает `undefined`

```javascript
function greet(name) {
  console.log(`Привет, ${name}!`);
  // нет return — вернёт undefined
}

let result = greet("Иван");  // result = undefined
```

### Множественный return

```javascript
function getWeatherStatus(temp) {
  if (temp > 30) return "Жарко";
  if (temp > 20) return "Тепло";
  if (temp > 10) return "Прохладно";
  return "Холодно";
}

// Ранний выход из функции
function divide(a, b) {
  if (b === 0) {
    return "Ошибка: деление на ноль";
  }
  return a / b;
}
```

---

## Области видимости (Scope)

**Scope** — где переменная доступна в коде.

### Глобальная область

```javascript
let globalVar = "Доступна везде";

function test() {
  console.log(globalVar);  // Доступна
}

test();
console.log(globalVar);    // Доступна
```

> ⚠️ Избегай глобальных переменных — они усложняют отладку и могут конфликтовать.

### Локальная область (функции)

```javascript
function test() {
  let localVar = "Только здесь";
  console.log(localVar);  // Доступна
}

test();
console.log(localVar);    // ReferenceError
```

### Блочная область

```javascript
if (true) {
  let blockVar = "Только в блоке";
  console.log(blockVar);  // Доступна
}
console.log(blockVar);    // ReferenceError
```

### Вложенные области (Scope Chain)

```javascript
let global = "глобальная";

function outer() {
  let outerVar = "внешняя";

  function inner() {
    let innerVar = "внутренняя";

    console.log(innerVar);  // Доступна
    console.log(outerVar);  // Доступна (поиск выше)
    console.log(global);    // Доступна (поиск выше)
  }

  inner();
  console.log(innerVar);  // ReferenceError
}

outer();
```

> 💡 Функция может читать переменные из внешних областей, но не наоборот.

---

## Чистые функции (Pure Functions)

**Чистая функция:**
1. Всегда возвращает одинаковый результат для одинаковых аргументов
2. Не имеет побочных эффектов (не меняет внешнее состояние)

```javascript
// ✅ Чистая функция
function add(a, b) {
  return a + b;
}

// ❌ Нечистая функция (зависит от внешней переменной)
let total = 0;
function addToTotal(value) {
  total += value;  // изменяет внешнее состояние
  return total;
}

// ❌ Нечистая функция (побочный эффект)
function greet(name) {
  console.log(`Привет, ${name}!`);  // console.log — побочный эффект
}
```

**Зачем писать чистые функции:**
- Легче тестировать
- Легче понимать
- Можно безопасно использовать повторно
- Меньше багов

---

## 🔧 Практика 2.1.4.1: Рефакторинг на функции

Возьми код из предыдущих практик и вынеси повторяющуюся логику в функции.

```javascript
// Данные о погоде
const city = "Москва";
let temperature = 15;
let humidity = 65;
let description = "Облачно";

// TODO 1: Создай функцию getTemperatureStatus
// Параметр: temp (число)
// Возвращает: строку со статусом
// "Жарко 🥵" если temp > 30
// "Тепло ☀️" если temp > 20
// "Прохладно 🍂" если temp > 10
// "Холодно ❄️" иначе

// TODO 2: Создай функцию celsiusToFahrenheit
// Параметр: celsius (число)
// Возвращает: температуру в Фаренгейтах
// Формула: F = C * 9/5 + 32

// TODO 3: Создай функцию getHumidityStatus
// Параметр: humidity (число)
// Возвращает: строку
// "Высокая влажность 💧" если > 80
// "Комфортная влажность" если > 60
// "Низкая влажность" иначе

// TODO 4: Создай функцию needUmbrella
// Параметры: humidity (число), description (строка)
// Возвращает: true/false
// true если humidity > 80 ИЛИ в description есть "дождь"

// TODO 5: Создай функцию formatWeatherReport
// Параметры: city, temp, humidity, description
// Возвращает: строку с отчётом о погоде
// Формат:
// === ПОГОДА В [CITY] ===
// Температура: [temp]°C ([fahrenheit]°F)
// Статус: [temperatureStatus]
// Влажность: [humidityStatus]
// Зонт: [нужен/не нужен]
// Используй все функции выше

// TODO 6: Вызови formatWeatherReport и выведи результат
```

**Проверь себя:**
- Все функции должны быть чистыми (кроме вывода в консоль)
- Функции не должны использовать внешние переменные
- Попробуй вызвать функции с разными аргументами

---

## 🔧 Практика 2.1.4.2: Работа с массивом температур

Создай функции для анализа недельного прогноза.

```javascript
const weekTemperatures = [18, 20, 22, 19, 17, 21, 23];

// TODO 1: Создай функцию getMaxTemp
// Параметр: массив температур
// Возвращает: максимальную температуру

// TODO 2: Создай функцию getMinTemp
// Параметр: массив температур
// Возвращает: минимальную температуру

// TODO 3: Создай функцию getAverageTemp
// Параметр: массив температур
// Возвращает: среднюю температуру (округлённую до 1 знака)
// Подсказка: используй .toFixed(1)

// TODO 4: Создай функцию countWarmDays
// Параметры: массив температур, порог (по умолчанию 20)
// Возвращает: количество дней с температурой >= порог

// TODO 5: Создай функцию getTempTrend
// Параметр: массив температур
// Возвращает: "Теплеет" если последняя > первой
//            "Холодает" если последняя < первой
//            "Стабильно" если равны

// TODO 6: Создай функцию getWeeklySummary
// Параметр: массив температур
// Возвращает: объект с полной статистикой
// {
//   max: ...,
//   min: ...,
//   average: ...,
//   warmDays: ...,
//   trend: ...
// }
// Используй все функции выше

// TODO 7: Выведи результат getWeeklySummary
```

---

## 🔧 Практика 2.1.4.3: Стрелочные функции

Перепиши функции используя стрелочный синтаксис.

```javascript
// TODO 1: Перепиши в стрелочную функцию (однострочную)
function double(n) {
  return n * 2;
}

// TODO 2: Перепиши в стрелочную функцию (однострочную)
function isWarm(temp) {
  return temp > 20;
}

// TODO 3: Перепиши в стрелочную функцию
function getWeatherEmoji(description) {
  if (description.includes("дождь")) return "🌧️";
  if (description.includes("солнце")) return "☀️";
  if (description.includes("облачно")) return "☁️";
  return "🌡️";
}

// TODO 4: Используй стрелочные функции с методами массива
const temperatures = [15, 20, 25, 18, 22];

// Создай новый массив, где каждая температура в Фаренгейтах
// Используй .map() и стрелочную функцию

// Отфильтруй только тёплые дни (>= 20)
// Используй .filter() и стрелочную функцию

// Посчитай сумму всех температур
// Используй .reduce() и стрелочную функцию
```

---

## 📝 Итоговое задание

Создай систему для работы с данными нескольких городов.

```javascript
const cities = [
  { name: "Москва", temp: 15, humidity: 65, description: "Облачно" },
  { name: "Санкт-Петербург", temp: 12, humidity: 75, description: "Дождь" },
  { name: "Казань", temp: 18, humidity: 60, description: "Ясно" },
  { name: "Сочи", temp: 25, humidity: 70, description: "Солнечно" }
];

// TODO 1: Создай функцию analyzeCity
// Параметр: объект города
// Возвращает: объект с анализом
// {
//   name: ...,
//   tempStatus: ... (используй функцию из практики 2.1.3.1),
//   humidityStatus: ...,
//   needUmbrella: ...,
//   comfortable: ... (true если temp 18-25 И humidity 50-70)
// }

// TODO 2: Создай функцию findWarmestCity
// Параметр: массив городов
// Возвращает: объект самого тёплого города

// TODO 3: Создай функцию findComfortableCities
// Параметр: массив городов
// Возвращает: массив городов с комфортной погодой
// Используй .filter() и проверку comfortable из analyzeCity

// TODO 4: Создай функцию getCitiesReport
// Параметр: массив городов
// Возвращает: строку с отчётом по всем городам
// Формат:
// === СВОДКА ПО ГОРОДАМ ===
//
// Москва:
// - Температура: 15°C (Прохладно)
// - Влажность: 65% (Комфортная)
// - Зонт: не нужен
//
// [и так для всех городов]
//
// Самый тёплый: Сочи (25°C)
// Комфортных городов: 2

// TODO 5: Выведи результат getCitiesReport(cities)
```

**Требования:**
- Все функции должны быть чистыми (где возможно)
- Используй стрелочные функции для коротких операций
- Переиспользуй функции из предыдущих практик
- Не дублируй код

---

## ✅ Чеклист раздела

После изучения темы ты должен:

- [ ] Создавать функции тремя способами (declaration, expression, arrow)
- [ ] Понимать разницу между этими способами
- [ ] Использовать параметры и значения по умолчанию
- [ ] Возвращать значения из функций
- [ ] Понимать области видимости (scope)
- [ ] Объяснить hoisting функций
- [ ] Использовать rest параметры (...)
- [ ] Писать чистые функции
- [ ] Выбирать подходящий тип функции для задачи
- [ ] Разбивать код на переиспользуемые функции

---

## 🔗 Ресурсы

- 📖 [Learn JavaScript: Функции](https://learn.javascript.ru/function-basics)
- 📖 [Learn JavaScript: Function Expression](https://learn.javascript.ru/function-expressions)
- 📖 [Learn JavaScript: Arrow functions](https://learn.javascript.ru/arrow-functions-basics)
- 📖 [MDN: Функции](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Functions)
- 📖 [MDN: Стрелочные функции](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Functions/Arrow_functions)

---

⏱ **Время на изучение**: 4-5 часов

---

**Предыдущая тема:** [2.1.3 Условия и циклы](./2.1.3-Условия-и-циклы.md)

**Следующая тема:** [2.1.5 DOM и события](./2.1.5-DOM-и-события.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](ссылка)
