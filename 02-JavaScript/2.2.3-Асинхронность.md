# 2.2.3 Асинхронность: callbacks → promises → async/await

Асинхронность — одна из ключевых особенностей JavaScript. Без понимания асинхронного кода невозможно работать с API, базами данных и любыми операциями, которые требуют времени.

> 💡 **Примечание:** В этой теме мы изучаем основы асинхронности через `Promise` и `async/await`. Работу с реальными API через `fetch` мы подробно разберём в следующей теме [2.2.4 Работа с API](./2.2.4-Работа-с-API.md). Пока что для примеров мы будем использовать `setTimeout` для имитации асинхронных операций.

---

## 🎯 Что ты узнаешь

- Разницу между синхронным и асинхронным кодом
- Что такое callback-функции и callback hell
- Как работают промисы (Promise)
- Методы промисов: `then`, `catch`, `finally`
- Создание собственных промисов
- Async/await — современный способ работы с асинхронностью
- Обработка ошибок в асинхронном коде
- `Promise.all`, `Promise.race`, `Promise.allSettled`
- Практическая работа с API

---

## 📖 Синхронный vs асинхронный код

### Синхронный код

Выполняется последовательно, строка за строкой. Следующая операция ждёт завершения предыдущей.

```javascript
console.log('1. Начало');

function heavyTask() {
  let sum = 0;
  for (let i = 0; i < 1000000000; i++) {
    sum += i;
  }
  return sum;
}

console.log('2. Запускаем тяжёлую задачу...');
const result = heavyTask(); // Блокирует выполнение
console.log('3. Результат:', result);
console.log('4. Конец');

// Вывод:
// 1. Начало
// 2. Запускаем тяжёлую задачу...
// (пауза несколько секунд)
// 3. Результат: 499999999500000000
// 4. Конец
```

**Проблема:** Пока выполняется `heavyTask()`, весь код заблокирован. В браузере интерфейс зависнет.

---

### Асинхронный код

Не блокирует выполнение. Операция запускается, а программа продолжает работать.

```javascript
console.log('1. Начало');

setTimeout(() => {
  console.log('3. Это выполнится через 2 секунды');
}, 2000);

console.log('2. Конец');

// Вывод:
// 1. Начало
// 2. Конец
// (пауза 2 секунды)
// 3. Это выполнится через 2 секунды
```

**Примеры асинхронных операций:**
- HTTP-запросы (fetch — изучим в [следующей теме](./2.2.4-Работа-с-API.md))
- Чтение/запись файлов
- Таймеры (setTimeout, setInterval)
- Работа с базами данных
- Геолокация
- Анимации

---

## 📖 Callbacks (колбэки)

**Callback** — функция, которая передаётся в другую функцию и вызывается позже.

### Простой пример

```javascript
function fetchUserData(userId, callback) {
  console.log(`Загружаем данные пользователя ${userId}...`);

  setTimeout(() => {
    const user = { id: userId, name: 'Анна', age: 25 };
    callback(user); // Вызываем callback с результатом
  }, 1000);
}

fetchUserData(1, (user) => {
  console.log('Пользователь:', user);
});

// Вывод:
// Загружаем данные пользователя 1...
// (пауза 1 секунда)
// Пользователь: { id: 1, name: 'Анна', age: 25 }
```

---

### Callback Hell (ад колбэков)

Когда нужно выполнить несколько асинхронных операций последовательно:

```javascript
// ❌ Плохо: вложенность колбэков
fetchUser(1, (user) => {
  fetchPosts(user.id, (posts) => {
    fetchComments(posts[0].id, (comments) => {
      fetchLikes(comments[0].id, (likes) => {
        console.log('Лайки:', likes);
        // Ещё глубже...
      });
    });
  });
});

// Проблемы:
// 1. Трудно читать
// 2. Сложно обрабатывать ошибки
// 3. Невозможно использовать try/catch
// 4. Дублирование логики обработки ошибок
```

**Решение:** Промисы и async/await.

---

## 📖 Промисы (Promises)

**Promise** — объект, представляющий результат асинхронной операции (успех или ошибка).

### Три состояния промиса

```javascript
// 1. Pending (ожидание) — начальное состояние
// 2. Fulfilled (выполнен) — операция успешна
// 3. Rejected (отклонён) — произошла ошибка

┌─────────┐
│ Pending │
└────┬────┘
     │
     ├──> Fulfilled (resolve)
     │
     └──> Rejected (reject)
```

---

### Создание промиса

```javascript
const promise = new Promise((resolve, reject) => {
  // Асинхронная операция
  const success = true;

  setTimeout(() => {
    if (success) {
      resolve('Операция успешна!'); // Переход в Fulfilled
    } else {
      reject('Ошибка!'); // Переход в Rejected
    }
  }, 1000);
});

console.log(promise); // Promise { <pending> }
```

---

### Использование промисов: then, catch, finally

```javascript
function fetchUserData(userId) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (userId > 0) {
        resolve({ id: userId, name: 'Иван', age: 30 });
      } else {
        reject('Неверный ID пользователя');
      }
    }, 1000);
  });
}

// Использование
fetchUserData(1)
  .then((user) => {
    console.log('Успех:', user);
    return user.age; // Можно вернуть значение для следующего then
  })
  .then((age) => {
    console.log('Возраст:', age);
  })
  .catch((error) => {
    console.error('Ошибка:', error);
  })
  .finally(() => {
    console.log('Операция завершена');
  });
```

**Методы:**
- `then(callback)` — вызывается при успехе (resolve)
- `catch(callback)` — вызывается при ошибке (reject)
- `finally(callback)` — вызывается всегда (успех или ошибка)

---

### Цепочки промисов

Промисы решают проблему callback hell:

```javascript
// ✅ Хорошо: цепочка промисов
fetchUser(1)
  .then(user => fetchPosts(user.id))
  .then(posts => fetchComments(posts[0].id))
  .then(comments => fetchLikes(comments[0].id))
  .then(likes => console.log('Лайки:', likes))
  .catch(error => console.error('Ошибка:', error));

// Намного читабельнее!
```

**Важно:** Каждый `then` возвращает новый промис.

```javascript
const promise = fetch('https://api.example.com/data')
  .then(response => response.json()) // Промис
  .then(data => data.users)           // Промис
  .then(users => users[0]);           // Промис

console.log(promise); // Promise (не значение!)
```

---

### Обработка ошибок

```javascript
fetch('https://api.example.com/data')
  .then(response => {
    if (!response.ok) {
      throw new Error(`HTTP error: ${response.status}`);
    }
    return response.json();
  })
  .then(data => {
    console.log(data);
  })
  .catch(error => {
    // Ловит ошибки из любого then
    console.error('Произошла ошибка:', error.message);
  })
  .finally(() => {
    console.log('Запрос завершён');
  });
```

---

## 📖 Async / Await

**Async/await** — синтаксический сахар над промисами. Делает асинхронный код похожим на синхронный.

### Базовый синтаксис

```javascript
// Функция с async всегда возвращает промис
async function fetchData() {
  return 'Данные';
}

fetchData().then(data => console.log(data)); // "Данные"
```

```javascript
// await ждёт выполнения промиса
async function loadUser() {
  const response = await fetch('https://api.example.com/user/1');
  const user = await response.json();
  return user;
}

loadUser().then(user => console.log(user));
```

**Правила:**
- `await` можно использовать только внутри `async` функции
- `await` ждёт, пока промис выполнится
- `async` функция всегда возвращает промис

---

### Сравнение: промисы vs async/await

```javascript
// С промисами
function getWeather() {
  return fetch('https://api.weather.com/current')
    .then(response => response.json())
    .then(data => data.temperature)
    .catch(error => console.error(error));
}

// С async/await ✅
async function getWeather() {
  try {
    const response = await fetch('https://api.weather.com/current');
    const data = await response.json();
    return data.temperature;
  } catch (error) {
    console.error(error);
  }
}
```

Async/await читается как обычный синхронный код!

---

### Обработка ошибок с try/catch

```javascript
async function fetchUserData(userId) {
  try {
    const response = await fetch(`https://api.example.com/users/${userId}`);

    if (!response.ok) {
      throw new Error(`HTTP error: ${response.status}`);
    }

    const user = await response.json();
    return user;

  } catch (error) {
    console.error('Ошибка загрузки данных:', error.message);
    return null; // Возвращаем null в случае ошибки
  }
}

const user = await fetchUserData(1);
console.log(user);
```

> 💡 **Примечание:** Обработка ошибок будет подробно рассмотрена в отдельной теме [2.2.7 Обработка ошибок](./2.2.7-Обработка-ошибок.md). Там ты узнаешь про создание собственных типов ошибок, валидацию данных и продвинутые паттерны обработки ошибок.

---

## 📖 Promise методы

### Promise.all

Ждёт выполнения **всех** промисов. Если хотя бы один отклонён — весь Promise.all отклонён.

```javascript
const promise1 = Promise.resolve(3);
const promise2 = new Promise(resolve => setTimeout(() => resolve(2), 1000));
const promise3 = Promise.resolve(5);

Promise.all([promise1, promise2, promise3])
  .then(values => {
    console.log(values); // [3, 2, 5]
  })
  .catch(error => {
    console.error('Одна из операций провалилась:', error);
  });
```

**Использование:** Загрузка нескольких ресурсов параллельно.

---

### Promise.race

Возвращает результат **первого** завершившегося промиса (успех или ошибка).

```javascript
const slow = new Promise(resolve => setTimeout(() => resolve('Медленный'), 2000));
const fast = new Promise(resolve => setTimeout(() => resolve('Быстрый'), 500));

Promise.race([slow, fast])
  .then(result => console.log(result)); // "Быстрый"
```

**Использование:** Таймауты для запросов, выбор самого быстрого сервера.

---

### Promise.allSettled

Ждёт выполнения **всех** промисов (успех или ошибка) и возвращает массив результатов.

```javascript
const promises = [
  Promise.resolve('Успех 1'),
  Promise.reject('Ошибка'),
  Promise.resolve('Успех 2')
];

Promise.allSettled(promises)
  .then(results => {
    console.log(results);
    // [
    //   { status: 'fulfilled', value: 'Успех 1' },
    //   { status: 'rejected', reason: 'Ошибка' },
    //   { status: 'fulfilled', value: 'Успех 2' }
    // ]
  });
```

**Использование:** Когда нужно знать результат всех операций, даже если некоторые провалились.

---

### Promise.any

Возвращает результат **первого успешного** промиса. Игнорирует ошибки.

```javascript
const promises = [
  Promise.reject('Ошибка 1'),
  Promise.resolve('Успех!'),
  Promise.reject('Ошибка 2')
];

Promise.any(promises)
  .then(result => console.log(result)) // "Успех!"
  .catch(error => console.error('Все промисы провалились'));
```

---

### Параллельное выполнение с async/await

Теперь, когда ты знаешь про `Promise.all`, вот как использовать его с async/await:

```javascript
// ❌ Последовательно (медленно)
async function loadData() {
  const user = await fetchUser(1);     // Ждём 1 секунду
  const posts = await fetchPosts(1);   // Ждём ещё 1 секунду
  const comments = await fetchComments(1); // Ждём ещё 1 секунду
  // Итого: 3 секунды
}

// ✅ Параллельно (быстро)
async function loadData() {
  const [user, posts, comments] = await Promise.all([
    fetchUser(1),
    fetchPosts(1),
    fetchComments(1)
  ]);
  // Итого: 1 секунда (все запросы идут одновременно)
}
```

**Важно:** Запросы запускаются одновременно, `await Promise.all` ждёт их завершения.

---

## 🌤️ Практика: Погодное приложение (Этап 2.3)

Интегрируем реальный API погоды в приложение!

### Подготовка: получение API ключа

1. Зарегистрируйся на [OpenWeather](https://openweathermap.org/)
2. Получи API ключ в разделе "API keys"
3. API ключ активируется в течение 10 минут

### О функции fetch

> 💡 **Важно:** `fetch` — это встроенная функция для HTTP-запросов. Подробно мы её изучим в [следующей теме](./2.2.4-Работа-с-API.md). Для текущих заданий используй готовый код ниже.

**Готовая функция для HTTP-запросов:**

```javascript
/**
 * Простая обёртка для fetch (используй в заданиях)
 * В следующей теме мы подробно разберём, как это работает
 */
function fetchWeatherData(url) {
  return fetch(url)
    .then(response => {
      if (!response.ok) {
        throw new Error(`HTTP ошибка! Статус: ${response.status}`);
      }
      return response.json();
    });
}
```

**Как использовать:**

```javascript
const API_KEY = 'твой_api_ключ';
const API_URL = 'https://api.openweathermap.org/data/2.5/weather';

// Пример использования
const url = `${API_URL}?q=Moscow&appid=${API_KEY}&units=metric&lang=ru`;

fetchWeatherData(url)
  .then(data => console.log(data))
  .catch(error => console.error(error));
```

---

### Задание 2.2.3.1: Первый запрос к API

Создай функцию для получения погоды с использованием промисов.

**Стартовый код:**

```javascript
const API_KEY = 'твой_api_ключ';
const API_URL = 'https://api.openweathermap.org/data/2.5/weather';

// Готовая функция для HTTP-запросов (используй её)
function fetchWeatherData(url) {
  return fetch(url)
    .then(response => {
      if (!response.ok) {
        throw new Error(`HTTP ошибка! Статус: ${response.status}`);
      }
      return response.json();
    });
}

// TODO: Создай функцию getWeatherByCity(city)
// Она должна:
// 1. Составить URL с параметрами (город, API ключ, units=metric, lang=ru)
// 2. Использовать fetchWeatherData для запроса
// 3. Вернуть промис с данными о погоде

function getWeatherByCity(city) {
  // Твоя реализация
}

// Проверка
getWeatherByCity('Москва')
  .then(data => {
    console.log('Город:', data.name);
    console.log('Температура:', data.main.temp);
    console.log('Описание:', data.weather[0].description);
  })
  .catch(error => {
    console.error('Ошибка:', error.message);
  });
```

<details>
<summary>💡 Решение</summary>

```javascript
function getWeatherByCity(city) {
  const url = `${API_URL}?q=${city}&appid=${API_KEY}&units=metric&lang=ru`;
  return fetchWeatherData(url);
}

// Проверка
getWeatherByCity('Москва')
  .then(data => {
    console.log('Город:', data.name);
    console.log('Температура:', Math.round(data.main.temp), '°C');
    console.log('Описание:', data.weather[0].description);
  })
  .catch(error => {
    console.error('Ошибка:', error.message);
  });
```

**Как это работает:**
1. Составляем URL с параметрами запроса
2. Вызываем `fetchWeatherData(url)` — она возвращает промис
3. Промис резолвится с данными о погоде

</details>

---

### Задание 2.2.3.2: Async/await версия

Перепиши функцию с использованием async/await.

**Стартовый код:**

```javascript
// Используй ту же функцию fetchWeatherData из предыдущего задания

async function getWeatherByCity(city) {
  // TODO: Реализуй с async/await
  // 1. Составь URL
  // 2. Используй await fetchWeatherData(url)
  // 3. Обработай ошибки через try/catch
}

// Проверка
async function test() {
  try {
    const weather = await getWeatherByCity('Санкт-Петербург');
    console.log('Город:', weather.name);
    console.log('Температура:', Math.round(weather.main.temp), '°C');
  } catch (error) {
    console.error('Ошибка:', error.message);
  }
}

test();
```

<details>
<summary>💡 Решение</summary>

```javascript
async function getWeatherByCity(city) {
  try {
    const url = `${API_URL}?q=${city}&appid=${API_KEY}&units=metric&lang=ru`;
    const data = await fetchWeatherData(url);
    return data;
  } catch (error) {
    throw new Error(`Не удалось получить погоду для ${city}: ${error.message}`);
  }
}

// Проверка
async function test() {
  try {
    const weather = await getWeatherByCity('Санкт-Петербург');
    console.log('Город:', weather.name);
    console.log('Температура:', Math.round(weather.main.temp), '°C');
    console.log('Описание:', weather.weather[0].description);
  } catch (error) {
    console.error('Ошибка:', error.message);
  }
}

test();
```

**Разница с промисами:**
- Код читается как синхронный (сверху вниз)
- `await` заменяет `.then()`
- `try/catch` заменяет `.catch()`
- Проще обрабатывать ошибки

</details>

---

### Задание 2.2.3.3: Загрузка погоды для нескольких городов

Создай функцию, которая загружает погоду для массива городов параллельно с помощью `Promise.all`.

**Стартовый код:**

```javascript
// Используй функцию getWeatherByCity из предыдущих заданий

async function getWeatherForCities(cities) {
  // TODO: Реализуй параллельную загрузку
  // 1. Создай массив промисов для каждого города
  // 2. Используй Promise.all для параллельного выполнения
  // 3. Верни массив данных о погоде
}

// Проверка
const cities = ['Москва', 'Санкт-Петербург', 'Казань', 'Новосибирск'];

getWeatherForCities(cities)
  .then(weatherData => {
    console.log('Погода в городах:');
    weatherData.forEach(data => {
      console.log(`${data.name}: ${Math.round(data.main.temp)}°C, ${data.weather[0].description}`);
    });
  })
  .catch(error => {
    console.error('Ошибка:', error.message);
  });
```

<details>
<summary>💡 Решение</summary>

```javascript
async function getWeatherForCities(cities) {
  // Создаём массив промисов
  const promises = cities.map(city => getWeatherByCity(city));

  // Promise.all запускает все промисы параллельно
  const results = await Promise.all(promises);

  return results;
}

// Короткая версия
async function getWeatherForCities(cities) {
  return await Promise.all(cities.map(city => getWeatherByCity(city)));
}
```

**Почему быстрее:**
- Все запросы выполняются одновременно
- Общее время = время самого долгого запроса (не сумма всех)
- Если один запрос занимает 1 секунду, 4 запроса займут ~1 секунду (не 4)


</details>

---

### Задание 2.2.3.4: Обработка ошибок с Promise.allSettled

Измени предыдущую функцию: если какой-то город не найден, не прерывай загрузку остальных.

```javascript
async function getWeatherForCitiesSafe(cities) {
  // Используй Promise.allSettled
  // Верни объекты с результатами: { city, success, data/error }
}

// Проверка
const cities = ['Москва', 'НесуществующийГород', 'Казань'];

getWeatherForCitiesSafe(cities)
  .then(results => {
    results.forEach(result => {
      if (result.success) {
        console.log(`✅ ${result.city}: ${result.data.main.temp}°C`);
      } else {
        console.log(`❌ ${result.city}: ${result.error}`);
      }
    });
  });
```

<details>
<summary>💡 Подсказка</summary>

```javascript
async function getWeatherForCitiesSafe(cities) {
  const promises = cities.map(city =>
    getWeatherByCity(city)
      .then(data => ({ city, success: true, data }))
      .catch(error => ({ city, success: false, error: error.message }))
  );

  return await Promise.allSettled(promises)
    .then(results => results.map(r => r.value));
}
```
</details>

---

### Задание 2.2.3.5: Таймаут для запросов

Создай функцию, которая отменяет запрос, если он выполняется дольше определённого времени.

```javascript
function fetchWithTimeout(url, timeout = 5000) {
  // Используй Promise.race для реализации таймаута
  // Если запрос не завершился за timeout мс — выбрось ошибку
}

// Проверка
fetchWithTimeout('https://api.openweathermap.org/data/2.5/weather?q=Moscow&appid=...', 3000)
  .then(response => response.json())
  .then(data => console.log(data))
  .catch(error => console.error('Ошибка:', error.message));
```

<details>
<summary>💡 Подсказка</summary>

```javascript
function fetchWithTimeout(url, timeout = 5000) {
  const timeoutPromise = new Promise((_, reject) => {
    setTimeout(() => reject(new Error('Превышено время ожидания')), timeout);
  });

  return Promise.race([
    fetch(url),
    timeoutPromise
  ]);
}
```
</details>

---

## 🏆 Итоговое задание 2.2.3

Создай **полноценное погодное приложение** с асинхронной загрузкой данных из API.

### Требования:

1. **Поиск погоды по названию города:**
   - Форма ввода города
   - Кнопка "Найти"
   - Асинхронная загрузка данных

2. **Отображение данных:**
   - Название города
   - Температура
   - Описание погоды
   - Иконка погоды
   - Влажность, скорость ветра

3. **Обработка ошибок:**
   - Город не найден
   - Проблемы с сетью
   - Пользовательские сообщения об ошибках

4. **Загрузка нескольких городов:**
   - Кнопка "Добавить в избранное"
   - Список избранных городов
   - Параллельная загрузка погоды для всех избранных

5. **UI индикаторы:**
   - Loader во время загрузки
   - Сообщения об успехе/ошибке

### HTML структура:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погодное приложение — API</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
    }

    h1 {
      color: white;
      text-align: center;
      margin-bottom: 30px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .search-box {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      margin-bottom: 20px;
    }

    .search-box form {
      display: flex;
      gap: 10px;
    }

    .search-box input {
      flex: 1;
      padding: 12px;
      border: 2px solid #ddd;
      border-radius: 8px;
      font-size: 16px;
    }

    .search-box button {
      padding: 12px 24px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .search-box button:hover {
      background: #5568d3;
    }

    .search-box button:disabled {
      background: #ccc;
      cursor: not-allowed;
    }

    .weather-display {
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      margin-bottom: 20px;
      text-align: center;
    }

    .weather-display.hidden {
      display: none;
    }

    .weather-icon {
      width: 100px;
      height: 100px;
    }

    .city-name {
      font-size: 32px;
      color: #333;
      margin: 10px 0;
    }

    .temperature {
      font-size: 48px;
      font-weight: bold;
      color: #667eea;
      margin: 10px 0;
    }

    .description {
      font-size: 20px;
      color: #666;
      text-transform: capitalize;
      margin-bottom: 20px;
    }

    .details {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin-top: 20px;
    }

    .detail-item {
      text-align: center;
    }

    .detail-label {
      color: #999;
      font-size: 14px;
    }

    .detail-value {
      color: #333;
      font-size: 18px;
      font-weight: bold;
    }

    .favorites {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    .favorites h3 {
      margin-bottom: 15px;
      color: #333;
    }

    .favorites-list {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 15px;
    }

    .favorite-item {
      background: #f5f5f5;
      padding: 15px;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .favorite-item:hover {
      background: #e0e0e0;
    }

    .loader {
      border: 4px solid #f3f3f3;
      border-top: 4px solid #667eea;
      border-radius: 50%;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 20px auto;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .error-message {
      background: #ffebee;
      color: #c62828;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 20px;
      text-align: center;
    }

    .error-message.hidden {
      display: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🌤 Погодное приложение</h1>

    <div class="search-box">
      <form id="search-form">
        <input
          type="text"
          id="city-input"
          placeholder="Введите название города..."
          required
        >
        <button type="submit" id="search-btn">Найти</button>
      </form>
    </div>

    <div id="loader" class="loader hidden"></div>

    <div id="error-message" class="error-message hidden"></div>

    <div id="weather-display" class="weather-display hidden">
      <img id="weather-icon" class="weather-icon" src="" alt="">
      <h2 id="city-name" class="city-name"></h2>
      <div id="temperature" class="temperature"></div>
      <p id="description" class="description"></p>

      <div class="details">
        <div class="detail-item">
          <div class="detail-label">Влажность</div>
          <div id="humidity" class="detail-value"></div>
        </div>
        <div class="detail-item">
          <div class="detail-label">Ветер</div>
          <div id="wind" class="detail-value"></div>
        </div>
        <div class="detail-item">
          <div class="detail-label">Давление</div>
          <div id="pressure" class="detail-value"></div>
        </div>
      </div>

      <button id="add-favorite-btn" class="search-box button" style="margin-top: 20px;">
        ⭐ Добавить в избранное
      </button>
    </div>

    <div class="favorites">
      <h3>Избранные города</h3>
      <div id="favorites-list" class="favorites-list"></div>
    </div>
  </div>

  <script>
    const API_KEY = 'твой_api_ключ'; // Замени на свой ключ
    const API_URL = 'https://api.openweathermap.org/data/2.5/weather';

    // Твой код здесь

    // Функции для работы с API
    async function getWeatherByCity(city) {
      // Реализуй загрузку погоды
    }

    // Функции для работы с UI
    function showLoader() {
      // Показать loader
    }

    function hideLoader() {
      // Скрыть loader
    }

    function showError(message) {
      // Показать сообщение об ошибке
    }

    function hideError() {
      // Скрыть сообщение об ошибке
    }

    function displayWeather(data) {
      // Отобразить данные о погоде
    }

    // Работа с избранным
    function addToFavorites(city) {
      // Добавить город в избранное (localStorage)
    }

    function loadFavorites() {
      // Загрузить избранные города из localStorage
    }

    function displayFavorites() {
      // Отобразить избранные города
    }

    // Обработчики событий
    document.getElementById('search-form').addEventListener('submit', async (e) => {
      e.preventDefault();
      // Обработка поиска
    });

    // Инициализация
    loadFavorites();
    displayFavorites();
  </script>
</body>
</html>
```

### Подсказки по реализации:

<details>
<summary>💡 Загрузка данных</summary>

```javascript
async function getWeatherByCity(city) {
  const url = `${API_URL}?q=${city}&appid=${API_KEY}&units=metric&lang=ru`;

  const response = await fetch(url);

  if (!response.ok) {
    throw new Error('Город не найден');
  }

  return await response.json();
}
```
</details>

<details>
<summary>💡 Обработчик поиска</summary>

```javascript
document.getElementById('search-form').addEventListener('submit', async (e) => {
  e.preventDefault();

  const city = document.getElementById('city-input').value.trim();

  if (!city) return;

  showLoader();
  hideError();

  try {
    const data = await getWeatherByCity(city);
    displayWeather(data);
  } catch (error) {
    showError(error.message);
  } finally {
    hideLoader();
  }
});
```
</details>

---

## ✅ Чеклист

После изучения этой темы ты должен уметь:

- [ ] Объяснить разницу между синхронным и асинхронным кодом
- [ ] Использовать callback-функции
- [ ] Понимать проблему callback hell
- [ ] Создавать и использовать промисы
- [ ] Работать с `then`, `catch`, `finally`
- [ ] Использовать async/await для асинхронного кода
- [ ] Обрабатывать ошибки через try/catch
- [ ] Применять `Promise.all`, `Promise.race`, `Promise.allSettled`
- [ ] Делать HTTP-запросы с помощью fetch
- [ ] Работать с реальными API

---

## 🔗 Ресурсы

**Промисы:**
- [Промисы — Learn JavaScript](https://learn.javascript.ru/promise-basics)
- [Цепочка промисов — Learn JavaScript](https://learn.javascript.ru/promise-chaining)
- [MDN: Promise](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Global_Objects/Promise)

**Async/await:**
- [Async/await — Learn JavaScript](https://learn.javascript.ru/async-await)
- [MDN: async function](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/async_function)

**Fetch API:**
- [Fetch — Learn JavaScript](https://learn.javascript.ru/fetch)
- [MDN: Fetch API](https://developer.mozilla.org/ru/docs/Web/API/Fetch_API)

**Видео:**
- [Асинхронность в JavaScript — Владилен Минин](https://www.youtube.com/watch?v=SHiUyM_fFME)
- [Промисы и async/await — Владилен Минин](https://www.youtube.com/watch?v=vIZs5tH-HGQ)

---

⏱ **Время на изучение**: 8-10 часов

---

**Предыдущая тема:** [2.2.2 Прототипы и классы](./2.2.2-Прототипы-и-классы.md)

**Следующая тема:** [2.2.4 Работа с API (fetch)](./2.2.4-Работа-с-API.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](ссылка)
