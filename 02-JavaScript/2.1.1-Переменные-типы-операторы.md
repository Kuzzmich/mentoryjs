# 2.1.1 Переменные, типы данных, операторы

> **Цель:** Научиться объявлять переменные, работать с разными типами данных и использовать операторы. Заложить фундамент для всего дальнейшего изучения JavaScript.

---

## Переменные: let, const, var

**Переменная** — именованный контейнер для хранения данных.

```javascript
let temperature = 25;        // можно изменить
const city = "Москва";       // нельзя изменить
var oldStyle = "не используй"; // устаревший способ
```

### Сравнение let, const, var

| Особенность | `let` | `const` | `var` |
|-------------|-------|---------|-------|
| **Изменение значения** | ✅ Да | ❌ Нет | ✅ Да |
| **Область видимости** | Блочная `{}` | Блочная `{}` | Функциональная |
| **Hoisting** | Нет доступа до объявления | Нет доступа до объявления | Доступна как `undefined` |
| **Переобъявление** | ❌ Ошибка | ❌ Ошибка | ✅ Можно |
| **Использование** | ✅ Для изменяемых | ✅ Для констант | ❌ Не используй |

### Подробнее о ключевых различиях

#### Hoisting (поднятие)

Объявления переменных "поднимаются" в начало области видимости:

```javascript
// var — можно использовать до объявления (вернёт undefined)
console.log(temp);  // undefined
var temp = 25;

// let/const — ошибка при использовании до объявления
console.log(city);  // ❌ ReferenceError
let city = "Москва";
```

#### Область видимости

**Блочная** (`let`/`const`) — переменная живёт только внутри `{}`:

```javascript
if (true) {
  let temp = 20;
}
console.log(temp);  // ❌ ReferenceError

for (let i = 0; i < 3; i++) {}
console.log(i);  // ❌ ReferenceError
```

**Функциональная** (`var`) — переменная "утекает" из блоков:

```javascript
if (true) {
  var temp = 20;
}
console.log(temp);  // 20 ⚠️ утекла из if

for (var i = 0; i < 3; i++) {}
console.log(i);  // 3 ⚠️ утекла из цикла
```

#### Почему не использовать var

- Утечка переменных из блоков (баги в циклах и условиях)
- Можно случайно переобъявить переменную
- Непредсказуемое поведение из-за hoisting

> 🎯 **Правило:** Используй `const` по умолчанию, `let` — если нужно изменять. Забудь про `var`.

### Примеры

```javascript
// ✅ Хорошо
let temperature = 20;
temperature = 25;  // OK, значение изменилось

const API_KEY = "abc123";
// API_KEY = "new";  // ❌ Ошибка! const нельзя переназначить

// ⚠️ Важно: const запрещает переназначение, но НЕ запрещает изменение содержимого
const weather = { temp: 20 };
weather.temp = 25;  // ✅ OK, изменяем свойство объекта
// weather = {};     // ❌ Ошибка! нельзя переназначить саму переменную
```

### Правила именования

```javascript
// ✅ Хорошие имена
let userName = "Алексей";        // camelCase для переменных
const MAX_TEMPERATURE = 40;      // UPPER_CASE для констант
let _privateValue = 10;          // _ для приватных (договоренность)

// ❌ Плохие имена
let a = 5;                       // не понятно, что это
let user-name = "Иван";          // нельзя использовать дефис
let 1user = "Петр";              // не может начинаться с цифры
let class = "test";              // нельзя использовать ключевые слова
```

---

## Типы данных

JavaScript — **динамически типизированный** язык. Переменная может хранить значения любого типа.

### Примитивные типы

```javascript
// 1. String (строка)
let city = "Москва";
let description = 'Облачно';
let message = `Температура: ${temperature}°C`;  // шаблонная строка

// 2. Number (число)
let temperature = 25;
let humidity = 65.5;
let infinity = Infinity;
let notANumber = NaN;  // Not a Number

// 3. Boolean (логический)
let isRaining = true;
let isSunny = false;

// 4. Undefined (не определено)
let futureValue;
console.log(futureValue);  // undefined

// 5. Null (пустое значение)
let noData = null;  // явное отсутствие значения

// Разница: undefined = "значение не задано", null = "значение пустое специально"
let user;              // undefined — переменная объявлена, но не инициализирована
let response = null;   // null — явно указываем "данных нет"

// 6. Symbol (уникальный идентификатор) — редко используется
let id = Symbol('id');

// 7. BigInt (большие целые числа)
let bigNumber = 123456789012345678901234567890n;
```

### Объекты (ссылочный тип)

```javascript
// Object (объект)
let weather = {
  city: "Москва",
  temperature: 25,
  description: "Облачно"
};

// Array (массив — тоже объект)
let temperatures = [20, 22, 25, 23, 21];

// Function (функция — тоже объект)
function getTemperature() {
  return 25;
}
```

### Проверка типа: typeof

```javascript
console.log(typeof "Москва");        // "string"
console.log(typeof 25);              // "number"
console.log(typeof true);            // "boolean"
console.log(typeof undefined);       // "undefined"
console.log(typeof null);            // "object" ⚠️ это баг в JS!
console.log(typeof {});              // "object"
console.log(typeof []);              // "object" (массив — это объект)
console.log(typeof function() {});   // "function"
```

---

## Приведение типов

JavaScript автоматически преобразует типы в разных ситуациях.

### Неявное приведение (автоматическое)

```javascript
// Строка + число = строка
console.log("Температура: " + 25);     // "Температура: 25"
console.log("10" + 5);                 // "105" (строка!)

// Арифметика: строка → число
console.log("10" - 5);                 // 5 (число)
console.log("10" * 2);                 // 20 (число)
console.log("abc" - 5);                // NaN (не число)

// Boolean в условиях
if ("текст") {  // непустая строка = true
  console.log("Выполнится");
}

if (0) {  // 0 = false
  console.log("Не выполнится");
}
```

### Явное приведение

```javascript
// К строке
let num = 25;
String(num);          // "25"
num.toString();       // "25"
num + "";             // "25"

// К числу
let str = "25";
Number(str);          // 25
parseInt(str);        // 25 (только целая часть)
parseFloat("25.5");   // 25.5
+str;                 // 25 (унарный плюс)

// К boolean
Boolean(1);           // true
Boolean(0);           // false
Boolean("текст");     // true
Boolean("");          // false
!!value;              // двойное отрицание = приведение к boolean
```

### Ложные (falsy) значения

В JavaScript есть значения, которые в boolean контексте становятся `false`:

```javascript
// 6 ложных значений:
false
0
"" (пустая строка)
null
undefined
NaN

// Всё остальное = true
```

> 💡 **Важно:** Используй явное приведение типов (`Number()`, `String()`, `Boolean()`) вместо неявного. Это делает код понятнее и предсказуемее.

```javascript
// ❌ Плохо: неявное приведение (непонятно, что происходит)
let result = "10" - 5;

// ✅ Хорошо: явное приведение (понятно, что преобразуем строку в число)
let result = Number("10") - 5;
```

---

## Операторы

### Арифметические

```javascript
let a = 10;
let b = 3;

console.log(a + b);   // 13  (сложение)
console.log(a - b);   // 7   (вычитание)
console.log(a * b);   // 30  (умножение)
console.log(a / b);   // 3.333... (деление)
console.log(a % b);   // 1   (остаток от деления)
console.log(a ** b);  // 1000 (возведение в степень)

// Инкремент и декремент
let count = 0;
count++;  // count = count + 1
count--;  // count = count - 1

// Составное присваивание
let temp = 20;
temp += 5;   // temp = temp + 5  →  25
temp -= 3;   // temp = temp - 3  →  22
temp *= 2;   // temp = temp * 2  →  44
temp /= 4;   // temp = temp / 4  →  11
```

### Операторы сравнения

```javascript
let x = 5;

// Равенство (с приведением типов)
console.log(5 == "5");   // true  ⚠️ сравнивает значения
console.log(5 != "6");   // true

// Строгое равенство (без приведения типов)
console.log(5 === "5");  // false ✅ сравнивает значение И тип
console.log(5 !== "5");  // true

// Сравнение
console.log(5 > 3);      // true
console.log(5 < 3);      // false
console.log(5 >= 5);     // true
console.log(5 <= 4);     // false
```

> 💡 **Всегда используй `===` и `!==` вместо `==` и `!=`** — это избавит от неожиданных багов.

### Логические операторы

```javascript
let isWarm = true;
let isSunny = false;

// И (AND) — все условия должны быть true
console.log(isWarm && isSunny);  // false

// ИЛИ (OR) — хотя бы одно условие true
console.log(isWarm || isSunny);  // true

// НЕ (NOT) — инверсия
console.log(!isWarm);            // false
console.log(!isSunny);           // true

// Сложные выражения
let temp = 25;
let isComfortable = temp >= 20 && temp <= 26;
console.log(isComfortable);      // true
```

> 💡 **Для условного выбора значений** (например, "Тепло" или "Холодно" в зависимости от температуры) используются условные операторы — изучим их в следующей теме.

---

## 🔧 Практика 2.1.1.1: Создаём данные для погодной карточки

Начнём создавать наше погодное приложение! Пока работаем со статичными данными.

**Шаг 1: Создай структуру проекта**

1. Создай папку `weather-app` на компьютере
2. Внутри создай два файла:
   - `index.html`
   - `script.js`

**Шаг 2: Напиши HTML и подключи JS**

В `index.html`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погодное приложение</title>
</head>
<body>
  <h1>Погода</h1>

  <!-- Подключаем JS файл в конце body -->
  <script src="script.js"></script>
</body>
</html>
```

> 💡 Скрипт подключается в конце `<body>`, чтобы HTML загрузился раньше JavaScript.

**Шаг 3: Напиши JavaScript**

В `script.js` объяви переменные для погоды:

```javascript
// Данные о погоде (статичные)
const city = "Москва";
let temperature = 15;
let feelsLike = 13;
const humidity = 65;
let description = "Облачно с прояснениями";
let windSpeed = 5.2;
let pressure = 755;

// Вывод в консоль
console.log(`Город: ${city}`);
console.log(`Температура: ${temperature}°C`);
console.log(`Ощущается как: ${feelsLike}°C`);
console.log(`Влажность: ${humidity}%`);
console.log(`Описание: ${description}`);
console.log(`Ветер: ${windSpeed} м/с`);
console.log(`Давление: ${pressure} мм рт. ст.`);
```

3. Проверь типы всех переменных через `typeof` и выведи в консоль
4. Создай переменную `isWarm`, которая будет содержать результат сравнения `temperature > 20`
5. Выведи `isWarm` в консоль

**Проверь себя:**
- Открой консоль (F12) — должна вывестись информация о погоде
- `isWarm` должна быть `false` (так как 15 < 20)
- Попробуй изменить `temperature` на `25` и перезагрузи — `isWarm` должна стать `true`

---

## 🔧 Практика 2.1.1.2: Работа с типами

**Задание:**

Представь, что данные с API пришли в виде строк. Преобразуй их в нужные типы:

```javascript
// Данные "с сервера" (пока симулируем строками)
let tempFromAPI = "18";
let humidityFromAPI = "72";
let windFromAPI = "3.5";

// TODO: Преобразуй строки в числа
// 1. Создай переменную temp из tempFromAPI (используй Number())
// 2. Создай переменную humidity из humidityFromAPI (используй parseInt())
// 3. Создай переменную wind из windFromAPI (используй parseFloat())

// TODO: Выведи результаты с типами
// console.log(temp, typeof temp);
// console.log(humidity, typeof humidity);
// console.log(wind, typeof wind);

// TODO: Создай переменную weatherSummary (строка):
// Формат: "Температура 18°C, влажность 72%, ветер 3.5 м/с"
// Используй шаблонную строку
```

**Ожидаемый результат в консоли:**
```
18 "number"
72 "number"
3.5 "number"
Температура 18°C, влажность 72%, ветер 3.5 м/с
```

---

## 🔧 Практика 2.1.1.3: Операторы в действии

**Задание:**

Рассчитай различные показатели погоды:

```javascript
let currentTemp = 15;
let minTemp = 12;
let maxTemp = 18;
let humidity = 75;
let description = "Облачно";

// TODO 1: Рассчитай среднюю температуру (minTemp + maxTemp) / 2
// Выведи: "Средняя температура: X°C"

// TODO 2: Рассчитай разницу между максимальной и минимальной температурой
// Выведи: "Разница температур: X°C"

// TODO 3: Проверь, комфортная ли температура (от 18 до 25)
// Создай переменную isComfortable (используй && для проверки диапазона)
// Выведи: "Комфортная температура: true/false"

// TODO 4: Определи, нужен ли зонт
// Условие: влажность > 80 ИЛИ в описании есть слово "дождь"
// Создай переменную needUmbrella (используй || и .includes())
// Выведи: "Нужен зонт: true/false"

// TODO 5: Проверь, холодно ли
// Создай переменную isCold: true если currentTemp < 15
// Выведи: "Холодно: true/false"
```

---

## 📝 Итоговое задание

**Создай мини-систему для анализа погодных данных:**

```javascript
// Данные
const cityName = "Санкт-Петербург";
let temp = 10;
let humidity = 85;
let windSpeed = 8;
let pressure = 745;
let isRaining = false;

// TODO 1: Создай переменную tempInFahrenheit
// Формула: F = C * 9/5 + 32

// TODO 2: Проверь, "плохая" ли погода
// Создай переменную isBadWeather (используй || для объединения условий):
// - холодно (temp < 10)
// - сильный ветер (windSpeed > 10)
// - идет дождь (isRaining === true)

// TODO 3: Создай переменную fullReport (многострочная строка)
// Формат:
// Погода в городе Санкт-Петербург:
// Температура: 10°C (50°F)
// Влажность: 85%
// Ветер: 8 м/с
// Давление: 745 мм рт. ст.
// Плохая погода: false
// Используй шаблонную строку

// TODO 4: Выведи fullReport в консоль

// TODO 5: Измени температуру на 15°C
// Пересчитай isBadWeather
// Выведи в консоль: "После изменения isBadWeather = " + значение переменной
```

**Проверь себя:**
- При temp = 10: isBadWeather должна быть `false` (10 не меньше 10, ветер не больше 10, не идёт дождь)
- При temp = 15: isBadWeather должна остаться `false`

---

## ✅ Чеклист раздела

После изучения темы ты должен:

- [ ] Понимать разницу между `let`, `const` и `var`
- [ ] Знать, когда использовать `let`, а когда `const`
- [ ] Перечислить все примитивные типы данных в JavaScript
- [ ] Использовать `typeof` для проверки типа
- [ ] Понимать разницу между `==` и `===`
- [ ] Применять арифметические операторы
- [ ] Использовать логические операторы (`&&`, `||`, `!`)
- [ ] Писать тернарные операторы
- [ ] Работать с шаблонными строками (template literals)
- [ ] Преобразовывать типы (явно и понимать неявное преобразование)

---

## 🔗 Ресурсы

- 📖 [Learn JavaScript: Переменные](https://learn.javascript.ru/variables)
- 📖 [Learn JavaScript: Типы данных](https://learn.javascript.ru/types)
- 📖 [MDN: let](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/let)
- 📖 [MDN: const](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Statements/const)
- 📖 [MDN: Операторы](https://developer.mozilla.org/ru/docs/Web/JavaScript/Guide/Expressions_and_Operators)

---

⏱ **Время на изучение**: 3-4 часа

---

**Следующая тема:** [2.1.2 Массивы и объекты](./2.1.2-Массивы-и-объекты.md)
