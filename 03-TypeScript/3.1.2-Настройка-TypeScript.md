# 3.1.2 Настройка TypeScript в проектах

Настройка TypeScript — это не просто установка пакета. Нужно правильно сконфигурировать компилятор, настроить редактор кода и подключить типы для библиотек. Разберём всё по шагам.

---

## 🎯 Что ты узнаешь

- Как установить TypeScript локально и глобально
- Что такое `tsconfig.json` и как его настроить
- Как компилировать TypeScript в JavaScript
- Режимы работы компилятора (обычный, watch, build)
- Интеграция TypeScript с VS Code
- Интеграция TypeScript с WebStorm
- Как подключать типы для библиотек (@types)
- Структура проекта с TypeScript
- Как настроить TypeScript в реальном проекте

---

## 📖 Установка TypeScript

### Локальная установка (рекомендуется)

Устанавливаем TypeScript как dev-зависимость проекта:

```bash
# Инициализируем npm проект (если ещё не сделано)
npm init -y

# Устанавливаем TypeScript
npm install --save-dev typescript
```

**Почему локально?**
- Каждый проект может использовать свою версию TypeScript
- Другие разработчики получат ту же версию через `npm install`
- Не засоряет глобальное окружение

### Глобальная установка (опционально)

Для быстрых экспериментов или изучения:

```bash
npm install -g typescript
```

После этого команда `tsc` будет доступна глобально.

### Проверка установки

```bash
# Если установлен локально
npx tsc --version

# Если установлен глобально
tsc --version

# Вывод: Version 5.x.x
```

---

## 📖 Создание tsconfig.json

`tsconfig.json` — конфигурационный файл TypeScript. Определяет, как компилятор будет работать.

### Создание конфига

```bash
# Создать tsconfig.json с базовыми настройками
npx tsc --init
```

Будет создан файл с множеством опций (большинство закомментировано).

### Минимальная конфигурация

Для старта можно использовать такой конфиг:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ES2020",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

**Что означают опции:**

| Опция | Что делает |
|-------|------------|
| `target` | В какую версию JS компилировать (ES5, ES2015, ES2020, etc.) |
| `module` | Система модулей (CommonJS, ES2020, ESNext) |
| `outDir` | Папка для скомпилированных .js файлов |
| `rootDir` | Папка с исходными .ts файлами |
| `strict` | Включить все строгие проверки (пока false для обучения) |
| `esModuleInterop` | Совместимость с CommonJS модулями |
| `skipLibCheck` | Не проверять типы в node_modules (ускоряет компиляцию) |
| `include` | Какие файлы компилировать |
| `exclude` | Какие файлы игнорировать |

---

## 📖 Первая компиляция

### Структура проекта

```
my-project/
├── src/
│   └── index.ts       # Исходный TypeScript
├── dist/              # Скомпилированный JavaScript (создастся автоматически)
├── tsconfig.json
└── package.json
```

### Создай первый TypeScript файл

```typescript
// src/index.ts

const greeting: string = "Hello, TypeScript!";
const year: number = 2024;

function greet(name: string): string {
  return `${greeting} Меня зовут ${name}. Год: ${year}`;
}

console.log(greet("Алексей"));
```

### Компиляция

```bash
# Компиляция всех файлов согласно tsconfig.json
npx tsc

# Проверяем результат
ls dist/
# Увидишь: index.js
```

### Посмотри на скомпилированный код

```javascript
// dist/index.js

const greeting = "Hello, TypeScript!";
const year = 2024;

function greet(name) {
  return `${greeting} Меня зовут ${name}. Год: ${year}`;
}

console.log(greet("Алексей"));
```

**Обрати внимание:** Типы исчезли! TypeScript компилируется в обычный JavaScript.

### Запуск скомпилированного кода

```bash
node dist/index.js
# Вывод: Hello, TypeScript! Меня зовут Алексей. Год: 2024
```

---

## 📖 Режимы компиляции

### Watch режим

Автоматическая перекомпиляция при изменении файлов:

```bash
npx tsc --watch
# или
npx tsc -w
```

Теперь при каждом сохранении `.ts` файла TypeScript будет автоматически пересобирать проект.

### Компиляция конкретного файла

```bash
# Игнорирует tsconfig.json
npx tsc src/index.ts --outDir dist
```

### Только проверка типов (без компиляции)

```bash
npx tsc --noEmit
```

Полезно для CI/CD — проверить ошибки типов без генерации файлов.

---

## 📖 npm scripts

Добавь удобные команды в `package.json`:

```json
{
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit",
    "dev": "tsc --watch"
  }
}
```

Теперь можно использовать:

```bash
npm run build   # Компиляция
npm run watch   # Watch режим
npm run check   # Проверка типов
npm run dev     # Разработка с авто-компиляцией
```

---

## 📖 Интеграция с VS Code

VS Code имеет встроенную поддержку TypeScript.

### Автоматические настройки

При наличии `tsconfig.json` VS Code автоматически:
- Показывает ошибки типов прямо в редакторе
- Предоставляет автодополнение
- Позволяет переходить к определениям (`F12`)
- Показывает подсказки при наведении

### Настройки VS Code для TypeScript

Создай `.vscode/settings.json` в корне проекта:

```json
{
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true,
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.organizeImports": true
  }
}
```

**Что это даёт:**
- Использует локальную версию TypeScript
- Автоформатирование при сохранении
- Автоматическая организация импортов

### Полезные расширения

Установи в VS Code:
- **ESLint** — линтер для JavaScript/TypeScript
- **Prettier** — форматирование кода
- **Error Lens** — показывает ошибки прямо в строке кода

---

## 📖 Интеграция с WebStorm

WebStorm (JetBrains) имеет первоклассную поддержку TypeScript из коробки.

### Автоматические настройки

При открытии проекта с `tsconfig.json` WebStorm автоматически:
- Подсвечивает ошибки типов в реальном времени
- Предоставляет интеллектуальное автодополнение
- Позволяет переходить к определениям (`Ctrl/Cmd + B`)
- Показывает документацию при наведении (`Ctrl/Cmd + Q`)
- Поддерживает безопасный рефакторинг (`Shift + F6` для переименования)

### Настройки TypeScript в WebStorm

**Выбор версии TypeScript:**

1. Открой `Settings` → `Languages & Frameworks` → `TypeScript`
2. Выбери **TypeScript version**: `Use TypeScript from project's node_modules`
3. WebStorm будет использовать версию из твоего `package.json`

**Включение TypeScript Language Service:**

По умолчанию включён. Проверь в:
- `Settings` → `Languages & Frameworks` → `TypeScript`
- Галочка **TypeScript Language Service** должна быть включена

### Полезные возможности WebStorm

**Автоматические импорты:**
- Начни писать имя типа или функции
- WebStorm предложит автоматически добавить импорт
- Настройка: `Settings` → `Editor` → `General` → `Auto Import`

**Форматирование кода:**
- `Ctrl/Cmd + Alt + L` — форматирование файла
- `Settings` → `Editor` → `Code Style` → `TypeScript` — настройка стиля

**Быстрые исправления:**
- `Alt + Enter` на ошибке — WebStorm предложит варианты исправления
- Автоматическое добавление типов, импортов, исправление ошибок

### Рекомендуемые плагины

WebStorm уже включает большинство необходимых инструментов, но можно добавить:
- **Database Tools and SQL** (встроенный) — если работаешь с БД
- **Prettier** — дополнительное форматирование (уже встроено в новых версиях)
- **GitToolBox** — улучшенная интеграция с Git

---

## 📖 Типы для библиотек

Многие JavaScript библиотеки не написаны на TypeScript. Для них нужны **файлы деклараций типов** (`.d.ts`).

### DefinitelyTyped

[DefinitelyTyped](https://github.com/DefinitelyTyped/DefinitelyTyped) — репозиторий с типами для тысяч библиотек.

Типы публикуются в npm под `@types/`:

```bash
# Пример: добавляем типы для Node.js
npm install --save-dev @types/node
```

### Автоматическая установка типов

Некоторые библиотеки уже включают типы:

```bash
# Библиотека с встроенными типами
npm install axios
# Типы уже есть, ничего дополнительно устанавливать не нужно
```

Другие требуют отдельной установки:

```bash
# Библиотека без типов
npm install lodash

# Добавляем типы отдельно
npm install --save-dev @types/lodash
```

### Проверка наличия типов

Набери в коде:

```typescript
import axios from 'axios';

axios. // ← Если видишь автодополнение, типы есть
```

Если типов нет — VS Code предложит установить `@types/...`.

---

## 📖 Структура проекта с TypeScript

### Рекомендуемая структура

```
weather-app/
├── src/                    # Исходники TypeScript
│   ├── modules/
│   │   ├── api/
│   │   │   └── weather.ts
│   │   ├── ui/
│   │   │   └── weatherCard.ts
│   │   └── utils/
│   │       └── format.ts
│   └── app.ts             # Главный файл
├── dist/                   # Скомпилированный JS (в .gitignore)
├── public/                 # Статические файлы (HTML, CSS, изображения)
│   ├── index.html
│   └── styles/
│       └── main.css
├── tsconfig.json
├── package.json
└── .gitignore
```

### .gitignore

Добавь в `.gitignore`:

```gitignore
# Скомпилированные файлы
dist/

# Зависимости
node_modules/

# IDE
.vscode/
.idea/

# OS
.DS_Store
```

### HTML для модулей

Обрати внимание: браузер **не понимает TypeScript**. Нужно подключать **скомпилированный JavaScript**:

```html
<!-- index.html (в корне проекта) -->
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Weather App</title>
  <link rel="stylesheet" href="public/styles/main.css">
</head>
<body>
  <div id="app"></div>

  <!-- Подключаем скомпилированный JS -->
  <!-- После сборки HTML копируется в dist/ рядом с app.js -->
  <script type="module" src="./app.js"></script>
</body>
</html>
```

> 💡 При сборке `index.html` копируется в `dist/` рядом с `app.js`, поэтому используем относительный путь `./app.js`.

---

## 🔧 Практика 3.1.2.1: Настройка TypeScript в новом проекте

Создадим простой проект с нуля, чтобы разобраться с настройкой.

**Шаг 1: Создай проект**

```bash
mkdir ts-hello
cd ts-hello
npm init -y
npm install --save-dev typescript
```

**Шаг 2: Создай tsconfig.json**

```bash
npx tsc --init
```

Отредактируй `tsconfig.json` (найди и раскомментируй/измени эти строки):

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ES2020",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": false
  }
}
```

**Шаг 3: Создай src/index.ts**

```typescript
// src/index.ts

interface User {
  name: string;
  age: number;
}

function greetUser(user: User): string {
  return `Привет, ${user.name}! Тебе ${user.age} лет.`;
}

const me: User = {
  name: "Алексей",
  age: 25
};

console.log(greetUser(me));
```

**Шаг 4: Скомпилируй и запусти**

```bash
# Компиляция
npx tsc

# Проверь dist/index.js
cat dist/index.js

# Запуск
node dist/index.js
```

**Задание:**
1. Попробуй намеренно сделать ошибку: `age: "25"` (строка вместо числа)
2. Запусти `npx tsc` — увидишь ошибку компиляции
3. Исправь ошибку
4. Добавь watch режим: `npx tsc --watch`
5. Измени код — убедись, что файл пересобирается автоматически

---

## 🔧 Практика 3.1.2.2: Настройка TypeScript в погодном приложении

Теперь настроим TypeScript в **погодном приложении из задания 2.2.6** (модульная структура с темами).

### Исходная структура (из 2.2.6)

```
weather-app/
├── index.html
├── app.js
├── modules/
│   ├── api/
│   │   ├── weather.js
│   │   └── geocoding.js
│   ├── ui/
│   │   ├── weatherCard.js
│   │   ├── forecast.js
│   │   └── favorites.js
│   ├── utils/
│   │   ├── format.js
│   │   └── storage.js
│   └── index.js
└── themes/
    ├── light.js
    └── dark.js
```

### Шаг 1: Инициализация TypeScript

Перейди в папку проекта:

```bash
cd weather-app

# Инициализируй npm (если не сделано)
npm init -y

# Установи TypeScript
npm install --save-dev typescript

# Создай tsconfig.json
npx tsc --init
```

### Шаг 2: Настройка tsconfig.json

Замени содержимое `tsconfig.json`:

```json
{
  "compilerOptions": {
    // Целевая версия JavaScript
    "target": "ES2020",

    // Система модулей
    "module": "ES2020",

    // Папки
    "outDir": "./dist",
    "rootDir": "./src",

    // Строгость (пока выключаем для постепенной миграции)
    "strict": false,

    // Дополнительные проверки
    "noImplicitAny": false,
    "strictNullChecks": false,

    // Совместимость
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    // Оптимизация
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    // Source maps для отладки
    "sourceMap": true,

    // Разрешение модулей
    "moduleResolution": "node"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

### Шаг 3: Реорганизация структуры

Создадим правильную структуру:

```bash
# Создай папку src
mkdir src

# Перемести модули в src
mv modules src/
mv themes src/
mv app.js src/app.ts

# Переименуй все .js в .ts
find src -name "*.js" -exec sh -c 'mv "$1" "${1%.js}.ts"' _ {} \;
```

Итоговая структура:

```
weather-app/
├── index.html              # HTML в корне проекта
├── public/                 # Статические файлы (CSS, images)
│   └── styles/
│       └── main.css
├── src/                    # TypeScript исходники
│   ├── modules/
│   │   ├── api/
│   │   │   ├── weather.ts
│   │   │   └── geocoding.ts
│   │   ├── ui/
│   │   │   ├── weatherCard.ts
│   │   │   ├── forecast.ts
│   │   │   └── favorites.ts
│   │   ├── utils/
│   │   │   ├── format.ts
│   │   │   └── storage.ts
│   │   └── index.ts
│   ├── themes/
│   │   ├── light.ts
│   │   └── dark.ts
│   └── app.ts
├── dist/                   # Скомпилированный JS (создастся при tsc)
├── tsconfig.json
├── package.json
└── .gitignore
```

### Шаг 4: Обновление package.json

Добавь скрипты и установи `live-server`:

```bash
npm install --save-dev live-server
```

Обнови `package.json`:

```json
{
  "scripts": {
    "build": "tsc && npm run copy:html",
    "copy:html": "cp index.html dist/index.html",
    "watch": "tsc --watch",
    "serve": "live-server dist",
    "dev": "npm run build && npm run serve",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "live-server": "^1.2.2"
  }
}
```

**Что делают скрипты:**
- `build` — компиляция TS → JS + копирование HTML в `dist/`
- `copy:html` — копирует HTML из корня в `dist/`
- `serve` — запускает сервер из папки `dist/`
- `dev` — сборка + запуск сервера

### Шаг 5: Создание .gitignore

```gitignore
# Скомпилированные файлы
dist/

# Зависимости
node_modules/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Logs
*.log
```

### Шаг 6: Обновление HTML

Измени `index.html` (в корне проекта):

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погода - TypeScript</title>
  <link rel="stylesheet" href="public/styles/main.css">
</head>
<body>
  <div class="weather-card">
    <div class="header">
      <h1>🌤️ Погода</h1>
      <button class="theme-toggle" id="themeToggle">🌙</button>
    </div>
    <div class="search">
      <input type="text" id="cityInput" placeholder="Введите город...">
      <button id="searchBtn">Поиск</button>
    </div>
    <div id="weatherInfo" class="weather-info"></div>
  </div>

  <!-- Подключаем скомпилированный TypeScript -->
  <!-- ВАЖНО: путь ./app.js, т.к. HTML будет скопирован в dist/ рядом с app.js -->
  <script type="module" src="./app.js"></script>
</body>
</html>
```

> 💡 **Важно:** Путь `./app.js` правильный, потому что при сборке `index.html` копируется в `dist/` рядом с `app.js`. CSS загружается из `public/styles/` (относительно корня проекта).

---

### Шаг 7: ⚠️ КРИТИЧЕСКИ ВАЖНО: Добавь расширения .js в импортах

**Проблема:** ES Modules в браузере требуют явного указания расширения файла.

В TypeScript файлах **всегда пиши `.js`** в импортах (даже если файлы `.ts`):

**❌ Не работает в браузере:**
```typescript
// src/app.ts
import { fetchWeather } from './modules/api';  // ❌ Браузер не найдёт
```

**✅ Работает:**
```typescript
// src/app.ts
import { fetchWeather } from './modules/api.js';  // ✅ С расширением
```

**Почему `.js`, а не `.ts`?**
- TypeScript **не меняет** пути импортов при компиляции
- Браузер работает с `.js` файлами, а не `.ts`
- TypeScript достаточно умён: ищет `api.ts` для проверки типов, но в итоговый `.js` пишет `api.js`

**Действие:**

Открой **все** `.ts` файлы с импортами и добавь расширение `.js`:

```typescript
// src/app.ts
import { fetchWeather } from './modules/api.js';
import { renderWeather } from './modules/ui.js';

// src/modules/ui/weatherCard.ts
import { formatTemp } from '../utils/format.js';

// src/modules/utils/storage.ts
import { encrypt } from './crypto.js';
```

> 🎯 **Правило:** В TypeScript пиши расширение `.js` для всех относительных импортов (`./` или `../`).

---

### Шаг 8: Первая компиляция

```bash
# Скомпилируй проект + скопируй HTML
npm run build

# Проверь структуру dist/
ls dist/
# Должны быть: index.html, app.js, modules/, themes/
```

**Что произошло:**
1. TypeScript скомпилировал `src/` → `dist/` (`.ts` → `.js`)
2. HTML скопирован из корня → `dist/`
3. Теперь всё готово для браузера в одной папке!

---

### Шаг 9: Запуск приложения

```bash
# Запусти dev режим (сборка + сервер)
npm run dev
```

Открой браузер на `http://127.0.0.1:8080` — приложение должно работать!

**Структура dist/ после сборки:**
```
dist/
├── index.html          ← из корня проекта
├── app.js             ← из src/app.ts
├── app.js.map         ← source map для отладки
├── modules/
│   ├── api/
│   │   ├── weather.js
│   │   └── geocoding.js
│   ├── ui/
│   │   ├── weatherCard.js
│   │   ├── forecast.js
│   │   └── favorites.js
│   └── utils/
│       ├── format.js
│       └── storage.js
└── themes/
    ├── light.js
    └── dark.js
```

> 💡 **Важно:** Сервер запускается из `dist/`, а не `public/`, потому что браузеру нужен скомпилированный JavaScript.

---

### Шаг 10: Разработка с watch режимом

Для удобной разработки запускай в **двух терминалах**:

**Терминал 1 — автокомпиляция:**
```bash
npm run watch
```

**Терминал 2 — сервер:**
```bash
npm run serve
```

Теперь при изменении `.ts` файлов код будет автоматически перекомпилироваться.

**Проверь:**
1. Открой `src/modules/utils/format.ts`
2. Измени функцию `formatTemp` — например, добавь восклицательный знак
3. Сохрани файл
4. Проверь терминал с `tsc --watch` — увидишь сообщение о компиляции
5. Обнови браузер — изменения применились!

> ⚠️ **Внимание:** Если меняешь HTML (`index.html`), нужно вручную запустить `npm run copy:html` или полную пересборку `npm run build`.

---

### ❌ Частые проблемы и решения

#### Проблема 1: 404 ошибка на модули

**Симптом:** В консоли браузера:
```
GET http://127.0.0.1:8080/modules/api - 404 (Not Found)
```

**Причина:** Забыли добавить расширение `.js` в импортах.

**Решение:**
```typescript
// ❌ Неправильно
import { fetchWeather } from './modules/api';

// ✅ Правильно
import { fetchWeather } from './modules/api.js';
```

---

#### Проблема 2: 404 на app.js

**Симптом:** В консоли браузера:
```
GET http://127.0.0.1:8080/app.js - 404 (Not Found)
```

**Причина:** Сервер запущен из `public/` вместо `dist/`.

**Решение:** Проверь `package.json`:
```json
{
  "scripts": {
    "serve": "live-server dist"  // ✅ Должно быть dist, не public
  }
}
```

---

#### Проблема 3: Изменения TypeScript не применяются

**Симптом:** Меняю `.ts` файл, но в браузере ничего не меняется.

**Причина:** Забыл запустить компиляцию.

**Решение:**
```bash
# Запусти watch режим
npm run watch

# Или пересобери вручную
npm run build
```

---

#### Проблема 4: Старая версия файла в браузере

**Симптом:** Вижу старый код, хотя пересобрал проект.

**Решение:** Очисти кэш браузера:
- `Ctrl + Shift + R` (Windows/Linux)
- `Cmd + Shift + R` (macOS)

---

## 🔧 Практика 3.1.2.3: Подключение типов для DOM

Добавим типы для работы с DOM API.

**Установка типов:**

```bash
npm install --save-dev @types/node
```

Теперь TypeScript будет понимать DOM API.

**Проверь автодополнение:**

Открой `src/app.ts` и попробуй:

```typescript
const button = document.getElementById('searchBtn');

button. // ← Поставь точку и увидишь автодополнение!
```

VS Code покажет все методы: `addEventListener`, `click`, `classList` и т.д.

**Пример с типами:**

```typescript
// src/app.ts

// TypeScript знает, что это HTMLInputElement
const cityInput = document.getElementById('cityInput') as HTMLInputElement;

// Теперь доступно свойство .value
const city = cityInput.value;

// TypeScript знает, что это HTMLButtonElement
const searchBtn = document.getElementById('searchBtn') as HTMLButtonElement;

searchBtn.addEventListener('click', () => {
  console.log('Клик!');
});
```

---

## 📝 Итоговое задание 3.1.2

**Задача:** Полностью настрой TypeScript в погодном приложении из 2.2.6 и убедись, что всё работает.

### Требования:

1. ✅ Установлен TypeScript локально
2. ✅ Создан `tsconfig.json` с правильными настройками
3. ✅ Все `.js` файлы переименованы в `.ts`
4. ✅ Структура проекта организована (`src/`, `dist/`, `public/`)
5. ✅ Проект успешно компилируется без ошибок
6. ✅ Настроены npm scripts (`build`, `watch`, `serve`)
7. ✅ Приложение работает в браузере
8. ✅ Watch режим работает корректно
9. ✅ Создан `.gitignore`

### Проверочный чеклист:

Выполни эти команды и убедись, что всё работает:

```bash
# 1. Компиляция проходит без ошибок
npm run build
# ✅ Должна создаться папка dist/ с .js файлами

# 2. Watch режим работает
npm run watch
# ✅ Терминал показывает "Watching for file changes"

# 3. Сервер запускается
npm run serve
# ✅ Браузер открывается на localhost:8080

# 4. Приложение работает
# ✅ Можешь искать погоду по городу
# ✅ Переключение темы работает
# ✅ Нет ошибок в консоли браузера
```

### Дополнительно (опционально):

1. Установи ESLint для TypeScript:
```bash
npm install --save-dev @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint
```

2. Создай `.eslintrc.json`:
```json
{
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended"
  ]
}
```

3. Добавь в `package.json`:
```json
{
  "scripts": {
    "lint": "eslint src/**/*.ts"
  }
}
```

---

## ✅ Чеклист раздела

После изучения темы ты должен уметь:

- [ ] Устанавливать TypeScript локально и глобально
- [ ] Создавать и настраивать `tsconfig.json`
- [ ] Понимать основные опции компилятора
- [ ] Компилировать TypeScript в JavaScript
- [ ] Использовать watch режим для разработки
- [ ] Настраивать VS Code для работы с TypeScript
- [ ] Устанавливать типы для библиотек через `@types/`
- [ ] Организовывать структуру проекта с TypeScript
- [ ] Настраивать npm scripts для удобной разработки

---

## 🔗 Ресурсы

**Документация:**
- [TypeScript Handbook: tsconfig.json](https://www.typescriptlang.org/docs/handbook/tsconfig-json.html) — подробно про конфиг
- [TypeScript Compiler Options](https://www.typescriptlang.org/tsconfig) — все опции компилятора
- [DefinitelyTyped](https://github.com/DefinitelyTyped/DefinitelyTyped) — типы для библиотек

**Статьи:**
- [TypeScript Project Setup](https://khalilstemmler.com/blogs/typescript/node-starter-project/) — настройка проекта
- [TypeScript + VS Code](https://code.visualstudio.com/docs/languages/typescript) — интеграция с VS Code

**Видео:**
- [Настройка TypeScript — Владилен Минин](https://www.youtube.com/watch?v=7NU6K4170As) — на русском

**Инструменты:**
- [TypeScript Playground](https://www.typescriptlang.org/play) — онлайн компилятор
- [TSConfig Bases](https://github.com/tsconfig/bases) — готовые tsconfig.json для разных сред

---

⏱ **Время на изучение**: 2-3 часа

---

**Предыдущая тема:** [3.1.1 Зачем TypeScript](./3.1.1-Зачем-TypeScript.md)

**Следующая тема:** [3.1.3 Базовые типы](./3.1.3-Базовые-типы.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](https://xmentors.ru/dev)
