# 1.1 Как работает компьютер и интернет

> **Цель:** Понять базовые концепции, чтобы осознанно писать код, а не копировать магические команды.

---

## Что такое код

**Код** — текстовые инструкции для компьютера. Компьютер понимает только машинный код (нули и единицы), поэтому нужны трансляторы:

| Тип | Как работает | Примеры |
|-----|-------------|---------|
| **Компилятор** | Переводит весь код в машинный ДО запуска | C, Go, Rust |
| **Интерпретатор** | Переводит и выполняет построчно | Python, Ruby |
| **JIT (Just-In-Time)** | Гибрид: интерпретирует + компилирует "горячие" участки | JavaScript (V8), Java |

**JavaScript** использует JIT-компиляцию в движке V8 (Chrome, Node.js).

### 🔧 Практика 1.1.1
Открой консоль браузера (`F12` → Console) и выполни:
```javascript
console.log("Привет, мир!");
2 + 2
```
Ты только что дал инструкции интерпретатору JavaScript.

---

## Клиент и сервер

```
┌──────────┐         запрос (request)        ┌──────────┐
│  Клиент  │  ─────────────────────────────▶ │  Сервер  │
│ (браузер)│  ◀─────────────────────────────  │ (Node.js)│
└──────────┘         ответ (response)        └──────────┘
```

- **Клиент** — программа, которая запрашивает данные (браузер, мобильное приложение)
- **Сервер** — программа, которая отвечает на запросы (твой будущий Node.js код)

### 🔧 Практика 1.1.2
1. Открой DevTools → вкладка **Network**
2. Перейди на любой сайт
3. Найди первый запрос (обычно документ HTML)
4. Изучи:
   - **Request URL** — куда пошёл запрос
   - **Request Method** — GET, POST и т.д.
   - **Status Code** — 200, 404, 500...
   - **Response** — что вернул сервер

---

## HTTP/HTTPS

**HTTP** (HyperText Transfer Protocol) — протокол общения клиента и сервера.

### Структура запроса

**GET-запрос** (получение данных):
```
GET /api/users HTTP/1.1        ← метод, путь, версия
Host: example.com              ← заголовки
Authorization: Bearer token123
                               ← пустая строка
                               ← тело пустое (GET не передаёт данные)
```

**POST-запрос** (отправка данных):
```
POST /api/users HTTP/1.1       ← метод, путь, версия
Host: example.com              ← заголовки
Content-Type: application/json
Authorization: Bearer token123
                               ← пустая строка
{"name": "Алексей", "role": "developer"}  ← тело запроса (JSON)
```

### Основные методы
| Метод | Действие | Пример |
|-------|----------|--------|
| GET | Получить данные | Загрузить список товаров |
| POST | Создать | Добавить товар |
| PUT/PATCH | Обновить | Изменить цену |
| DELETE | Удалить | Удалить товар |

### Коды ответов
| Диапазон | Значение | Частые примеры |
|----------|----------|----------------|
| 2xx | Успех | 200 OK, 201 Created |
| 3xx | Редирект | 301, 304 |
| 4xx | Ошибка клиента | 400 Bad Request, 404 Not Found |
| 5xx | Ошибка сервера | 500, 503 |

**HTTPS** = HTTP + шифрование (TLS). Данные защищены от перехвата.

### 🔧 Практика 1.1.3
Выполни в терминале (или используй [httpie.io/app](https://httpie.io/app)):

**Linux / macOS:**
```bash
curl -v https://jsonplaceholder.typicode.com/posts/1
```

**Windows (PowerShell):**
```powershell
Invoke-WebRequest -Uri "https://jsonplaceholder.typicode.com/posts/1" -Method GET | Select-Object -ExpandProperty Content
# Или установи curl через winget:
winget install curl.curl
# После этого команда curl будет работать как в Linux
```

Изучи:
- Заголовки запроса и ответа
- Код ответа
- Тело ответа (JSON)

---

## DNS: как браузер находит сервер

```
Ты вводишь: google.com
     │
     ▼
┌─────────────┐    "Какой IP у google.com?"    ┌─────────────┐
│   Браузер   │  ────────────────────────────▶ │ DNS-сервер  │
│             │  ◀────────────────────────────  │             │
└─────────────┘       "142.250.74.206"         └─────────────┘
     │
     ▼
Браузер подключается к 142.250.74.206
```

**DNS** (Domain Name System) — "телефонная книга" интернета. Переводит домены в IP-адреса.

### 🔧 Практика 1.1.4

**Linux / macOS:**
```bash
# Узнай IP-адрес сайта
nslookup google.com

# Более подробная информация
dig google.com
```

**Windows (PowerShell / CMD):**
```powershell
# Узнай IP-адрес сайта
nslookup google.com

# Альтернатива через ping
ping google.com

# Через PowerShell
Resolve-DnsName google.com
```

---

## Домен, хостинг, сервер

| Термин | Что это | Аналогия |
|--------|---------|----------|
| **Домен** | Имя сайта (example.com) | Название магазина |
| **Хостинг** | Место, где лежит код | Аренда помещения |
| **Сервер** | Компьютер, который отвечает на запросы | Сотрудник магазина |
| **IP-адрес** | Уникальный адрес сервера | Координаты на карте |

### Типы хостинга
- **Shared** — делишь сервер с другими (дёшево, медленно)
- **VPS** — виртуальный выделенный сервер (контроль, нужны навыки)
- **PaaS** — платформа (Vercel, Railway, Render) — деплой без настройки сервера
- **Serverless** — функции по запросу (AWS Lambda, Cloudflare Workers)

---

## Что происходит при открытии сайта

```
1. Ввёл google.com
          │
2. DNS-запрос → получил IP
          │
3. TCP-соединение с сервером (handshake)
          │
4. TLS-соединение (для HTTPS)
          │
5. HTTP-запрос GET /
          │
6. Сервер отвечает HTML
          │
7. Браузер парсит HTML, запрашивает CSS, JS, картинки
          │
8. Рендеринг страницы
```

### 🔧 Практика 1.1.5
1. Открой DevTools → Network
2. Включи **Preserve log**
3. Очисти логи
4. Перейди на новый сайт
5. Отсортируй по **Waterfall** — увидишь порядок загрузки
6. Найди:
   - HTML-документ (первый запрос)
   - CSS и JS файлы
   - Изображения

---

## Порты

Один сервер может запускать несколько программ. **Порт** — номер "двери" к конкретной программе.

| Порт | Сервис |
|------|--------|
| 80 | HTTP |
| 443 | HTTPS |
| 3000 | Часто Node.js dev-сервер |
| 5173 | Vite (Vue) dev-сервер |
| 27017 | MongoDB |

```
http://localhost:3000/api/users
         │       │    └── путь
         │       └── порт
         └── хост (твой компьютер)
```

### 🔧 Практика 1.1.6

**Linux / macOS:**
```bash
# Посмотри, какие порты заняты
lsof -i -P | grep LISTEN

# Или через netstat
netstat -tulpn
```

**Windows (PowerShell / CMD):**
```powershell
# Все прослушиваемые порты
netstat -an | findstr LISTENING

# Через PowerShell с именами процессов
Get-NetTCPConnection -State Listen | Select-Object LocalPort, OwningProcess
```

---

## 📝 Итоговое задание

**Задача:** Проанализируй загрузку любого сайта и ответь на вопросы.

1. Открой DevTools → Network на сайте (например, github.com)
2. Заполни таблицу:

| Вопрос | Твой ответ |
|--------|------------|
| Сколько всего запросов? | |
| Какой HTTP-метод у первого запроса? | |
| Какой статус-код у первого запроса? | |
| Сколько весит HTML-документ? | |
| Какие JS-файлы загружаются? | |
| Есть ли запросы к другим доменам? | |

3. Выполни `nslookup github.com` — запиши IP-адрес
4. Попробуй открыть сайт по IP (вставь IP в адресную строку) — работает? Почему?

---

## 🔗 Ресурсы

- [How DNS Works (комикс)](https://howdns.works/)
- [HTTP Status Codes](https://http.cat/) — коды в картинках
- [What happens when...](https://github.com/alex/what-happens-when) — детальное объяснение
- [Learn JavaScript: Введение](https://learn.javascript.ru/intro)

---

## ✅ Чеклист раздела

- [ ] Понимаю разницу между компилятором и интерпретатором
- [ ] Могу объяснить, что такое клиент и сервер
- [ ] Знаю основные HTTP-методы и коды ответов
- [ ] Понимаю, как DNS переводит домен в IP
- [ ] Умею использовать DevTools → Network
- [ ] Могу проследить путь запроса от ввода URL до рендеринга
