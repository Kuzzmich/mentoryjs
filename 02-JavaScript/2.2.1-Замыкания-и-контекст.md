# 2.2.1 Замыкания и контекст (this)

Две концепции, которые отличают Junior от Middle-разработчика. Понимание замыканий и контекста — ключ к написанию профессионального JavaScript-кода.

---

## 🎯 Что ты узнаешь

- Что такое замыкание и где оно используется
- Как работает лексическое окружение
- Контекст выполнения и `this`
- Как управлять контекстом: `call`, `apply`, `bind`
- Частые ошибки с контекстом
- Практическое применение в реальных проектах

---

## 📖 Замыкания (Closures)

### Что это такое?

**Замыкание** — это функция, которая "запоминает" переменные из своей внешней области видимости, даже после того, как внешняя функция завершила выполнение.

```javascript
function createCounter() {
  let count = 0; // Приватная переменная

  return function() {
    count++;
    return count;
  };
}

const counter = createCounter();
console.log(counter()); // 1
console.log(counter()); // 2
console.log(counter()); // 3

// count недоступен снаружи!
console.log(count); // ReferenceError: count is not defined
```

**Что происходит:**
1. `createCounter` создаёт переменную `count`
2. Возвращает функцию, которая использует `count`
3. Внутренняя функция "замыкается" на переменной `count`
4. `count` живёт в памяти, пока существует ссылка на внутреннюю функцию

---

### Лексическое окружение

Когда функция создаётся, она сохраняет ссылку на **лексическое окружение** (Lexical Environment) — место, где она была объявлена.

```javascript
const name = 'Глобальная';

function outer() {
  const name = 'Внешняя';

  function inner() {
    const name = 'Внутренняя';
    console.log(name); // Какое значение выведется?
  }

  inner();
}

outer(); // "Внутренняя"
```

JavaScript ищет переменную по цепочке окружений:
1. Локальное окружение функции `inner`
2. Окружение функции `outer`
3. Глобальное окружение

---

### Зачем нужны замыкания?

**1. Инкапсуляция (приватные переменные)**

```javascript
function createWallet(initialBalance) {
  let balance = initialBalance; // Приватная переменная

  return {
    deposit(amount) {
      balance += amount;
      return balance;
    },
    withdraw(amount) {
      if (amount > balance) {
        return 'Недостаточно средств';
      }
      balance -= amount;
      return balance;
    },
    getBalance() {
      return balance;
    }
  };
}

const wallet = createWallet(1000);
console.log(wallet.getBalance()); // 1000
wallet.deposit(500);              // 1500
wallet.withdraw(200);             // 1300

// Напрямую изменить balance нельзя!
wallet.balance = 999999; // Не сработает
console.log(wallet.getBalance()); // 1300
```

**2. Фабричные функции**

```javascript
function createMultiplier(multiplier) {
  return function(number) {
    return number * multiplier;
  };
}

const double = createMultiplier(2);
const triple = createMultiplier(3);

console.log(double(5)); // 10
console.log(triple(5)); // 15
```

**3. Обработчики событий с состоянием**

```javascript
function setupButton(buttonId, initialCount) {
  let clickCount = initialCount;

  const button = document.getElementById(buttonId);

  button.addEventListener('click', function() {
    clickCount++;
    button.textContent = `Кликов: ${clickCount}`;
  });
}

setupButton('btn1', 0);
setupButton('btn2', 10);
// Каждая кнопка имеет свой независимый счётчик
```

---

### Частые ошибки с замыканиями

**❌ Проблема: замыкание в цикле**

```javascript
for (var i = 1; i <= 3; i++) {
  setTimeout(function() {
    console.log(i);
  }, i * 1000);
}
// Вывод: 4, 4, 4 (не 1, 2, 3!)
```

**✅ Решение 1: используй `let`**

```javascript
for (let i = 1; i <= 3; i++) {
  setTimeout(function() {
    console.log(i);
  }, i * 1000);
}
// Вывод: 1, 2, 3
```

**✅ Решение 2: создай замыкание**

```javascript
for (var i = 1; i <= 3; i++) {
  (function(j) {
    setTimeout(function() {
      console.log(j);
    }, j * 1000);
  })(i);
}
// Вывод: 1, 2, 3
```

---

## 📖 Контекст выполнения (this)

### Что такое `this`?

`this` — ключевое слово, которое ссылается на **контекст выполнения** функции. Его значение зависит от того, **как** функция была вызвана.

---

### Правила определения `this`

**1. Глобальный контекст**

```javascript
console.log(this); // В браузере: Window
                   // В Node.js: global
```

**2. Метод объекта**

```javascript
const user = {
  name: 'Анна',
  greet() {
    console.log(`Привет, я ${this.name}`);
  }
};

user.greet(); // "Привет, я Анна"
```

**3. Обычная функция**

```javascript
function showThis() {
  console.log(this);
}

showThis(); // В строгом режиме: undefined
            // Без строгого режима: Window (браузер)
```

**4. Стрелочная функция**

```javascript
const user = {
  name: 'Иван',
  regularFunc: function() {
    console.log(this.name); // "Иван"
  },
  arrowFunc: () => {
    console.log(this.name); // undefined
  }
};

user.regularFunc(); // "Иван"
user.arrowFunc();   // undefined
```

> 💡 **Важно:** Стрелочные функции **НЕ имеют своего `this`**. Они берут `this` из внешнего окружения.

---

### Потеря контекста

**Проблема:**

```javascript
const user = {
  name: 'Мария',
  greet() {
    console.log(`Привет, я ${this.name}`);
  }
};

const greetFunc = user.greet;
greetFunc(); // "Привет, я undefined"
```

**Почему потеряла связь?**

`this` определяется **способом вызова** функции, а не местом её объявления:

```javascript
// 1. Вызов как метод объекта → this = user
user.greet(); // this = user

// 2. Копируем ссылку на функцию (без объекта!)
const greetFunc = user.greet;
// Теперь greetFunc — это просто функция, без привязки к user

// 3. Вызов как обычной функции → this = undefined (strict mode)
greetFunc(); // this = undefined
```

**Аналогия:** Представь, что метод объекта — это сотрудник компании. Пока он работает в офисе (`user.greet()`), у него есть доступ ко всем ресурсам компании. Но если ты просто запомнишь его имя и позвонишь ему домой (`greetFunc()`), он уже не сможет получить доступ к корпоративным данным — он вне контекста компании.

---

### Управление контекстом

**1. `call()` — вызов с указанием `this`**

```javascript
function introduce(city, country) {
  console.log(`Я ${this.name} из ${city}, ${country}`);
}

const user = { name: 'Алексей' };

introduce.call(user, 'Москва', 'Россия');
// "Я Алексей из Москва, Россия"
```

**2. `apply()` — как `call`, но с массивом аргументов**

```javascript
introduce.apply(user, ['Санкт-Петербург', 'Россия']);
// "Я Алексей из Санкт-Петербург, Россия"
```

**3. `bind()` — создаёт новую функцию с привязанным `this`**

```javascript
const user = {
  name: 'Елена',
  greet() {
    console.log(`Привет, я ${this.name}`);
  }
};

const greetFunc = user.greet.bind(user);
greetFunc(); // "Привет, я Елена"

setTimeout(user.greet.bind(user), 1000); // Работает!
```

---

### Стрелочные функции и контекст

Стрелочные функции решают проблему потери контекста:

```javascript
const user = {
  name: 'Дмитрий',
  friends: ['Анна', 'Иван'],

  showFriends() {
    this.friends.forEach(function(friend) {
      // ❌ this = undefined
      console.log(`${this.name} дружит с ${friend}`);
    });
  },

  showFriendsCorrect() {
    this.friends.forEach(friend => {
      // ✅ this = user
      console.log(`${this.name} дружит с ${friend}`);
    });
  }
};

user.showFriends();        // Ошибка
user.showFriendsCorrect(); // "Дмитрий дружит с Анна", "Дмитрий дружит с Иван"
```

---

## 🌤️ Практика: Погодное приложение (Этап 2.1)

Применим замыкания и контекст для улучшения архитектуры приложения.

### Задание 2.2.1.1: Модуль для работы с избранным

Создай модуль для управления избранными городами с приватным состоянием.

```javascript
// Создай функцию createFavoritesManager
function createFavoritesManager() {
  // Приватные переменные (используй замыкание)
  // - favorites (массив избранных городов)

  // Верни объект с методами:
  // - add(city) — добавляет город
  // - remove(city) — удаляет город
  // - getAll() — возвращает все избранные
  // - has(city) — проверяет наличие города
  // - getCount() — количество избранных
}

// Проверка
const favorites = createFavoritesManager();
favorites.add('Москва');
favorites.add('Санкт-Петербург');
console.log(favorites.getAll());      // ['Москва', 'Санкт-Петербург']
console.log(favorites.has('Москва')); // true
console.log(favorites.getCount());    // 2
```

<details>
<summary>💡 Подсказка</summary>

Используй массив внутри функции и возвращай объект с методами.

```javascript
function createFavoritesManager() {
  const favorites = [];

  return {
    add(city) {
      if (!favorites.includes(city)) {
        favorites.push(city);
      }
    },
    // остальные методы...
  };
}
```
</details>

---

### Задание 2.2.1.2: Фабрика температурных конвертеров

Создай фабрику для преобразования температур.

```javascript
// Создай функцию createConverter(fromUnit, toUnit)
// fromUnit и toUnit могут быть: 'C', 'F', 'K'

// Пример:
const celsiusToFahrenheit = createConverter('C', 'F');
console.log(celsiusToFahrenheit(0));   // 32
console.log(celsiusToFahrenheit(100)); // 212

const fahrenheitToCelsius = createConverter('F', 'C');
console.log(fahrenheitToCelsius(32));  // 0
console.log(fahrenheitToCelsius(212)); // 100
```

<details>
<summary>💡 Подсказка</summary>

Формулы преобразования:
- C → F: `(C * 9/5) + 32`
- F → C: `(F - 32) * 5/9`
- C → K: `C + 273.15`
- K → C: `K - 273.15`

Используй замыкание, чтобы запомнить направление конвертации.
</details>

---

### Задание 2.2.1.3: Объект погоды с методами

Создай объект `weatherApp` с методами для работы с городами.

```javascript
const weatherApp = {
  currentCity: 'Москва',
  temperature: 20,

  setCity(city) {
    this.currentCity = city;
    console.log(`Город изменён на: ${this.currentCity}`);
  },

  setTemperature(temp) {
    this.temperature = temp;
    console.log(`Температура: ${this.temperature}°C в городе ${this.currentCity}`);
  },

  displayInfo() {
    console.log(`${this.currentCity}: ${this.temperature}°C`);
  }
};

// Проверь работу
weatherApp.setCity('Казань');
weatherApp.setTemperature(15);
weatherApp.displayInfo();

// Проблема: попробуй вызвать метод через setTimeout
setTimeout(weatherApp.displayInfo, 1000);
// Что произошло? Исправь с помощью bind
```

<details>
<summary>💡 Подсказка</summary>

Используй `.bind(weatherApp)` для привязки контекста.

```javascript
setTimeout(weatherApp.displayInfo.bind(weatherApp), 1000);
```
</details>

---

### Задание 2.2.1.4: Кнопка обновления с замыканием

Создай счётчик обновлений данных о погоде.

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Счётчик обновлений</title>
</head>
<body>
  <button id="refresh-btn">Обновить погоду</button>
  <p id="update-counter">Обновлений: 0</p>

  <script>
    // Создай функцию setupRefreshButton(), которая:
    // 1. Использует замыкание для хранения счётчика
    // 2. При клике увеличивает счётчик
    // 3. Обновляет текст в #update-counter

    function setupRefreshButton() {
      // Твой код здесь
    }

    setupRefreshButton();
  </script>
</body>
</html>
```

<details>
<summary>💡 Подсказка</summary>

```javascript
function setupRefreshButton() {
  let updateCount = 0;

  const button = document.getElementById('refresh-btn');
  const counter = document.getElementById('update-counter');

  button.addEventListener('click', function() {
    updateCount++;
    counter.textContent = `Обновлений: ${updateCount}`;
  });
}
```
</details>

---

### Задание 2.2.1.5: Обработчики с правильным контекстом

Создай фабричную функцию `createWeatherCard` с методами и событиями.

```javascript
function createWeatherCard(city, temperature) {
  // Создай объект карточки с методами
  const weatherCard = {
    city: city,
    temperature: temperature,

    render() {
      const card = document.createElement('div');
      card.className = 'weather-card';
      card.innerHTML = `
        <h3>${this.city}</h3>
        <p>${this.temperature}°C</p>
        <button class="refresh-btn">Обновить</button>
      `;

      const button = card.querySelector('.refresh-btn');

      // ❌ Проблема: this будет потерян
      button.addEventListener('click', this.refresh);

      // ✅ Исправь проблему тремя способами:
      // 1. С помощью bind
      // 2. С помощью стрелочной функции
      // 3. Сохранив this в переменную

      return card;
    },

    refresh() {
      console.log(`Обновление данных для ${this.city}`);
      this.temperature = Math.round(Math.random() * 30);
      console.log(`Новая температура: ${this.temperature}°C`);
    }
  };

  return weatherCard;
}

// Проверка
const card = createWeatherCard('Москва', 20);
document.body.appendChild(card.render());
```

<details>
<summary>💡 Решения</summary>

**Способ 1: bind**
```javascript
button.addEventListener('click', this.refresh.bind(this));
```

**Способ 2: стрелочная функция**
```javascript
button.addEventListener('click', () => this.refresh());
```

**Способ 3: сохранение this**
```javascript
const self = this;
button.addEventListener('click', function() {
  self.refresh();
});
```
</details>

---

## 🏆 Итоговое задание 2.2.1

Объедини все знания: создай **улучшенный модуль погодного приложения** с использованием замыканий и правильного контекста.

### Требования:

1. **Создай модуль `WeatherApp`** (используй замыкание):
   - Приватные переменные: `currentCity`, `favorites`, `updateCount`
   - Методы: `setCity()`, `addToFavorites()`, `getUpdateCount()`, `displayInfo()`

2. **Создай фабрику `createWeatherCard(city, temp)`**:
   - Возвращает объект с методами `render()` и `update()`
   - Методы должны корректно работать с `this`

3. **Обработчики событий**:
   - Кнопка добавления в избранное (с правильным контекстом)
   - Кнопка обновления (увеличивает счётчик через замыкание)

### HTML структура:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погодное приложение — Этап 2.1</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: Arial, sans-serif; background: #e3f2fd; padding: 20px; }

    .container { max-width: 600px; margin: 0 auto; }

    .weather-card {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      margin-bottom: 20px;
    }

    .controls {
      display: flex;
      gap: 10px;
      margin-top: 15px;
    }

    button {
      padding: 10px 20px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 14px;
    }

    .btn-primary { background: #2196f3; color: white; }
    .btn-secondary { background: #ff9800; color: white; }

    .favorites {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    .favorites h3 { margin-bottom: 10px; }
    .favorites ul { list-style: none; }
    .favorites li { padding: 8px 0; border-bottom: 1px solid #eee; }

    .stats {
      background: #fff3e0;
      padding: 15px;
      border-radius: 8px;
      margin-top: 20px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="weather-card">
      <h2 id="city-name">Москва</h2>
      <p id="temperature" style="font-size: 48px; margin: 10px 0;">20°C</p>

      <div class="controls">
        <button id="refresh-btn" class="btn-primary">Обновить</button>
        <button id="add-favorite-btn" class="btn-secondary">В избранное</button>
      </div>
    </div>

    <div class="favorites">
      <h3>Избранные города</h3>
      <ul id="favorites-list"></ul>
    </div>

    <div class="stats">
      <p id="update-count">Всего обновлений: 0</p>
    </div>
  </div>

  <script>
    // Твой код здесь
  </script>
</body>
</html>
```

### Ожидаемое поведение:

- При клике на "Обновить" — температура меняется случайно, счётчик увеличивается
- При клике на "В избранное" — текущий город добавляется в список избранного
- Не должно быть дубликатов в избранном
- Контекст должен работать корректно во всех обработчиках

<details>
<summary>💡 Подсказка по структуре</summary>

```javascript
function createWeatherApp() {
  let currentCity = 'Москва';
  let currentTemp = 20;
  let updateCount = 0;
  const favorites = createFavoritesManager();

  return {
    setCity(city) { /* ... */ },
    refresh() { /* используй замыкание для updateCount */ },
    addToFavorites() { /* ... */ },
    render() { /* подключи обработчики с правильным this */ }
  };
}

const app = createWeatherApp();
app.render();
```
</details>

---

## ✅ Чеклист

После изучения этой темы ты должен уметь:

- [ ] Объяснить, что такое замыкание
- [ ] Создавать функции с приватными переменными
- [ ] Использовать фабричные функции
- [ ] Понимать, как определяется `this` в разных ситуациях
- [ ] Объяснить разницу между обычными и стрелочными функциями в контексте `this`
- [ ] Использовать `call`, `apply`, `bind`
- [ ] Избегать потери контекста в обработчиках событий
- [ ] Применять замыкания для решения реальных задач

---

## 🔗 Ресурсы

**Замыкания:**
- [Замыкания — Learn JavaScript](https://learn.javascript.ru/closure)
- [MDN: Closures](https://developer.mozilla.org/ru/docs/Web/JavaScript/Closures)
- [You Don't Know JS: Scope & Closures](https://github.com/azat-io/you-dont-know-js-ru/blob/master/scope%20%26%20closures/README.md)

**Контекст (this):**
- [Контекст this — Learn JavaScript](https://learn.javascript.ru/object-methods)
- [MDN: this](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Operators/this)
- [JavaScript.info: Object methods, "this"](https://javascript.info/object-methods)

**Видео:**
- [Замыкания в JavaScript — Владилен Минин](https://www.youtube.com/watch?v=pahO5XjnfLA)
- [Контекст выполнения this — Владилен Минин](https://www.youtube.com/watch?v=UGapN-hrekw)

---

⏱ **Время на изучение**: 6-8 часов

---

**Предыдущая тема:** [2.1.5 DOM и события](./2.1.5-DOM-и-события.md)

**Следующая тема:** [2.2.2 Прототипы и классы](./2.2.2-Прототипы-и-классы.md)

---

> ## 🚀 Ускоряем карьерный рост в разработке
>
> **Менторство по подписке для Fullstack-разработчиков**
>
> - 7 дней бесплатно, чтобы попробовать
> - Неограниченная обратная связь по коду и архитектуре
> - Максимум практики на реальных задачах
>
> [**Подробнее о менторах, обучении и кейсах →**](ссылка)
