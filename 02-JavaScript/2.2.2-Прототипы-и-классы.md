# 2.2.2 Прототипы и классы

JavaScript — прототипно-ориентированный язык. Классы в ES6 — это синтаксический сахар над прототипами. Понимание прототипов помогает глубже понять, как работает наследование в JavaScript.

---

## 🎯 Что ты узнаешь

- Что такое прототип и как работает прототипное наследование
- Цепочка прототипов (prototype chain)
- Функции-конструкторы
- Классы ES6: синтаксис и возможности
- Наследование через `extends`
- Публичные и приватные поля
- Когда использовать классы, а когда — фабричные функции

---

## 📖 Прототипы

### Что такое прототип?

**Прототип** — это объект, от которого другие объекты наследуют свойства и методы.

Каждый объект в JavaScript имеет скрытую ссылку на свой прототип `[[Prototype]]` (доступна через `__proto__` или `Object.getPrototypeOf()`).

```javascript
const user = {
  name: 'Иван'
};

console.log(user.__proto__); // Object.prototype
console.log(user.toString);  // Метод из Object.prototype
console.log(user.toString()); // "[object Object]"
```

**Откуда взялся `toString`?**
JavaScript ищет метод в цепочке прототипов:
1. В самом объекте `user` → не найден
2. В `user.__proto__` (Object.prototype) → найден! ✅

---

### Цепочка прототипов

```javascript
const animal = {
  eats: true,
  walk() {
    console.log('Животное идёт');
  }
};

const rabbit = {
  jumps: true,
  __proto__: animal  // rabbit наследует от animal
};

console.log(rabbit.eats);  // true (взято из animal)
console.log(rabbit.jumps); // true (своё свойство)
rabbit.walk();             // "Животное идёт" (взято из animal)

// Цепочка: rabbit → animal → Object.prototype → null
console.log(rabbit.__proto__);               // animal
console.log(rabbit.__proto__.__proto__);     // Object.prototype
console.log(rabbit.__proto__.__proto__.__proto__); // null
```

**Как работает поиск свойства:**

```javascript
rabbit.jumps
// 1. Ищет в rabbit → найдено ✅

rabbit.eats
// 1. Ищет в rabbit → не найдено
// 2. Ищет в animal → найдено ✅

rabbit.toString()
// 1. Ищет в rabbit → не найдено
// 2. Ищет в animal → не найдено
// 3. Ищет в Object.prototype → найдено ✅
```

---

### Функции-конструкторы

До ES6 классы создавались через функции-конструкторы:

```javascript
function User(name, age) {
  this.name = name;
  this.age = age;
}

// Методы добавляются в прототип
User.prototype.greet = function() {
  console.log(`Привет, я ${this.name}`);
};

User.prototype.getInfo = function() {
  return `${this.name}, ${this.age} лет`;
};

// Создание экземпляров
const user1 = new User('Анна', 25);
const user2 = new User('Иван', 30);

user1.greet();              // "Привет, я Анна"
console.log(user2.getInfo()); // "Иван, 30 лет"

// Проверка прототипа
console.log(user1.__proto__ === User.prototype); // true
console.log(user1.constructor === User);         // true
```

**Почему методы добавляются в прототип?**

```javascript
// ❌ Плохо: метод создаётся для каждого объекта
function User(name) {
  this.name = name;
  this.greet = function() {
    console.log(`Привет, я ${this.name}`);
  };
}
// Каждый new User создаёт новую функцию greet → трата памяти

// ✅ Хорошо: метод один на всех
function User(name) {
  this.name = name;
}
User.prototype.greet = function() {
  console.log(`Привет, я ${this.name}`);
};
// Все экземпляры используют один и тот же метод из прототипа
```

---

### Наследование через прототипы

```javascript
function Animal(name) {
  this.name = name;
}

Animal.prototype.walk = function() {
  console.log(`${this.name} идёт`);
};

function Rabbit(name, earLength) {
  Animal.call(this, name); // Вызов родительского конструктора
  this.earLength = earLength;
}

// Настройка наследования
Rabbit.prototype = Object.create(Animal.prototype);
Rabbit.prototype.constructor = Rabbit;

Rabbit.prototype.jump = function() {
  console.log(`${this.name} прыгает`);
};

const rabbit = new Rabbit('Белый кролик', 10);
rabbit.walk(); // "Белый кролик идёт" (из Animal)
rabbit.jump(); // "Белый кролик прыгает" (из Rabbit)

console.log(rabbit instanceof Rabbit); // true
console.log(rabbit instanceof Animal); // true
```

---

## 📖 Классы ES6

Классы — это современный и удобный синтаксис для создания объектов с общими свойствами и методами.

### Базовый синтаксис

```javascript
class User {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  greet() {
    console.log(`Привет, я ${this.name}`);
  }

  getInfo() {
    return `${this.name}, ${this.age} лет`;
  }
}

const user = new User('Мария', 28);
user.greet();              // "Привет, я Мария"
console.log(user.getInfo()); // "Мария, 28 лет"
```

**Что происходит под капотом:**
- `constructor` — вызывается при `new User()`
- Методы автоматически добавляются в `User.prototype`
- Классы работают в строгом режиме (`'use strict'`)

---

### Классы vs функции-конструкторы

```javascript
// Старый способ (ES5)
function User(name) {
  this.name = name;
}
User.prototype.greet = function() {
  console.log(`Привет, ${this.name}`);
};

// Новый способ (ES6+)
class User {
  constructor(name) {
    this.name = name;
  }

  greet() {
    console.log(`Привет, ${this.name}`);
  }
}

// Результат одинаковый!
```

**Различия:**
- Классы нельзя вызвать без `new` (будет ошибка)
- Методы классов неперечислимы (enumerable: false)
- Классы всегда в строгом режиме

---

### Наследование через `extends`

```javascript
class Animal {
  constructor(name) {
    this.name = name;
  }

  walk() {
    console.log(`${this.name} идёт`);
  }
}

class Rabbit extends Animal {
  constructor(name, earLength) {
    super(name); // Вызов конструктора родителя
    this.earLength = earLength;
  }

  jump() {
    console.log(`${this.name} прыгает`);
  }

  // Переопределение метода
  walk() {
    console.log(`${this.name} прыгает вместо ходьбы`);
  }
}

const rabbit = new Rabbit('Белый кролик', 10);
rabbit.walk(); // "Белый кролик прыгает вместо ходьбы"
rabbit.jump(); // "Белый кролик прыгает"
```

**Важно про `super`:**

```javascript
class Child extends Parent {
  constructor() {
    // ❌ Ошибка: нельзя использовать this до super()
    // this.value = 5;

    super(); // ✅ Сначала вызываем super()
    this.value = 5; // Только потом используем this
  }

  method() {
    super.method(); // Вызов метода родителя
    // Дополнительная логика...
  }
}
```

---

### Геттеры и сеттеры

```javascript
class User {
  constructor(firstName, lastName) {
    this.firstName = firstName;
    this.lastName = lastName;
  }

  // Геттер — вычисляемое свойство
  get fullName() {
    return `${this.firstName} ${this.lastName}`;
  }

  // Сеттер — контроль изменения
  set fullName(value) {
    const [firstName, lastName] = value.split(' ');
    this.firstName = firstName;
    this.lastName = lastName;
  }

  get age() {
    return this._age;
  }

  set age(value) {
    if (value < 0) {
      console.log('Возраст не может быть отрицательным');
      return;
    }
    this._age = value;
  }
}

const user = new User('Иван', 'Петров');
console.log(user.fullName); // "Иван Петров" (геттер)

user.fullName = 'Мария Сидорова'; // Сеттер
console.log(user.firstName);      // "Мария"
console.log(user.lastName);       // "Сидорова"

user.age = 25;
console.log(user.age); // 25

user.age = -5; // "Возраст не может быть отрицательным"
```

---

### Статические методы и свойства

Статические методы принадлежат классу, а не экземплярам.

```javascript
class MathHelper {
  static PI = 3.14159;

  static square(n) {
    return n * n;
  }

  static random(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
  }
}

console.log(MathHelper.PI);           // 3.14159
console.log(MathHelper.square(5));    // 25
console.log(MathHelper.random(1, 10)); // Случайное число 1-10

// ❌ Нельзя вызвать на экземпляре
const helper = new MathHelper();
helper.square(5); // Ошибка!
```

**Практическое применение:**

```javascript
class User {
  constructor(name, role) {
    this.name = name;
    this.role = role;
  }

  // Статический метод-фабрика
  static createAdmin(name) {
    return new User(name, 'admin');
  }

  static createGuest(name) {
    return new User(name, 'guest');
  }
}

const admin = User.createAdmin('Алексей');
const guest = User.createGuest('Аноним');

console.log(admin.role); // "admin"
console.log(guest.role); // "guest"
```

---

### Приватные поля (ES2022)

Приватные поля начинаются с `#` и доступны только внутри класса.

```javascript
class BankAccount {
  #balance = 0; // Приватное поле

  constructor(owner) {
    this.owner = owner;
  }

  deposit(amount) {
    if (amount > 0) {
      this.#balance += amount;
      console.log(`Пополнено: ${amount}₽`);
    }
  }

  withdraw(amount) {
    if (amount > this.#balance) {
      console.log('Недостаточно средств');
      return;
    }
    this.#balance -= amount;
    console.log(`Снято: ${amount}₽`);
  }

  getBalance() {
    return this.#balance;
  }

  // Приватный метод
  #validateAmount(amount) {
    return amount > 0 && amount <= 1000000;
  }
}

const account = new BankAccount('Иван');
account.deposit(1000);
account.withdraw(300);
console.log(account.getBalance()); // 700

// ❌ Приватные поля недоступны снаружи
console.log(account.#balance); // SyntaxError
account.#validateAmount(500);  // SyntaxError
```

---

## 🌤️ Практика: Погодное приложение (Этап 2.2)

Применим классы для создания модульной архитектуры приложения.

### Задание 2.2.2.1: Базовый класс WeatherCard

Создай класс для карточки погоды.

```javascript
class WeatherCard {
  constructor(city, temperature, description) {
    // Твой код
  }

  render() {
    // Создай и верни DOM-элемент карточки
    // Структура:
    // <div class="weather-card">
    //   <h3>{city}</h3>
    //   <p class="temp">{temperature}°C</p>
    //   <p class="desc">{description}</p>
    // </div>
  }

  update(temperature, description) {
    // Обнови данные карточки
  }
}

// Проверка
const card = new WeatherCard('Москва', 20, 'Облачно');
document.body.appendChild(card.render());
```

<details>
<summary>💡 Подсказка</summary>

```javascript
class WeatherCard {
  constructor(city, temperature, description) {
    this.city = city;
    this.temperature = temperature;
    this.description = description;
  }

  render() {
    const card = document.createElement('div');
    card.className = 'weather-card';
    card.innerHTML = `
      <h3>${this.city}</h3>
      <p class="temp">${this.temperature}°C</p>
      <p class="desc">${this.description}</p>
    `;
    return card;
  }

  update(temperature, description) {
    this.temperature = temperature;
    this.description = description;
  }
}
```
</details>

---

### Задание 2.2.2.2: Класс с приватными полями

Создай класс `TemperatureConverter` с приватным хранилищем истории конвертаций.

```javascript
class TemperatureConverter {
  // Приватное поле для истории
  #history = [];

  celsiusToFahrenheit(celsius) {
    const fahrenheit = (celsius * 9/5) + 32;
    this.#addToHistory('C->F', celsius, fahrenheit);
    return fahrenheit;
  }

  fahrenheitToCelsius(fahrenheit) {
    const celsius = (fahrenheit - 32) * 5/9;
    this.#addToHistory('F->C', fahrenheit, celsius);
    return celsius;
  }

  #addToHistory(type, input, output) {
    // Добавь запись в историю
  }

  getHistory() {
    // Верни копию истории (не оригинал!)
    return [...this.#history];
  }

  clearHistory() {
    this.#history = [];
  }
}

// Проверка
const converter = new TemperatureConverter();
console.log(converter.celsiusToFahrenheit(0));   // 32
console.log(converter.fahrenheitToCelsius(212)); // 100
console.log(converter.getHistory());
// [
//   { type: 'C->F', input: 0, output: 32 },
//   { type: 'F->C', input: 212, output: 100 }
// ]
```

<details>
<summary>💡 Подсказка</summary>

```javascript
#addToHistory(type, input, output) {
  this.#history.push({
    type,
    input,
    output,
    timestamp: new Date()
  });
}
```
</details>

---

### Задание 2.2.2.3: Наследование классов

Создай базовый класс `WeatherWidget` и унаследуй от него специализированные виджеты.

```javascript
class WeatherWidget {
  constructor(city) {
    this.city = city;
    this.element = null;
  }

  render() {
    throw new Error('Метод render должен быть реализован');
  }

  update(data) {
    throw new Error('Метод update должен быть реализован');
  }
}

class TemperatureWidget extends WeatherWidget {
  constructor(city, temperature) {
    super(city);
    this.temperature = temperature;
  }

  render() {
    // Создай элемент, сохрани в this.element и верни
    // Должен показывать: "{city}: {temperature}°C"
  }

  update(temperature) {
    this.temperature = temperature;
    // Обнови текст в this.element
  }
}

class ForecastWidget extends WeatherWidget {
  constructor(city, forecast) {
    super(city);
    this.forecast = forecast; // Массив [{day, temp}, ...]
  }

  render() {
    // Создай элемент со списком прогноза
    // День 1: 20°C
    // День 2: 22°C
    // День 3: 18°C
  }

  update(forecast) {
    this.forecast = forecast;
    // Обнови содержимое
  }
}

// Проверка
const tempWidget = new TemperatureWidget('Москва', 20);
document.body.appendChild(tempWidget.render());

const forecastWidget = new ForecastWidget('Санкт-Петербург', [
  { day: 1, temp: 20 },
  { day: 2, temp: 22 },
  { day: 3, temp: 18 }
]);
document.body.appendChild(forecastWidget.render());
```

---

### Задание 2.2.2.4: Статические методы-фабрики

Создай класс `WeatherData` со статическими методами для создания разных типов данных.

```javascript
class WeatherData {
  constructor(city, temperature, conditions) {
    this.city = city;
    this.temperature = temperature;
    this.conditions = conditions;
    this.timestamp = new Date();
  }

  static createSunny(city, temperature) {
    return new WeatherData(city, temperature, 'sunny');
  }

  static createRainy(city, temperature) {
    return new WeatherData(city, temperature, 'rainy');
  }

  static createCloudy(city, temperature) {
    return new WeatherData(city, temperature, 'cloudy');
  }

  static createSnowy(city, temperature) {
    return new WeatherData(city, temperature, 'snowy');
  }

  getIcon() {
    const icons = {
      sunny: '☀️',
      rainy: '🌧️',
      cloudy: '☁️',
      snowy: '❄️'
    };
    return icons[this.conditions] || '🌡️';
  }

  toString() {
    return `${this.getIcon()} ${this.city}: ${this.temperature}°C`;
  }
}

// Проверка
const weather1 = WeatherData.createSunny('Москва', 25);
const weather2 = WeatherData.createRainy('Санкт-Петербург', 15);

console.log(weather1.toString()); // "☀️ Москва: 25°C"
console.log(weather2.toString()); // "🌧️ Санкт-Петербург: 15°C"
```

---

### Задание 2.2.2.5: Геттеры и сеттеры с валидацией

Создай класс `WeatherSettings` с контролем значений через сеттеры.

```javascript
class WeatherSettings {
  #temperatureUnit = 'C'; // Приватное поле
  #updateInterval = 60;   // В секундах

  get temperatureUnit() {
    return this.#temperatureUnit;
  }

  set temperatureUnit(unit) {
    // Валидация: только 'C' или 'F'
    // Если невалидное значение — не изменяй и выведи ошибку
  }

  get updateInterval() {
    return this.#updateInterval;
  }

  set updateInterval(seconds) {
    // Валидация: от 30 до 3600 секунд
    // Если невалидное — не изменяй и выведи ошибку
  }

  get updateIntervalMinutes() {
    return this.#updateInterval / 60;
  }

  toString() {
    return `Единицы: ${this.#temperatureUnit}, Обновление: каждые ${this.updateIntervalMinutes} мин`;
  }
}

// Проверка
const settings = new WeatherSettings();
console.log(settings.toString()); // "Единицы: C, Обновление: каждые 1 мин"

settings.temperatureUnit = 'F';
settings.updateInterval = 120;
console.log(settings.toString()); // "Единицы: F, Обновление: каждые 2 мин"

settings.temperatureUnit = 'K'; // Должна быть ошибка
settings.updateInterval = 10;   // Должна быть ошибка
```

<details>
<summary>💡 Подсказка</summary>

```javascript
set temperatureUnit(unit) {
  if (unit !== 'C' && unit !== 'F') {
    console.log('Ошибка: temperatureUnit может быть только C или F');
    return;
  }
  this.#temperatureUnit = unit;
}

set updateInterval(seconds) {
  if (seconds < 30 || seconds > 3600) {
    console.log('Ошибка: updateInterval должен быть от 30 до 3600 секунд');
    return;
  }
  this.#updateInterval = seconds;
}
```
</details>

---

## 🏆 Итоговое задание 2.2.2

Создай **объектно-ориентированное погодное приложение** с использованием классов, наследования и приватных полей.

### Требования:

1. **Базовый класс `WeatherComponent`:**
   - Методы: `render()`, `update()`, `destroy()`

2. **Класс `WeatherCard` extends WeatherComponent:**
   - Приватное поле `#data`
   - Методы: `render()`, `update(newData)`, `getCity()`

3. **Класс `WeatherApp`:**
   - Приватные поля: `#cards`, `#settings`
   - Статический метод: `createDefault()`
   - Методы: `addCard(city)`, `removeCard(city)`, `updateAll()`

4. **Класс `WeatherSettings`:**
   - Приватные поля для настроек
   - Геттеры и сеттеры с валидацией

### HTML структура:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погодное приложение — Классы</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    h1 {
      color: white;
      text-align: center;
      margin-bottom: 30px;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .controls {
      background: white;
      padding: 20px;
      border-radius: 12px;
      margin-bottom: 20px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    .controls input {
      padding: 10px;
      border: 2px solid #ddd;
      border-radius: 8px;
      font-size: 16px;
      margin-right: 10px;
      width: 300px;
    }

    .controls button {
      padding: 10px 20px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.3s;
    }

    .controls button:hover {
      background: #5568d3;
    }

    .weather-cards {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
    }

    .weather-card {
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
      transition: transform 0.3s, box-shadow 0.3s;
    }

    .weather-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 24px rgba(0,0,0,0.15);
    }

    .weather-card h3 {
      color: #333;
      margin-bottom: 15px;
      font-size: 24px;
    }

    .weather-card .temp {
      font-size: 48px;
      font-weight: bold;
      color: #667eea;
      margin: 10px 0;
    }

    .weather-card .description {
      color: #666;
      margin-bottom: 15px;
    }

    .weather-card button {
      background: #f44336;
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 6px;
      cursor: pointer;
      font-size: 14px;
    }

    .weather-card button:hover {
      background: #d32f2f;
    }

    .settings {
      background: white;
      padding: 20px;
      border-radius: 12px;
      margin-top: 20px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.1);
    }

    .settings h3 {
      margin-bottom: 15px;
      color: #333;
    }

    .settings label {
      display: block;
      margin-bottom: 10px;
      color: #666;
    }

    .settings select {
      padding: 8px;
      border: 2px solid #ddd;
      border-radius: 6px;
      margin-left: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🌤 Погодное приложение</h1>

    <div class="controls">
      <input type="text" id="city-input" placeholder="Введите название города">
      <button id="add-btn">Добавить город</button>
      <button id="update-all-btn">Обновить всё</button>
    </div>

    <div class="weather-cards" id="cards-container"></div>

    <div class="settings">
      <h3>⚙️ Настройки</h3>
      <label>
        Единицы температуры:
        <select id="temp-unit">
          <option value="C">Цельсий (°C)</option>
          <option value="F">Фаренгейт (°F)</option>
        </select>
      </label>
    </div>
  </div>

  <script>
    // Твой код здесь

    // Структура классов:

    class WeatherComponent {
      // Базовый класс для всех компонентов
    }

    class WeatherCard extends WeatherComponent {
      // Карточка погоды
    }

    class WeatherApp {
      // Главный класс приложения
    }

    class WeatherSettings {
      // Настройки приложения
    }

    // Инициализация
    const app = WeatherApp.createDefault();
    app.addCard('Москва');
    app.addCard('Санкт-Петербург');
  </script>
</body>
</html>
```

### Ожидаемое поведение:

- Добавление городов по кнопке
- Удаление карточки по кнопке на карточке
- Обновление всех карточек (случайная температура)
- Переключение единиц температуры (C/F)
- Использование приватных полей для данных
- Наследование WeatherCard от WeatherComponent

<details>
<summary>💡 Архитектура</summary>

```javascript
class WeatherComponent {
  constructor() {
    this.element = null;
  }

  render() {
    throw new Error('Метод render() должен быть реализован');
  }

  update() {
    throw new Error('Метод update() должен быть реализован');
  }

  destroy() {
    if (this.element) {
      this.element.remove();
    }
  }
}

class WeatherCard extends WeatherComponent {
  #data;

  constructor(city) {
    super();
    this.#data = {
      city,
      temperature: Math.round(Math.random() * 30),
      description: this.#getRandomDescription()
    };
  }

  #getRandomDescription() {
    const descriptions = ['Ясно', 'Облачно', 'Дождь', 'Снег'];
    return descriptions[Math.floor(Math.random() * descriptions.length)];
  }

  render() {
    // Создай карточку...
  }

  update() {
    // Обнови данные...
  }

  getCity() {
    return this.#data.city;
  }
}

class WeatherApp {
  #cards = new Map();
  #settings;
  #container;

  constructor(container) {
    this.#container = container;
    this.#settings = new WeatherSettings();
  }

  static createDefault() {
    const container = document.getElementById('cards-container');
    return new WeatherApp(container);
  }

  addCard(city) {
    // Добавь карточку...
  }

  removeCard(city) {
    // Удали карточку...
  }

  updateAll() {
    // Обнови все карточки...
  }
}
```
</details>

---

## ✅ Чеклист

После изучения этой темы ты должен уметь:

- [ ] Объяснить, что такое прототип и цепочка прототипов
- [ ] Создавать объекты через функции-конструкторы
- [ ] Понимать, как работает наследование через прототипы
- [ ] Создавать классы с помощью синтаксиса ES6
- [ ] Использовать наследование через `extends` и `super`
- [ ] Создавать геттеры и сеттеры
- [ ] Использовать статические методы и свойства
- [ ] Создавать приватные поля и методы (ES2022)
- [ ] Понимать разницу между классами и фабричными функциями
- [ ] Выбирать подходящий подход для конкретной задачи

---

## 🔗 Ресурсы

**Прототипы:**
- [Прототипное наследование — Learn JavaScript](https://learn.javascript.ru/prototype-inheritance)
- [F.prototype — Learn JavaScript](https://learn.javascript.ru/function-prototype)
- [MDN: Прототипы объектов](https://developer.mozilla.org/ru/docs/Learn/JavaScript/Objects/Object_prototypes)

**Классы:**
- [Классы — Learn JavaScript](https://learn.javascript.ru/classes)
- [Наследование классов — Learn JavaScript](https://learn.javascript.ru/class-inheritance)
- [MDN: Классы](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Classes)
- [Приватные поля классов](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/Private_class_fields)

**Видео:**
- [ООП в JavaScript — Владилен Минин](https://www.youtube.com/watch?v=aQkgUUmUJy4)
- [Классы в JavaScript — Владилен Минин](https://www.youtube.com/watch?v=uLY9GXGMXaA)

---

⏱ **Время на изучение**: 6-8 часов

---

**Предыдущая тема:** [2.2.1 Замыкания и контекст (this)](./2.2.1-Замыкания-и-контекст.md)

**Следующая тема:** [2.2.3 Асинхронность: callbacks → promises → async/await](./2.2.3-Асинхронность.md)
