# 1.2.1 Терминал / командная строка

> **Цель:** Освоить минимум команд для комфортной работы с проектами. Глубокое погружение не требуется — большинство задач решается в IDE.

---

## Какой терминал использовать

| ОС | Рекомендация | Где найти |
|----|--------------|-----------|
| **Windows** | Windows Terminal + PowerShell | Microsoft Store → Windows Terminal |
| **macOS** | iTerm2 или встроенный Terminal | [iterm2.com](https://iterm2.com/) |
| **Linux** | Встроенный терминал | Ctrl + Alt + T |

> 💡 **Совет для Windows:** Установи Windows Terminal — он удобнее стандартного CMD и поддерживает вкладки, темы, PowerShell и WSL.

---

## Навигация по файловой системе

| Действие | Linux / macOS | Windows (PowerShell) |
|----------|---------------|----------------------|
| Где я нахожусь | `pwd` | `pwd` или `Get-Location` |
| Список файлов | `ls` | `ls` или `dir` |
| Список с деталями | `ls -la` | `ls -Force` или `dir /a` |
| Перейти в папку | `cd folder` | `cd folder` |
| На уровень выше | `cd ..` | `cd ..` |
| В домашнюю папку | `cd ~` | `cd ~` или `cd $HOME` |
| Очистить экран | `clear` | `cls` или `clear` |

### 🔧 Практика 1.2.1.1

**Задание:** Найди папку с проектами (или создай её).

**Linux / macOS:**
```bash
cd ~
mkdir -p projects
cd projects
pwd
# Должно вывести: /home/username/projects или /Users/username/projects
```

**Windows (PowerShell):**
```powershell
cd ~
mkdir projects -Force
cd projects
pwd
# Должно вывести: C:\Users\Username\projects
```

---

## Работа с файлами и папками

| Действие | Linux / macOS | Windows (PowerShell) |
|----------|---------------|----------------------|
| Создать папку | `mkdir folder` | `mkdir folder` |
| Создать файл | `touch file.txt` | `New-Item file.txt` |
| Скопировать | `cp file.txt copy.txt` | `cp file.txt copy.txt` |
| Переместить/переименовать | `mv old.txt new.txt` | `mv old.txt new.txt` |
| Удалить файл | `rm file.txt` | `rm file.txt` |
| Удалить папку | `rm -rf folder` | `rm -r folder -Force` |
| Прочитать файл | `cat file.txt` | `cat file.txt` или `type file.txt` |

> ⚠️ **Осторожно:** `rm -rf` удаляет без подтверждения и корзины. Проверяй путь дважды.

### 🔧 Практика 1.2.1.2

**Задание:** Создай структуру тестового проекта.

**Linux / macOS:**
```bash
cd ~/projects
mkdir test-project
cd test-project
mkdir src public
touch src/index.js public/index.html README.md
ls -la
```

**Windows (PowerShell):**
```powershell
cd ~/projects
mkdir test-project
cd test-project
mkdir src, public
New-Item src/index.js, public/index.html, README.md
ls
```

**Результат:** Должна получиться структура:
```
test-project/
├── src/
│   └── index.js
├── public/
│   └── index.html
└── README.md
```

---

## Полезные команды на каждый день

| Действие | Linux / macOS | Windows (PowerShell) |
|----------|---------------|----------------------|
| История команд | `history` | `history` |
| Повторить последнюю | `!!` | Стрелка вверх ↑ |
| Поиск в истории | `Ctrl + R` | `Ctrl + R` |
| Прервать выполнение | `Ctrl + C` | `Ctrl + C` |
| Автодополнение | `Tab` | `Tab` |
| Открыть папку в проводнике | `open .` (macOS) / `xdg-open .` (Linux) | `explorer .` |
| Открыть в VS Code | `code .` | `code .` |

### 🔧 Практика 1.2.1.3

**Задание:** Попробуй быстрые клавиши.

1. Введи несколько команд (cd, ls, pwd)
2. Нажми стрелку вверх ↑ — листай историю
3. Начни вводить `cd pro` и нажми `Tab` — автодополнение
4. Нажми `Ctrl + R` и начни вводить команду — поиск в истории
5. Открой текущую папку в VS Code: `code .`

---

## Переменные окружения

Переменные окружения — настройки системы, доступные всем программам.

| Действие | Linux / macOS | Windows (PowerShell) |
|----------|---------------|----------------------|
| Показать все | `env` | `Get-ChildItem Env:` |
| Показать одну | `echo $PATH` | `echo $env:PATH` |
| Установить временно | `export MY_VAR=value` | `$env:MY_VAR = "value"` |

**Важные переменные:**
- `PATH` — где система ищет исполняемые файлы
- `HOME` / `USERPROFILE` — домашняя папка
- `NODE_ENV` — окружение Node.js (development/production)

### 🔧 Практика 1.2.1.4

**Linux / macOS:**
```bash
echo $PATH
echo $HOME
export TEST_VAR="Hello"
echo $TEST_VAR
```

**Windows (PowerShell):**
```powershell
echo $env:PATH
echo $env:USERPROFILE
$env:TEST_VAR = "Hello"
echo $env:TEST_VAR
```

---

## Установка и запуск Node.js

### Установка Node.js

**Windows:**
1. Скачай LTS-версию с [nodejs.org](https://nodejs.org/)
2. Запусти установщик, оставь настройки по умолчанию
3. Перезапусти терминал

Или через winget:
```powershell
winget install OpenJS.NodeJS.LTS
```

**macOS:**
```bash
# Через Homebrew (рекомендуется)
brew install node

# Или скачай установщик с nodejs.org
```

**Linux (Ubuntu/Debian):**
```bash
# Установка через NodeSource (актуальная версия)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

> 💡 **Совет:** Для управления несколькими версиями Node.js используй [nvm](https://github.com/nvm-sh/nvm) (Linux/macOS) или [nvm-windows](https://github.com/coreybutler/nvm-windows).

### Проверка установки

После установки команды работают одинаково везде:

```bash
# Проверить версии
node -v
npm -v

# Запустить JS-файл
node src/index.js

# Инициализировать проект
npm init -y

# Установить пакет
npm install express

# Запустить скрипт из package.json
npm run dev

# Установить пакет глобально
npm install -g nodemon
```

### 🔧 Практика 1.2.1.5

**Задание:** Создай и запусти первый Node.js скрипт.

```bash
cd ~/projects/test-project
npm init -y
```

Открой `src/index.js` и добавь:
```bash
console.log("Node.js работает!");
console.log("Версия:", process.version);
console.log("Платформа:", process.platform);
```

Запусти:
```bash
node src/index.js
```

---

## Шпаргалка: 15 команд, которые нужно запомнить

```bash
pwd                 # где я
ls                  # что тут лежит
cd folder           # перейти в папку
cd ..               # на уровень выше
mkdir folder        # создать папку
touch file          # создать файл (Linux/macOS)
rm file             # удалить файл
rm -rf folder       # удалить папку
cat file            # показать содержимое
code .              # открыть в VS Code
node file.js        # запустить JS
npm init -y         # инициализировать проект
npm install pkg     # установить пакет
npm run script      # запустить скрипт
Ctrl + C            # остановить процесс
```

---

## 📝 Итоговое задание

**Задача:** Создай структуру проекта только через терминал.

1. Перейди в папку `~/projects`
2. Создай папку `my-first-app`
3. Внутри создай структуру:
```
my-first-app/
├── src/
│   ├── controllers/
│   ├── models/
│   └── index.js
├── public/
│   ├── css/
│   └── js/
├── .gitignore
└── README.md
```
4. Инициализируй npm-проект (`npm init -y`)
5. Открой проект в VS Code (`code .`)
6. Проверь структуру командой `ls -la` (или `tree`, если установлена)

---

## 🔗 Ресурсы

- [Linux Command Line Basics](https://ubuntu.com/tutorials/command-line-for-beginners)
- [PowerShell для начинающих](https://docs.microsoft.com/ru-ru/powershell/scripting/overview)
- [Explainshell](https://explainshell.com/) — объяснение любой команды

---

## ✅ Чеклист раздела

- [ ] Умею перемещаться по папкам (cd, pwd, ls)
- [ ] Могу создавать и удалять файлы/папки
- [ ] Использую Tab для автодополнения
- [ ] Знаю, как открыть проект в VS Code из терминала
- [ ] Могу запустить Node.js скрипт
- [ ] Понимаю базовые npm-команды
