# 2.1.5 DOM и события

> **Цель:** Научиться управлять HTML-страницей через JavaScript: находить элементы, изменять их содержимое и стили, реагировать на действия пользователя.

---

## Что такое DOM?

**DOM (Document Object Model)** — древовидная структура HTML-документа, которой можно управлять через JavaScript.

```
document
  └── html
      ├── head
      │   └── title
      └── body
          ├── h1
          └── div
              ├── p
              └── button
```

Каждый HTML-элемент — это **объект** со свойствами и методами.

---

## Поиск элементов

### querySelector / querySelectorAll

Поиск по CSS-селектору (рекомендуется):

```javascript
// Первый элемент
const title = document.querySelector('h1');
const temp = document.querySelector('.temperature');
const btn = document.querySelector('#update-btn');

// Все элементы (возвращает NodeList)
const items = document.querySelectorAll('.city-card');
console.log(items.length);  // количество

// Перебор
items.forEach(item => {
  console.log(item.textContent);
});
```

### getElementById / getElementsByClassName

Старый способ (работает быстрее, но менее гибкий):

```javascript
// По ID (один элемент)
const app = document.getElementById('app');

// По классу (коллекция HTMLCollection)
const cards = document.getElementsByClassName('card');

// По тегу
const paragraphs = document.getElementsByTagName('p');
```

> 💡 **Используй `querySelector`** — он работает как CSS-селекторы.

---

## Чтение и изменение содержимого

### textContent

Текст без HTML-тегов:

```javascript
const city = document.querySelector('.city-name');

// Прочитать
console.log(city.textContent);  // "Москва"

// Изменить
city.textContent = "Санкт-Петербург";
```

### innerHTML

HTML-содержимое (можно вставлять теги):

```javascript
const card = document.querySelector('.weather-card');

// Прочитать
console.log(card.innerHTML);

// Изменить
card.innerHTML = `
  <h2>Москва</h2>
  <p class="temp">15°C</p>
`;
```

> ⚠️ **Осторожно с innerHTML:** Если вставляешь пользовательский ввод — риск XSS-атаки. Используй `textContent` для текста.

---

## Изменение атрибутов

```javascript
const img = document.querySelector('.weather-icon');

// Прочитать атрибут
console.log(img.src);
console.log(img.alt);

// Изменить атрибут
img.src = "icons/sunny.png";
img.alt = "Солнечно";

// Проверить наличие
if (img.hasAttribute('data-temp')) {
  console.log(img.getAttribute('data-temp'));
}

// Установить data-атрибут
img.setAttribute('data-temp', '25');

// Удалить атрибут
img.removeAttribute('data-temp');
```

---

## Работа со стилями

### style (inline-стили)

```javascript
const temp = document.querySelector('.temperature');

// Изменить стиль
temp.style.color = 'red';
temp.style.fontSize = '24px';
temp.style.backgroundColor = '#fff';  // camelCase!

// Несколько стилей
temp.style.cssText = 'color: red; font-size: 24px;';
```

> 💡 CSS-свойства в JS пишутся в **camelCase**: `background-color` → `backgroundColor`

### classList (управление классами)

```javascript
const card = document.querySelector('.weather-card');

// Добавить класс
card.classList.add('active');

// Удалить класс
card.classList.remove('hidden');

// Переключить (toggle)
card.classList.toggle('dark-mode');

// Проверить наличие
if (card.classList.contains('active')) {
  console.log('Карточка активна');
}

// Заменить класс
card.classList.replace('old-class', 'new-class');
```

---

## Создание и удаление элементов

### Создание

```javascript
// Создать элемент
const card = document.createElement('div');
card.className = 'weather-card';

// Добавить содержимое
card.innerHTML = `
  <h3>Москва</h3>
  <p>15°C</p>
`;

// Вставить в DOM
const container = document.querySelector('.container');
container.appendChild(card);           // в конец
container.prepend(card);               // в начало
container.insertBefore(card, first);   // перед элементом
```

### Удаление

```javascript
const card = document.querySelector('.weather-card');

// Удалить элемент
card.remove();

// Удалить через родителя (старый способ)
card.parentElement.removeChild(card);
```

---

## События

**Событие** — действие, которое произошло на странице (клик, ввод текста, загрузка).

### addEventListener

```javascript
const btn = document.querySelector('#update-btn');

btn.addEventListener('click', () => {
  console.log('Кнопка нажата!');
});

// С параметром события
btn.addEventListener('click', (event) => {
  console.log('Клик по:', event.target);
  console.log('Координаты:', event.clientX, event.clientY);
});
```

### Типы событий

**Мышь:**
```javascript
element.addEventListener('click', handler);       // клик
element.addEventListener('dblclick', handler);    // двойной клик
element.addEventListener('mouseenter', handler);  // навели мышь
element.addEventListener('mouseleave', handler);  // убрали мышь
element.addEventListener('mousemove', handler);   // движение мыши
```

**Клавиатура:**
```javascript
input.addEventListener('keydown', handler);   // клавиша нажата
input.addEventListener('keyup', handler);     // клавиша отпущена
input.addEventListener('keypress', handler);  // клавиша нажата (устарело)
```

**Формы:**
```javascript
input.addEventListener('input', handler);    // изменение значения
input.addEventListener('change', handler);   // изменение и потеря фокуса
form.addEventListener('submit', handler);    // отправка формы
input.addEventListener('focus', handler);    // получение фокуса
input.addEventListener('blur', handler);     // потеря фокуса
```

**Страница:**
```javascript
window.addEventListener('load', handler);         // страница загружена
window.addEventListener('DOMContentLoaded', handler); // DOM готов
window.addEventListener('resize', handler);       // изменение размера окна
window.addEventListener('scroll', handler);       // прокрутка
```

### Объект события (event)

```javascript
button.addEventListener('click', (event) => {
  console.log(event.type);         // "click"
  console.log(event.target);       // элемент, на котором произошло событие
  console.log(event.currentTarget);// элемент, на котором висит обработчик
  console.log(event.clientX);      // координата X
  console.log(event.clientY);      // координата Y

  event.preventDefault();          // отменить действие по умолчанию
  event.stopPropagation();         // остановить всплытие
});
```

### preventDefault

Отменяет стандартное действие браузера:

```javascript
// Отменить переход по ссылке
link.addEventListener('click', (e) => {
  e.preventDefault();
  console.log('Переход отменён');
});

// Отменить отправку формы
form.addEventListener('submit', (e) => {
  e.preventDefault();
  console.log('Форма не отправлена');
  // Обработать данные через JS
});
```

---

## 🔧 Практика 2.1.5.1: Отображение погоды на странице

Создадим интерфейс для погодного приложения и выведем данные.

**Шаг 1: HTML**

Создай файл `index.html`:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Погода</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: 50px auto;
      padding: 20px;
    }
    .weather-card {
      border: 2px solid #ddd;
      border-radius: 10px;
      padding: 20px;
      text-align: center;
    }
    .temperature {
      font-size: 48px;
      font-weight: bold;
      margin: 20px 0;
    }
    .description {
      font-size: 20px;
      color: #666;
    }
  </style>
</head>
<body>
  <div class="weather-card">
    <h1 class="city-name">Загрузка...</h1>
    <div class="temperature">--°C</div>
    <div class="description">...</div>
  </div>

  <script src="script.js"></script>
</body>
</html>
```

**Шаг 2: JavaScript**

В `script.js`:

```javascript
// Данные о погоде
const weatherData = {
  city: "Москва",
  temperature: 15,
  description: "Облачно с прояснениями"
};

// TODO 1: Найди элементы на странице
// Используй querySelector для .city-name, .temperature, .description

// TODO 2: Отобрази данные о погоде
// Используй textContent для вывода city, temperature, description

// TODO 3: Измени цвет температуры в зависимости от значения
// Если температура < 10: синий
// Если температура >= 10 и < 20: зелёный
// Если температура >= 20: красный
// Используй element.style.color
```

---

## 🔧 Практика 2.1.5.2: Интерактивность

Добавим кнопку для обновления погоды.

**Добавь в HTML (перед закрывающим `</div>` карточки):**

```html
<button id="update-btn">Обновить погоду</button>
```

**В JavaScript:**

```javascript
// Несколько вариантов погоды
const weatherOptions = [
  { city: "Москва", temp: 15, desc: "Облачно" },
  { city: "Сочи", temp: 25, desc: "Солнечно" },
  { city: "Мурманск", temp: 5, desc: "Снег" },
  { city: "Казань", temp: 18, desc: "Ясно" }
];

// TODO 1: Найди кнопку #update-btn

// TODO 2: Добавь обработчик события 'click'
// При клике:
// - Выбери случайный элемент из weatherOptions
// - Обнови данные на странице
// - Измени цвет температуры (как в предыдущей практике)
// - Выведи в консоль: "Погода обновлена для города: ..."

// Подсказка: случайный индекс
// Math.floor(Math.random() * weatherOptions.length)
```

---

## 🔧 Практика 2.1.5.3: Список городов

Создадим список городов с возможностью выбора.

**Добавь в HTML (после карточки погоды):**

```html
<div class="cities-list">
  <h2>Выберите город:</h2>
  <ul id="cities"></ul>
</div>
```

**В JavaScript:**

```javascript
const cities = ["Москва", "Санкт-Петербург", "Казань", "Сочи", "Новосибирск"];

// TODO 1: Найди элемент ul#cities

// TODO 2: Создай список городов
// Для каждого города из массива:
// - Создай элемент <li>
// - Установи текст города
// - Добавь класс 'city-item'
// - Добавь в список

// TODO 3: Добавь обработчик события на КАЖДЫЙ элемент списка
// При клике на город:
// - Обнови название города в карточке погоды
// - Сгенерируй случайную температуру (от 0 до 30)
// - Обнови отображение температуры
// - Выведи в консоль: "Выбран город: ..."
```

---

## 📝 Итоговое задание

Создай интерактивное погодное приложение с формой поиска города.

**HTML:**

```html
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Погодное приложение</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 600px;
      margin: 50px auto;
      padding: 20px;
    }
    .search-form {
      margin-bottom: 20px;
    }
    input {
      padding: 10px;
      width: 70%;
      font-size: 16px;
    }
    button {
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
    }
    .weather-card {
      border: 2px solid #ddd;
      border-radius: 10px;
      padding: 20px;
      display: none;
    }
    .weather-card.visible {
      display: block;
    }
    .error {
      color: red;
      display: none;
    }
    .error.visible {
      display: block;
    }
  </style>
</head>
<body>
  <div class="search-form">
    <input type="text" id="city-input" placeholder="Введите город">
    <button id="search-btn">Найти</button>
  </div>

  <div class="error" id="error-message"></div>

  <div class="weather-card" id="weather-card">
    <h1 id="city-name"></h1>
    <div id="temperature"></div>
    <div id="description"></div>
    <button id="clear-btn">Очистить</button>
  </div>

  <script src="script.js"></script>
</body>
</html>
```

**JavaScript:**

```javascript
// База данных погоды (симуляция)
const weatherDatabase = {
  "москва": { temp: 15, desc: "Облачно" },
  "сочи": { temp: 25, desc: "Солнечно" },
  "казань": { temp: 18, desc: "Ясно" },
  "мурманск": { temp: 5, desc: "Снег" },
  "санкт-петербург": { temp: 12, desc: "Дождь" }
};

// TODO 1: Найди все необходимые элементы
// - Поле ввода #city-input
// - Кнопка поиска #search-btn
// - Карточка погоды #weather-card
// - Сообщение об ошибке #error-message
// - Элементы для отображения данных (city-name, temperature, description)
// - Кнопка очистки #clear-btn

// TODO 2: Создай функцию showWeather(city, temp, desc)
// Которая:
// - Скрывает ошибку (убирает класс 'visible')
// - Показывает карточку погоды (добавляет класс 'visible')
// - Заполняет данные о погоде
// - Устанавливает цвет температуры в зависимости от значения

// TODO 3: Создай функцию showError(message)
// Которая:
// - Скрывает карточку погоды
// - Показывает сообщение об ошибке
// - Устанавливает текст ошибки

// TODO 4: Добавь обработчик на кнопку поиска
// При клике:
// - Получи значение из поля ввода
// - Приведи к нижнему регистру (.toLowerCase())
// - Убери пробелы (.trim())
// - Проверь, есть ли город в базе данных
// - Если есть: вызови showWeather с данными
// - Если нет: вызови showError("Город не найден")

// TODO 5: Добавь обработчик на Enter в поле ввода
// Подсказка: событие 'keydown', проверка event.key === 'Enter'

// TODO 6: Добавь обработчик на кнопку очистки
// При клике:
// - Скрой карточку погоды
// - Скрой ошибку
// - Очисти поле ввода (input.value = '')
// - Верни фокус на поле ввода (input.focus())

// TODO 7 (бонус): Добавь список последних поисков
// Сохраняй последние 5 запросов и выводи их под полем ввода
```

**Проверь себя:**
- Поиск работает для всех городов из базы написанных в любом регистре
- Для несуществующего города показывается ошибка
- Enter в поле ввода запускает поиск
- Кнопка "Очистить" скрывает карточку и очищает поле
- Цвет температуры меняется корректно

---

## ✅ Чеклист раздела

После изучения темы ты должен:

- [ ] Понимать, что такое DOM
- [ ] Находить элементы через `querySelector`
- [ ] Изменять текст через `textContent` и `innerHTML`
- [ ] Работать с атрибутами элементов
- [ ] Изменять стили через `style` и `classList`
- [ ] Создавать новые элементы и добавлять в DOM
- [ ] Удалять элементы
- [ ] Добавлять обработчики событий через `addEventListener`
- [ ] Работать с объектом `event`
- [ ] Использовать `preventDefault()` для отмены действий по умолчанию

---

## 🔗 Ресурсы

- 📖 [Learn JavaScript: DOM](https://learn.javascript.ru/document)
- 📖 [Learn JavaScript: События](https://learn.javascript.ru/events)
- 📖 [MDN: DOM](https://developer.mozilla.org/ru/docs/Web/API/Document_Object_Model)
- 📖 [MDN: События](https://developer.mozilla.org/ru/docs/Learn/JavaScript/Building_blocks/Events)
- 🎮 [DOM Manipulator Game](https://dom-tutorials.appspot.com/static/index.html)

---

⏱ **Время на изучение**: 5-6 часов

---

**Предыдущая тема:** [2.1.4 Функции и области видимости](./2.1.4-Функции-и-области-видимости.md)

**Следующий раздел:** [2.2 Продвинутый уровень](../README.md#22-продвинутый-уровень)
